# wellperion-boot 이력(경위)

> 이 파일은 **부팅 때 읽지 않는다.** 규칙이 왜 그렇게 정해졌는지가 필요할 때만 연다.
> 현행 규칙 본문 = `SKILL.md`. 규칙을 바꿀 때 그 경위를 여기 날짜별로 append 한다
> (CLAUDE.md 「SSOT .md 규칙 — 누적기록 금지」의 짝 이력 파일).

2026-08-04: §2-1 점검 항목에 **#11 「GM 이 업무 시트에 남긴 새 항목이 월간운영계획에 반영됐나」** 신설 + 기존 #10(실무진 신고)에 **넘긴 뒤 회신까지 짝맞춤(받음→끝냄)** 문구 추가. 원인 = GM 지시(2026-08-03) "웰리한테는 내가 간단한 업무라도 기록해둘테니 월간운영계획에 기록을 해서 체크해줬으면 좋겠고, 실무진들 전달내용도 항상 체크해서 전달해줘." 실측(배10513) — 접수 자리는 업무 시트(`todo_list`) **생성자='김남욱GM'** 행으로 확인(담당자 배정되면 S3 로 넘어가 대상에서 빠짐 · L23). 같은 실측에서 실무진 피드백 열린 배 5척 확인, 1일 이상 묵은 것(275·299·304)은 이미 담당(cto·cpo)까지는 넘어갔으나 **넘긴 뒤 회신 여부**를 보는 칸이 없었다 — #10 에 그 칸을 얹었다. 새 스크립트·새 감시기 신설 없이 기존 아침 점검표에 항목 하나(#11)와 문구 한 줄(#10)만 얹었다(약속 L21).

2026-08-03: §2-1 권한 분기에 **「내가 할 수 있고 금지 목록에 없으면 내가 한다」** 추가(GM 재확정 · 08-01 규약을 넓힘). 원인 = 시토가 배포 2건(회원목록 캐시 예열 · 행번호 맹목쓰기 차단)을 GM 결재로 올렸는데, 둘 다 기존 것 개선·되돌릴 수 있음·안전이 느는 방향이라 올릴 자리가 아니었다. GM: "내가 결재할 건은 아닌 것 같은데? 시토가 판단해봐" → 이어서 "로그인세션도 가능하면 스스로 하고, 안 되거나 금지된 것만 올려라". **🔒보안이 3-트리거에서 빠져 기본 자율로 이동**했다(기존 인증 체계 재사용 한정 — 새 인증 체계 신설·비로그인 공개 확대는 여전히 결재, 시토 해석). 올리는 것은 ①내가 못 하는 것(GM 손·계정 필요) ②금지된 것(🚫💰📐🏷️) 둘뿐. 새 파일·새 게이트 0 — 이미 있던 권한 분기 절에 한 줄 얹었다(약속 L21).

2026-08-03: §2-1 점검 항목에 **#1-b 「도는 것 말고, 한 번이라도 성공한 적이 있나」** 추가(#1 을 쪼개 넣어 번호 재부여 없음 — 기존 1~10 참조 문단 그대로 유효). 원인 = 그날 하루에 같은 부류가 셋 나왔다: GAS 정리도구가 대상 누락으로 한 번도 안 지움(버전 감소 구간 0) · 무인 러너 되돌리기 5/5 실패 · GAS 버전경보 13일 발신 0건("이관"이 실제로는 삭제였다). 셋 다 기존 #1·#2·#5 를 **전부 통과**한다 — 예약 Ready, 산출물 존재, 코드 실재. 기존 항목이 전부 "장치가 살아 있나"를 묻고 **"장치가 결과를 낸 적 있나"를 묻는 칸이 없었다.** 판정은 성공 건수 숫자 하나면 되고, 0이면 없는 장치로 친다. 새 스크립트·새 감시기 신설 0 — 이미 7역할이 매일 지나가는 점검표에 한 줄만 얹었다(약속 L21). ▸같은 날 실제 수리: 정리도구 대상 추가(e1fda3125·버전 197→2) · 러너 안전망 교체(9a3f824bf) · 경보 원복+죽은 이관처 제거(net-zero).

2026-08-03: §2-1 시로·시뽀 배제 절에 **「묻지도 않는다」** 추가(GM 재확정 · 정본=`ssot/kpi.json` `_배제역할_2026_07_28` 에도 같은 문장 박음). 원인 = 이 규칙이 2026-07-27·07-28 두 번 확정되고 코드(`queue_dispatch.EXCLUDED_ROLES`)로도 막혀 있었는데, **막힌 것은 '배 생성'뿐**이라 그 도메인 파일의 처분을 GM께 **선택 카드로 묻는 경로**는 그대로 열려 있었다. 2026-08-03 시토가 무인 러너 스윕 사고(인사 허브 사진 2장 삭제 혼입)를 수리하면서 복구 여부를 GM 결재로 올렸고, GM이 "나우열M 담당이라 안 건드려도 되, 알아서할거야"라고 세 번째로 말해야 했다. 기존 조항의 "GM께 한 줄로 알리고 끝"을 시토가 **'알림'이 아니라 '결정 요청'으로 실행**한 것이 구멍 — 같은 문장에 '물음표를 붙이지 않는다'를 명시해 해석 여지를 없앴다. 새 파일·새 가드 신설 없이 7역할이 모두 지나가는 관문(이 절 + kpi.json 직독)에만 박았다(약속 L21). ▸사고 원인(러너 스윕)은 시토 소관이라 같은 날 별도로 수리 완료(배296 · 커밋 9a3f824bf).

2026-08-02: §2-1 점검 항목에 **#9 외부·상위 발신 본문을 실제로 열어 봤나** 신설(기존 #9 실무진 신고는 #10 로 밀림). 원인 = 배264(웰리가 CEO-2026-08-01-CHAIRMAN-REPORT 종료 시 남긴 '다음') — 2026-08-01 09:30 회장님 방 매출보고가 '발송 성공' 상태로 정상 나갔지만 본문 숫자가 주차매출 백필 반영 **전** 구값이었다. 새 값은 그날 오후 배포됐는데 발송 자체는 성공이라 어떤 감시기도 못 잡았고, 웰리가 다음날 본문을 직접 열어 보고서야 발견했다. 기존 #7(내가 내보낸 숫자가 진짜인가)·#8(내 결과물이 GM 화면까지 갔나)과 같은 계열 — "상태값 정상"과 "내용 정확"이 다른 것을 한 번 더 확인. 표 아래 번호 참조 문단(9번→10번, 1~8→1~9) 동기화.

2026-08-01: §2-1 에 **아침 루틴 4단계 권한 분기**(스캔→항로등록→판단→결정실행→기록) 박음. GM 원문 규약 확정 — 기존 `reversible`(가역/비가역) 축만 있던 데에 **작업 유형(수정·업데이트 vs 신규 생성)** 축을 새로 추가, **신규 생성은 가역이어도 항상 GM 승인**(백엔드=시토·프론트=웰리)으로 못박았다. 코드 게이트 = `queue_dispatch.py --work-type {update,new}`(미선언은 회귀 0) + `welly_orchestrate.select_autonomous_ships()`가 `work_type=="new"`를 자율 후보에서 제외(제외 사유 stderr 로그). `welly_auto_runner.py`는 같은 선별기를 그대로 재사용해 별도 배선 불필요(단일 관문 확인 완료). 웰리 판단으로 GM 원문의 3-트리거(💰🔒🚫) 외에 기존 금지 5종의 **📐전략·🏷️공식값은 계속 GM 결재로 유지**(자율화 대상 아님). `status/_ceo_log.jsonl`은 ceo_watcher 전용 완료-검증 로그라 아침 루틴 결과 배선이 없음을 확인 — 새 스크립트·새 로그 파일 신설 없음(약속 L21).

2026-07-27: §2-1 에 **①발견을 새 배로 만들지 않기(상설 배 흡수) ②남이 쓰는 것을 흔적 없다고 끄지 않기 ③AI 내부 문제는 GM께 올리지 않기** 3개를 박았다. 원인 = GM 지적 "우리가 현실 업무를 위해 AI가 작업하는 거 맞지? 지금은 AI를 위해 현실 업무가 늘어나는 구조 같아 너무 불안하다." 실측이 GM 말과 같았다 — 열린 배 84척 중 실무 29·AI 살림 55, 그날 GM이 내린 결정 6건 중 4건이 AI 내부 살림. 뿌리는 이 §2-1 자신이다: "본인 모듈을 훑어 발견 즉시 배로 올려라"가 AI 일감을 스스로 증식시켰다(GM이 유일한 감지기가 되지 않게 하려던 장치가, GM을 AI 관리인으로 만들었다). 같은 날 `chro-gmail-job-title-fetch`(나우열M 채용 업무)를 저장소 흔적 0건을 근거로 껐다가 GM 지적으로 되살린 실사고도 여기 같이 박았다 — 원격에서 도는 것은 저장소에 흔적이 없는 게 정상이고, **실무진이 쓰는 것은 담당 확인 없이 끄지 않는다**(나우열M = 인사 시로 / 재무 시뽀 소관, 웰리가 손댈 자리 아님).

2026-07-27: §2-1 에 **내 배의 `audience`·`reversible` 두 칸 채우기**를 아침 자가점검과 같은 자리에 붙였다. 원인 = 자율 착수 선별기가 '배가 하는 일'이 아니라 '배에 적어둔 설명 문장'을 낱말 스캔해 판정해 왔다 — 7역할 전수 실측(열린 배 81척) 결과 통과 15척, 탈락 66척 중 비가역 낱말 오탐 45·라이브/GAS 낱말 11. 설명을 자세히 쓸수록 차단되는 구조라 좋은 기록을 벌주고 있었다(GM 지적: ".bat 켤 때 자동 진행하는 역할과 안 하는 역할이 갈린다, 이 기준이 중요하다"). 수리는 새 판정기 없이 큐에 이미 있던 `reversible` 필드를 되살려 **선언값이 낱말 스캔을 이기게** 하고(미선언은 기존 스캔으로 폴백 — 회귀 0), 채우는 자리를 여기 관문 한 곳에만 뒀다(약속 L21). 코드=`queue_dispatch.py`·`welly_orchestrate.py`·`welly_auto_runner.py`.

2026-07-27: §1 에 `wellperion-gm-report` **스킬 로드 의무** 추가(7역할 공통). 원인 = GM 2026-07-25 확정한 「★표로만 답한다」(중간 과정·러닝 데이터까지 화면에서 뺀다)가 ①`ssot/약속.json` L18 ②`wellperion-gm-report` §4-2 두 곳에 제대로 적혀 있었고 ③부팅 `.bat` 7개 (8)단계도 그 스킬을 가리키고 있었는데, **어느 것도 스킬을 실제로 로드시키지 않았다** — 전 C-Level이 규칙 본문을 한 번도 읽지 않은 채 GM께 보고해 왔다(2026-07-27 GM 지적: "C레벨 전체 반영하기로 했는데 안 되어 있다"). 2026-07-25 §2-1 사고와 **같은 뿌리**(가리키는 것과 읽히는 것의 차이)라 같은 방식으로 막았다. 새 파일·새 훅·`.bat` 7개 수정 없이 **7역할이 모두 지나가는 관문 한 곳**(이 스킬 §1)에만 박았다(약속 L21). 각 에이전트 `.md`·`.bat` 는 미변경.

2026-07-27: §2-1 점검 항목 6→8 확장(#7 내가 내보낸 숫자가 진짜인가 · #8 내 결과물이 GM 화면까지 갔나). 원인 = GM 지적 "이거 오전에 찾아서 진행한 거야?" — 그날 아침 점검은 1~6만 봐서 '등록부 모듈이 살아 있나'만 확인했고, 정작 큰 것들은 전부 GM 질문에서 뒤늦게 나왔다: 발행 요약 4일 무발신(호출부가 옛 상태값만 보던 사본 결함) · IG 도달 6일 미커밋(커밋기가 실패해도 exit 0) · M1이 옛 발행상태 표시(검증 결과가 작업트리에 고립) · 문의 알림이 은퇴한 v1 백엔드에서 발신(트리거 미이전) · 강습 신규 등록 7일째 0(팀시트 11곳 735행에 회원정보 공백 → 원장 편입 불가). 다섯 건의 공통점 = **모듈은 "정상"인데 결과가 사람에게 닿지 않거나 숫자가 거짓**. 모듈 생사(1~6)만으로는 이 부류가 영원히 안 잡힌다. 새 스크립트·새 점검기를 만들지 않고 이미 도는 아침 점검 절차에 두 줄만 얹었다(약속 L21 관문에만).

2026-07-25: §2-1 아침 자가점검 실행 절차 신설(약속 L20 발효 완성). 원인 = 2026-07-24 GM 지시가 ①약속 L20 본문 ②`Start-AI {ROLE}.bat` 7개의 (5)단계 지시문 두 곳에는 박혔는데, 그 지시문이 가리키는 **「wellperion-boot 스킬 §2-1」이 이 파일에 존재하지 않았다**(§2 다음이 §3). 전 역할이 "자가점검하라"는 명령만 받고 **무엇을 어떻게 보라는 절차는 못 받은 상태**로 하루를 돌았다. 배 10019(부팅 .bat 배선)는 "7개 .bat에 문장이 있는지"까지만 검증하고 **가리키는 대상의 실재 여부를 안 봐서** 완료로 닫혔다 — 그래서 §2-1 본문에 점검 항목 #5(죽은 포인터)와 완료 게이트(포인터 실재 확인 의무)를 같이 넣었다. 약속 L02("정한 건 코드로 박는다")·L21(관문에만·새 파일 0)에 맞춰 새 스킬·새 스크립트 신설 없이 빈 절만 채웠다.

2026-07-23: §7 부팅 직후 자율 진행 신설(GM go). "부팅되자마자 항로 확인해서 웰리 판단하에 자율 진행" 지시를 문서가 아니라 **코드 진입점**으로 박음 — `welly_auto_runner.py`에 `boot_candidate()`/`--boot-candidate`(읽기전용, 큐·상태·로그 미기록) 추가해 예약 러너와 **동일한 선별·모호성 게이트**를 부팅 세션이 재사용하게 했다. 새 선별 엔진을 만들지 않은 이유 = 게이트가 두 벌이 되면 부팅 경로가 우회로가 되기 때문(헌법 불변원리 1·약속 L01). headless `RUNNER_LIVE` 게이트는 미변경.

2026-07-23: §6 부팅 선언 표 표준 신설(7역할 공통·CEO 포함). 원인 = `Start-AI *.bat` 7개가 "페르소나·KPI·권한 마크다운 표 선언"이라고만 지시하고 **양식을 정의하지 않아** 역할마다 칼럼명(`항목|기본값`·`항목|정의`·`구분|처리주체`·`묶음|정의`)·행 구성·이모지 사용이 전부 달랐다(GM 지적). 같은 커밋에서 7개 `.bat`의 (5)단계를 이 §6 포인터로 교체하고, (7)단계에 하드카피돼 있던 옛 L18 표 요소(🔴이슈·🔧해결 — 2026-07-22 폐기분)를 `wellperion-gm-report` 스킬 §4 포인터로 교체했다(약속 L01 한 곳만 본다).

2026-07-22: `ai-cfo`·`ai-chro`·`ai-cmo`·`ai-coo`·`ai-cpo`·`ai-cto` 6개 파일에 바이트 단위로 복붙돼 있던 §2(부팅 흡수)·§5(연동 도구)·§6(한국어)·§7(자율 실행) 공통 블록을 이 스킬로 추출. 각 에이전트 `.md`는 스킬 참조 + 최소 크리티컬 인라인 지시(§1 헌법 직독)만 남기고, 역할 고유부(§1 ERP 메뉴·§3 보고 라인·닉네임·페르소나)는 그대로 유지.


---

## §2-1 점검 항목이 왜 그렇게 늘었나 (2026-08-06 본문에서 이관)

> **10번이 왜 9번 다음이 아니라 가장 무거운가(2026-07-30 GM 지적 "오전에 문제점 다 찾고 있는거지?"):**
> 그날 실측이 GM 물음에 아니라고 답했다 — 오늘 큰 것 7건이 **전부 GM 이 먼저** 짚었고, 아침 점검이 스스로 찾은 것은 2건(둘 다 AI 살림)뿐이었으며 그 2건은 **저녁까지 안 고쳐져 있었다.**
> 결정적 한 건: **예약 달력의 예약자 이름이 다른 사람으로 바뀐 사고**는 2026-07-28 에 실무진이 이미 신고(FB260728-112703)해 둔 것이었다. **2일을 묵었고**, 그날 아침 점검은 「내 모듈이 도는가」만 보느라 그 신고를 한 번도 쳐다보지 않았다. GM 이 이틀 뒤에 물어서야 원인 추적이 시작됐다.
> 점검 항목 1~9 는 전부 **기계**를 본다. 사람이 손들어 올린 신호를 보는 칸이 없었다. 웰리·시포·시우처럼 실무진을 가진 역할에게는 이게 1~9 보다 먼저다.

> **1-b 가 왜 필요한가(2026-08-03 · 하루에 세 번 같은 부류):** 그날 나온 큰 것 셋이 전부 **"등록돼 있고 켜져 있는데 실제로는 아무 일도 안 하던 것"**이었다. ①GAS 버전 정리도구 — 대상 목록에 정작 문제인 프로젝트가 빠져 있어 **한 번도 지운 적이 없었다**(버전수 07-24 145→08-03 197, **감소 구간 0**). ②무인 러너 스윕 되돌리기 — 5회 발동해 **5회 다 실패**(`revert.ok` 가 true 인 적 0), 게다가 엉뚱한 커밋을 겨냥하고 있었다. ③GAS 버전 경보 — "다른 곳으로 이관"한다며 옛 경로를 끊었는데 새 목적지가 한 번도 라이브로 안 켜져 **13일간 발신 0건**, 그 사이 🔴 진입을 아무도 몰랐다.
> 셋 다 #1(어제 돌았나)·#2(산출물 있나)·#5(포인터 살아있나)를 **전부 통과한다** — 예약은 Ready고 파일도 있고 코드도 실재한다. 갈라지는 지점은 딱 하나, **"그래서 결과가 바뀐 적이 있나"**다. 판정은 숫자 하나면 된다: 그 장치가 낸 성공 건수. **0 이면 그 장치는 없는 것이다.**

> **7·8이 왜 뒤늦게 추가됐나(2026-07-27):** 그날 아침 점검은 1~6만 봐서 '등록부 모듈이 살아 있나'만 확인했고, 정작 큰 것들은 전부 GM 질문에서 나왔다 — 발행 요약 4일 무발신 · 도달 데이터 6일 미커밋 · M1이 옛 상태 표시 · 문의 알림이 은퇴한 백엔드에서 발신 · 강습 신규 등록 7일째 0(수집 정지). 공통점은 **모듈은 "정상"인데 결과가 사람에게 닿지 않거나 숫자가 거짓**이었다는 것. 모듈 생사만 보면 이 부류는 영원히 안 잡힌다.

> **왜 코드로 옮겼나(2026-07-28 GM 지적 "왜 계속 잊어버리는걸까"):** 규칙이 ①이 문서에만
> 있었고 ②문장이 **"웰리는"으로 시작해** 다른 역할이 읽으면 자기 얘기가 아니었다. 그래서
> 시토가 전사 AI 큐를 정리하면서 시로 배 2척을 **배제하는 대신 정성껏 다시 써 올렸다** —
> 규칙을 어긴 게 아니라 규칙이 자기에게 안 걸려 있었다. 주어를 '웰리'에서 **'AI 전원'**으로
> 바꾸고, 역할과 무관한 관문에 박아 문장을 읽지 않아도 막히게 했다(약속 L02·L21).

---

## SKILL.md 정리 — 본문에서 이관 (2026-08-13 · CLAUDE.md 「SSOT .md 규칙」 준수)

> SKILL.md 가 23,598자로 부팅 창의 최대 항목(시토 실측 108,675자 중)이었다. 아래는 규칙 문장은
> 그대로 SKILL.md 에 남기고, "왜 그렇게 정했나"의 경위 서술만 이곳으로 옮긴 것이다(규칙 삭제 0).

**§1 자율화규약 직독 — 왜 "가리키는 것과 읽히는 것은 다르다"인가:** 2026-07-27 에 `.bat` 7개가
`wellperion-gm-report` 스킬을 가리키기만 하고 아무도 안 읽어 전 역할이 보고 규칙을 모른 채 일한
사고가 있었다. 같은 실수를 반복하지 않으려고 자율화규약 직독도 같은 관문에 박았다.

**§1 GM 신호 원장 기록 — 왜 여기 적혀 있나:** 프로필은 `status/gm_observation_ledger.jsonl`
**하나만** 읽는다. 안 남긴 교정은 대화에만 있다가 세션 종료와 함께 사라지고, 다음 세션의 다른
역할이 같은 지적을 다시 받는다. 실측(2026-07-30): 원장 249건 중 GM 세션신호는 **10건(4.1%)** —
학습기는 도는데 학습할 데이터가 안 들어가고 있다. 흡수 경로는 2026-07-29 발효했는데 저장소
어디에서도 이 인자를 부르지 않아 하루 동안 기록이 1건뿐이었다(경로는 있고 아무도 몰랐다).

**§2 큐 슬라이스 — 왜 통째로 안 읽나:** 큐 파일은 22만 자다. GM 은 항상 셸 5개로 일하시므로
통째로 읽으면 부팅 한 번에 100만 자가 넘게 든다(GM 지적 "`.bat` 여는 것만으로 맥락·토큰이 엄청
든다"). 역할 슬라이스는 1,200~4,700자 — 실측 98% 절감.

**§2-1 「맨 먼저 GM 화면을 먼저 연다」 — 왜 최우선인가:** 2026-07-27 하루 동안 큰 문제 5건을
전부 GM이 먼저 찾았다(AI 배 과다 · G1 안 읽힘 · AI를 위해 현실 업무가 늚 · 텔레그램 보고 안 읽힘
· 배가 있는데 대기). 같은 날 자가점검이 찾은 건 1건뿐이었다.

**§2-1 「남이 쓰는 것을 흔적 없다고 끄지 마라」 — chro-gmail-job-title-fetch 사고(2026-07-27):**
나우열M 채용 업무용 `chro-gmail-job-title-fetch` 를 "저장소에 코드·산출물 흔적 0건"을 근거로
껐다가 GM 지적으로 되살렸다. 원격(구글 계정·외부 서비스)에서 도는 것은 저장소에 흔적이 없는 게
정상이었다.

**§2-1 「낡은 지시 금지」 — 2026-07-25 실사고 3회:** 하루에 같은 원인으로 3번 — 가리키는 절이
없는데 완료로 닫음(배10019) · 이미 이관 끝난 결재 5건을 승인표로 만들어 GM 께 올림 · 어제 GM 이
반려한 인재상 반영을 다시 착수(배9963).

**§2-1 「가짜 완료 금지」 — 실사고:** §2-1 절 자체가 없는 상태로 7개 부팅 파일이 하루 종일 거기를
가리켰고, 검증이 "문장 있음"에서 멈춰 완료로 닫힌 적이 있다(2026-07-25).

**§2-1 시토 겸직(전사 AI 업무 총괄) — 시설 우선 순위 실측:** 2026-07-27 실측에서 AI 배 31척을
지는 동안 법정 안전점검 15건이 방치됐다. 그래서 "시설·안전·점검이 항상 우선"을 겸직의 유일한
실패 조건으로 못박았다.

**§2-1 시로·시뽀 「묻지도 않는다」 — 2026-08-03 실사고:** 무인 러너가 쓸어담은 커밋에 인사 허브
사진 2장(`chro/hub/photos/r125·r126`) 삭제가 섞였다. 시토가 복구할지 말지를 GM께 선택 카드로
올렸고, GM이 이 규칙을 다시 말해야 했다. 사고 자체(러너 스윕)는 시토 소관이라 고치는 게 맞았지만,
그 도메인 파일을 어떻게 할지는 처음부터 물을 자리가 아니었다.

**§2-1 「침범 금지」 — 2026-07-27 실사고:** 웰리가 "배로 넘기면 된다"고 읽고 시로·시뽀 역할로
에이전트를 띄워 그 도메인 파일을 고쳤다(근무표·연차 퇴사 표기 등). 같은 날 `chro-gmail-job-title-fetch`
를 흔적 없다는 이유로 껐다가 되살린 건도 같은 뿌리 — 웰리 부팅 선언에 "침범 금지"라고 적어두고
웰리가 어긴 사례였다.

**§2-1 「AI 내부 문제는 GM께 올리지 않는다」 — 2026-07-27 실측:** GM이 내린 결정 6건 중 4건이
AI 내부 살림이었다(잡코리아 모듈 온오프·북극성 카드 삭제·공지 감시 장치 신설 여부·AI가 낸 원장
오염 수습).

**§2-1 「찾은 것을 새 배로 만들지 마라」 — 2026-07-27 GM 지적·실측:** GM: "AI를 위해 현실 업무가
늘어나는 구조인 것 같아 너무 불안하다." 실측 — 열린 배 84척 중 실무진·GM 일 29척, AI 내부 살림
55척. 그 55척을 만든 주범이 이 자가점검 절 자신이었다.

**§5 「끝낸 배는 실행한 쪽이 직접 닫는다」 — 왜 바꿨나:** 그 전엔 "큐는 웰리가 중앙에서만
갱신한다"였는데, 그 규칙이 하루에 중복 배정을 두 번 만들었다(배10310·10348 — 이미 끝난 일을
웰리가 다시 시킴). 실행은 끝났는데 배가 PENDING 으로 남아 있으니 웰리가 또 후보로 집었다.

**§5-2 `safe_commit.py` — 왜 만들었나(2026-07-23):** 여러 세션이 작업트리 하나·인덱스 하나를
공유하는 구조라 `git add` 로 올려둔 파일이 다른 세션의 `git commit` 에 통째로 딸려 들어갔다.
하루에 5회 발생했고, 그중 1회는 옛 사본이 최신 코드를 덮어써 기능이 되돌아갔다. 또 한 번은 커밋
가드가 막지 않았으면 활성 배 2척이 사라질 뻔했다. 커밋 후 HEAD 확인 규칙도 같은 날 사고에서
왔다 — "커밋 성공" 출력만 믿었다가 다른 커밋에 편입된 것을 놓칠 뻔한 적이 있다.


---

# 2026-09-05 이관 — CLAUDE.md 「SSOT .md 규칙(누적기록 금지)」 준수

SKILL.md 를 24,074자 → 규칙만 남기고, 아래 블록을 원문 그대로 옮겼다. 스킬 본문의 그 자리엔 `▸경위 = 이력.md` 한 마디만 남는다.

## 2026-09-05 이관 — 머리말 · 이 스킬이 왜 생겼나

이 스킬은 **6개 비-CEO C-Level 에이전트(ai-cfo·ai-chro·ai-cmo·ai-coo·ai-cpo·ai-cto)에 바이트 단위로 복붙돼 있던 공통 부팅·연동·자율실행 절차**를 한 곳으로 추출한 것이다(2026-07-22, 중복 제거).

## 2026-09-05 이관 — §1 · `status/gm_profile.md` 부팅 슬라이스

  - **같이 흡수: `status/gm_profile.md`** (2026-07-27 웰리 결정). 형식은 위 스킬이, **GM이 실제로 무엇을 싫어하고 무엇에 즉답하는지**는 이 파일이 갖고 있다 — 둘 다 없으면 매번 GM이 같은 지적을 반복하게 된다. **여기서 파일을 직접 열지 않는다** — §2 `hangro_board.py --role` 명령이 「📋 GM 프로필 요약(부팅 슬라이스)」로 `선호·습관·자주 놓치는 것` 3섹션(전체 5,854자 중 ~1,700자)을 함께 낸다(배433 · 2026-08-06). 표면점검·북극성·루틴준수 등 **자가점검용 상세가 필요할 때만** 파일 자체를 연다. ▸읽을 때 주의: 이 프로필은 **날짜 표기가 있는 항목만 확정**이다. 날짜 없는 서술은 기계 자기기록만으로 추정된 것일 수 있으니 잠정으로 읽는다(전체를 열면 프로필 안 「근거 정직 표기」 절이 그 비율을 매일 적는다).

## 2026-09-05 이관 — §2 · 큐를 통째로 읽지 않는 이유

  - **왜:** 큐 파일은 22만 자다. 통째로 읽으면 부팅 한 번에 맥락·토큰이 크게 든다 — 역할 슬라이스는 **1,200~4,700자**.
  - **이 한 줄이 §6 KPI값·§1 GM 프로필까지 같이 낸다(배433 · 2026-08-06).** 출력 끝에 `📌 담당 KPI(...)`(→ §6 표 값)와 `📋 GM 프로필 요약`(→ §1 gm_profile.md 흡수)이 자동으로 붙는다 — 두 파일을 따로 열 필요 없다.

## 2026-09-05 이관 — §2 · 미완 GM 지시를 손으로 세면 허수가 나온다 (2026-09-03 실측)

- 부팅 지시문은 "area 가 GM지시이고 result=warn 인데 같은 ref 의 ok 가 없는 것"을 미완이라 적는다. **그 방식으로 세면 허수가 나온다** — 완료 기록의 `area` 는 그때그때 다르게 적혀 왔기 때문이다(실측 2026-09-03 웰리: GM지시 412 · GM요청 98 · 검수 1 · 판정 2 · 큐정리 1). area 까지 같아야 닫힌 것으로 세면 이미 끝난 지시가 미완으로 쌓인다.
- 실측: 그 방식으로 센 미완 **86건**이 정본 기준 **0건**이었다(전 역할 0건). 짝은 **ref 로만** 맞춘다.

## 2026-09-05 이관 — §2-1 · 찾은 것으로 배를 만들지 않는다 (GM 원문 2026-08-19)

**★★찾은 것으로 배를 만들지 않는다 (GM 확정 2026-08-19 · 이 절에서 가장 강한 규칙).**

GM 원문: *"아침에 자가점검을 토대로 배편을 만들지말고, 전체 정리를해주기만해(어디에서 나에게 보여줄지는 선택해). 정리를 한 것 들을 기준으로 한번 더 나랑 의논을해서 내가 전달을 해줄게, 그걸로 배편을 만들거나 아니면 그냥 참고만 하면될 것 같아."*

  - 보여줄 자리를 셸로 고른 이유 = GM 이 아침에 웰리·시토 두 창을 여시고, 그 자리에서 바로 의논이 이어진다. 새 화면·새 알림을 만들지 않는다(약속 L21).
- **그 표를 보고 GM 이 전달해 주신 것만 배가 된다.** 전달이 없으면 그 항목은 참고로 끝난다 — 다음 아침에 또 올라와도 정상이다.

**★찾은 것을 새 배로 만들지 마라 (2026-07-27 GM 지시 → 2026-08-19 더 강해졌다).**
2026-07-27 판은 "상설 배 1척에 줄로 붙여라"였다. **2026-08-19 GM 이 그것도 없앴다 — 아예 배로 만들지 말고 표로만 낸다**. 상설 수집 배(웰리 배233 등)는 받을 것이 없어져 종결했다.

## 2026-09-05 이관 — §2-1 · GM 화면을 GM보다 먼저 여는 이유

**원인 = 점검 대상이 "내 모듈이 도는가"였다.** 모듈은 다 돌고 있었고, 망가져 있던 건 **GM이 보는 화면**이었다. GM이 유일한 감지기가 되지 않게 하려던 절이 정반대로 작동했다.

  2. **사실인가** — 화면 숫자가 원천과 같은가. 며칠 전 값이 굳어 있지는 않은가(2026-07-27 실측: GM 일일 요약이 7일째 옛 숫자였다).

## 2026-09-05 이관 — §2-1 · 점검표 #9 · #10-b 실사고 서술

**#9 「외부·상위 발신 본문을 실제로 열어 봤나」 문제로 치는 칸 원문:** 2026-09-02 실사고: 경보를 받고 프로그램 응답만 대조해 "거짓 경보"라 GM 께 잘못 보고했다 — 실제로는 값을 쓰는 시트와 읽는 시트가 갈려 있었고, 이미지를 한 번 열었으면 3분에 끝났다. **발신은 됐는데 내용이 옛값.** 상태값은 정상이라 어떤 감시기도 안 잡는다. 실측(2026-08-02 웰리): 2026-08-01 09:30 회장님 방 매출보고가 정상 발송됐지만 본문 숫자는 주차매출 백필 반영 **전** 구값('40.5억/72억 56%')이었다 — 새 값(40.69억·56.5%)은 그날 오후 배포됐는데 발송 자체는 성공이라 아무도 몰랐고, 웰리가 다음날 본문을 열어 보고서야 발견했다.

**#10 「실무진 신고가 며칠 묵었나」 원문 꼬리:** **넘긴 걸로 끝내지 않는다** — 넘어간 뒤 담당이 신고자에게 실제로 회신했는지까지 짝을 맞춘다(받음→끝냄, 2026-08-04 GM 지시로 확장). 담당 배정만 되고 회신이 없는 채로 묵으면 여전히 미해결이다.

**#10-b 실측:** 아침 카톡은 이 칸만 읽는다(잘린 배 제목을 보내던 것을 2026-08-06 에 끊었다) — 칸이 비면 그 일은 사람에게 영영 안 알려지고, 발신 실패가 아니라 '보낼 게 없음'으로 보여 어떤 경보도 안 울린다. 실측(2026-08-06): 대상 15척 전원 공란.

## 2026-09-05 이관 — §2-1 · 배 세 칸 `reversible` 실측

| `reversible` | `true`(되돌릴 수 있음) / `false`(발행·배포·삭제·외부전송·결제·보안·전략·공식값) | 자율 착수 판정이 **배 설명글의 낱말**로 떨어진다 — 설명을 자세히 쓸수록 차단된다(2026-07-27 실측 오탐 45척) |

## 2026-09-05 이관 — §2-1 · 권한 분기 2026-08-03 GM 원문·배경

  GM 원문 ①"내가 결재할 건은 아닌 것 같은데? 시토가 판단해봐" ②"**로그인세션도 가능하면 스스로 하고, 안 되거나, 금지된 것만** 나한테 승인·결재 올리면 되지."
  - **판정 한 줄: "내가 할 수 있고, 금지 목록에 없으면 — 내가 한다."** 되묻는 것이 안전한 게 아니라 GM 시간을 쓰는 것이다.
  - ▸배경: 2026-08-03 시토가 배포 2건(회원목록 캐시 예열 · 행번호 맹목쓰기 차단)을 결재로 올렸고 GM 이 "그건 네가 판단할 일"이라고 돌려보냈다. 둘 다 **기존 것 개선 · 되돌릴 수 있음 · 안전이 느는 방향**이라 애초에 올릴 자리가 아니었다.
- **★웰리 판단(2026-08-01).** GM 원문은 3-트리거만 적었지만, 기존 금지 5종(💰결제·🔒보안·🚫금지·📐전략·🏷️공식값)의 나머지 둘 — **📐전략·🏷️공식값은 계속 GM 결재로 유지한다.** 자율로 바꾸면 사고가 난다(연 매출 목표·주차 매출 포함 여부가 전부 공식값 결재 사례였다). GM이 달리 정하면 그때 바꾼다.
- **⑤기록:** 배 등록·커밋·푸시(§5-2)로 이미 `status/_queue.json`에 남는다. `status/_ceo_log.jsonl`은 ceo_watcher 전용 완료-검증 로그라 아침 루틴 결과를 받는 배선이 없다(2026-08-01 확인 — 미배선, 새로 만들지 않음).

## 2026-09-05 이관 — §2-1 · 실무진 업무를 찾으라는 GM 원문·본보기

GM: "G1 에는 가능하면 **실무진 업무들을 만들어서** 알려줘. 이번에 실무진 피드백 만든 것처럼, **실무진이랑 AI 가 소통하면서 자동으로 돌아가는 게** 제일 좋다."
자가점검의 무게중심을 **내 모듈 고치기 → 내 실무진 도울 거리 찾기**로 옮긴다. 위에서 AI 살림 배를 줄인 자리를 이걸로 채운다.
- **되는 형태의 본보기(2026-07-27 실제로 돌아간 것):**
  - 실무진이 화면에서 불편을 남김 → 자동으로 담당 C-Level 배가 섬 → 고침 → **그 사람 화면에 회신이 뜸**(이경연 실장 조기방문 건).
  - 실무진에게 **프롬프트를 건네 직접 구현하게 함** — 나우열M 이 구매·비품 3탭을 그날 안에 완성(AI 가 대신한 게 아니라 더 빨리 하게 도움).

## 2026-09-05 이관 — §2-1 · 시로·시뽀 불가침 GM 원문

두 역할은 **나우열M 이 운영하는 영역**이다. GM: "시로랑 시뽀는 이제부터 건드리지 말자."
- **★★묻지도 않는다 (2026-08-03 GM 재확정 · "나우열M 담당이라 안 건드려도 된다, 알아서 할 거야").**
  '알린다'와 '결정을 청한다'는 다르다. 그 도메인 파일을 **되살릴지·지울지·고칠지 GM께 선택지로 올리는 것도 금지**다 — 올리는 순간 나우열M 이 알아서 할 일이 GM 결정거리로 바뀐다.

## 2026-09-05 이관 — §2-1 · AI 내부 문제 비율 실측

**★AI 내부 문제는 GM께 올리지 않는다 (2026-07-27 GM 지적 · 웰리 규칙).**
GM 시간이 회사가 아니라 AI를 돌보는 데 쓰이면 GM이 말한 "AI를 위해 현실 업무가 늘어나는 구조"가 된다.
- **매일 잰다:** ①실무 배 : AI 배 ②GM이 내린 결정 중 AI 내부 건 비율. 두 숫자가 안 내려가면 이 규칙이 안 먹은 것이다. 목표 = **AI 살림 배가 실무 배보다 적어질 것**(2026-07-27 기준 55 vs 29 — 역전 전까지 "AI가 현실 업무를 돕고 있다"고 말하지 않는다).

## 2026-09-05 이관 — §6 · 값 출처 슬라이스 자수

**값 출처(직독 순서):** ① `📌 담당 KPI` 슬라이스(§2 `hangro_board.py --role` 출력에 자동 포함 — kpi.json 12,694자 중 297자 수준만 실림, 배433 · 2026-08-06) ② S2 본인탭 `data-panel={role}`(페르소나·핵심역할·실무진) ③ `ssot/약속.json`·`ssot/CONSTITUTION.md`(권한·게이트).

## 2026-09-05 이관 — §7 · 병렬이 기본인 이유 (2026-07-30 GM 지적)

   - **왜 적었나:** 이 절에 "후보 배 1척이 원칙"만 있어서 부팅 세션들이 그걸 **안전 상한**으로 읽고 하루를 한 척씩 직렬로 썼다. GM이 물어야 병렬로 바뀌는 상태였다 — 하루 처리량이 GM 기대치보다 구조적으로 낮았다.

## 2026-09-05 이관 — §8 · S2 공통탭에서 옮겨 온 경위 · §8-1 GM 원문

> 2026-08-08 GM 지시로 S2 공통탭에서 옮겨 왔다("나열된 글로 하지말고 스킬을 박제해서 진행하는게
> 더 효율적"). S2 는 이제 이 절을 가리키는 한 줄만 둔다 — 같은 규칙을 두 곳에 두지 않는다(약속 L01).

**§8-1 GM 원문(2026-08-19):** *"배를 처리하고, 다음에 이어서 뭔가를 계속 하려고 하다보니 어쩔수없이 이어서 배가 늘어난 것 같은데, 이것도 없었으면 좋겠어. 억지로 안만들어도되, 그냥 종결처리해도되."*

**§8-1 종전 촉구 장치:** 촉구 장치(표류 판정·다음 자동보강)는 2026-08-19 에 전부 삭제했다. "다음에 ~하면 확인" 류 조건 대기 배도 그날 전부 종결했다.

**§8-2 종전 ③번:** 종전 ③번 '다음'(`--next`/`--terminal` 빈칸 통과 금지)은 **2026-08-19 GM 지시로 폐지**했다. 안 적으면 종결이다.
