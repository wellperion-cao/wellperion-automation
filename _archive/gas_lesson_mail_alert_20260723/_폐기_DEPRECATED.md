# 폐기 — 2026-07-23 GM 결정

「웰페리온 | 강습 문의 알림」(`자동 접수 알림.js` · Apps Script `onFormSubmit` 구글폼 제출 트리거)은 **폐기**되었습니다.

🚫 **이 파일을 GAS 프로젝트에 되살리지 마십시오.**

- **폐기일:** 2026-07-23
- **결정자:** GM (지시 원문 — "지금 강습문의 들어오면 담당자에게 메일 보내는건 삭제해서 필요가 없어, 자체폼으로 신청되면 텔레그램 알림이 뜨니까" / 재확인 "응 메일 알림은 삭제해야해")
- **사유:** 트리거 원본인 **구글 폼이 삭제**되어 이 알림은 더 이상 발화하지 않는다. 강습 문의 접수는 **자체 폼 + 텔레그램 알림**으로 대체 완료.
- **발견 경로:** 2026-07-23 콘텐츠 접수 GAS(`instructor_intake.js`) 배포 작업 중, 같은 GAS 프로젝트(scriptId `1Q5Riwzq…yta4PhGP0`)에 **동거하는 파일**로 발견되었다. 종전 `instructor_intake.js` 주석의 "완전 독립 전용 프로젝트" 서술은 사실이 아니었다.

## ★ 보안 경고 — 재사용 금지

이 파일 **103행에 텔레그램 봇 토큰(@namuki_report_bot)이 평문 하드코딩**돼 있었다.

- 라이브 GAS 에서는 **2026-07-23 제거 완료**(파일 자체를 프로젝트에서 삭제 → 새 버전 배포).
- **본 보존본에서는 토큰 값만 `__REDACTED_2026-07-23__` 로 마스킹**했다. 이 저장소는 GitHub Pages 로 공개되기 때문이다. 그 외 코드는 원본 그대로다.
- 토큰 자체의 저장소 이력 노출은 **별건**이며, 재발급·이력 정리는 시토(CTO) 배 `CTO-2026-07-23-BOT-TOKEN-ROTATION` 으로 인계했다.
- 이 파일의 코드를 복사해 다른 스크립트에 쓰지 말 것. 토큰은 반드시 **GAS ScriptProperties** 로 주입한다.

## 동작 (폐기 전 · 기록용)

폼 제출 시 ① 희망종목 → 담당 팀장 G메일 라우팅(미매칭 시 `cao@wellperion.com` 폴백) ② 핵심멤버방 텔레그램(group `-5065206276`)에 동일 본문 전달. 본문에 접수 시트 원본 링크가 포함돼 있었다(→ `scripts/precommit_sheet_link_guard.py` 신설 계기).

## 남아 있는 것

- 접수 시트 `1b0XU1oTHlXzBhEzUOar5GEm44vjopdO25qfsh-awDXw` 는 **DB 로 계속 사용 중** — 삭제 대상 아님.
- 같은 GAS 프로젝트의 `instructor_intake.js`(콘텐츠 접수 `doPost`·`doGet`)는 **정상 가동** — 무관.
- 기록보존: git 이력 + 본 마커 + 이 폴더의 원본 사본(토큰 마스킹).
