/*!
 * _gm_direct_tasks.js · GM업무.html · 월간운영계획.html(GM 토글) 공용 소스 (2026-08-05 GM 지시 —
 * "구매 관련은 시뽀 건이라 건드리지 말고, 김남욱GM 업무 진행건은 업무 시트에서 가져올 생각 말고 별도로
 * 만들어" — 정본을 업무 시트(todo_list)에서 status/monthly_ops_plan.json 해당월 목표로 전환). 대표님 칸은
 * 여전히 업무 시트(todo_list)를 그대로 쓴다(월간운영계획.html readWorkApproval() 참조) — 두 칸이 서로 다른
 * 소스를 쓰는 것은 GM 지시에 따른 의도된 분리다.
 * 판정 기준: 해당월 objectives 중 ①제목에 "(GM 직접)" 포함(GM 이 직접 관장하는 건, 실측 예: 리셉션
 * 상담실 리뉴얼 · 목욕탕 허가건 진행 · 지원부 약품·비품 비교 구매 구조 만들기) 또는 ②id 가 웰페리온
 * ERP/브로제이 건(2026-08-08) 단 하나(2026-08-07 GM: "GM 업무 화면에 내걸로 넣어놔줘" — 같은 top3 중
 * 매출·회원서비스 2건은 "1번은 운영부 매출이라 빼줘, 2번도 운영부" 로 제외 지시). 원본 데이터엔 카테고리 칸이 없어 목표 내용을 보고 업무 시트와 동일한 9종 체계로 사람이
 * 분류해 아래 표에 고정했다(회장님_지시사항.html CHAIRMAN_ITEMS.cat과 같은 방식) — 새 건이 늘면 이 표에
 * 한 줄만 추가한다(약속 L01, 로직 복제 없음). 판단이 서지 않는 항목은 '분류 미정'(지어내지 않음).
 */
(function () {
  "use strict";
  var GM_TAG = '(GM 직접)';
  // id → 9종 체계 카테고리(시트 9종: 매출 및 영업/인사/파트너팀/운영 정책/시설 및 환경/회원·CS/IT·시스템·자동화/교육·조직문화/회의)
  var CATEGORY_BY_ID = {
    '2026-08-18': '시설 및 환경', // 리셉션 상담실 리뉴얼 — 공사·공간 전환
    '2026-08-21': '시설 및 환경', // 목욕탕 허가건 진행 — 시설(목욕탕) 인허가
    '2026-08-23': '운영 정책',    // 지원부 약품·비품 비교 구매 구조 — 내부 프로세스 설계
    '2026-08-07': '운영 정책',    // 4개 부서 통합 관리 안정화 — 부서 운영 체계(2026-08-05 GM 담당 확정 건)
    '2026-08-24': '매출 및 영업', // 오넛티 추석 선물세트 2차 — 제휴 판매 프로젝트(2026-08-06 GM 등록)
    '2026-08-25': '인사',         // 실무진 업무 점수 체계 — 평가·보상 축(2026-08-06 GM 지시)
    '2026-08-26': '운영 정책',     // 운영부 단일 보고 라인 — 조직 지휘 체계(2026-08-06 GM 확정)
    '2026-08-27': '운영 정책',     // 상가(아르모니움) 소통 체계 — 외부 상가 운영 협의(2026-08-06 GM 등록)
    '2026-08-28': '시설 및 환경',  // 세탁물 공급 부족 — 타월·운동복 공급(업체 배송 배분) 문제(2026-08-06 GM 등록)
    '2026-08-30': '매출 및 영업',  // 법인 멤버십 판매 시작 — 기업·공관 대상(2026-08-10 GM 결정 "판다, 바로 준비")
    // 2026-08-31(8월 매출 부족분)은 2026-08-10 GM 지시로 삭제·보관 — 축별 항목(법인·요금인상)이 각자 서 있어 상위 묶음이 중복이었다.
    '2026-08-32': '매출 및 영업',  // 회원권 요금 인상 2026년 10월 시행 준비 — 실행은 시포, GM이 챙기는 건(2026-08-10 GM 결정). 공간 디자인(2026-08-18)이 1단계 하위
    '2026-08-33': '시설 및 환경',  // CCTV 전체 교체 — 93대 NVR 비딩(시설 보안 설비)
    '2026-08-34': '회원·CS',      // 회원 서비스 개선 — 편의 설비·집중 서비스·프로세스
    '2026-08-50': '시설 및 환경',  // 감염병 예방 강화 — 중국 코로나 재확산 대비(회장님 말씀 2026-08-25). 위생·시설 운영 축이라 시설 및 환경
    '2026-08-48': '회원·CS',      // 종합접수처 밀린 접수 회신 — 중간관리자 방 회신을 GM 이 체크(2026-08-21 지시)
    '2026-08-39': '회원·CS',      // 회원 유지 — 이탈 원인·재등록 대기·회원 데이터 정확도
    '2026-08-40': '교육·조직문화', // 조직문화 — 회장님 행사·직원 교육
    '2026-08-51': '매출 및 영업',  // 공식 계정 기록 시작 — 브랜딩 축(GM 확정 2026-08-25). 9종에 브랜딩 칸이 없어 대외 노출 축인 '매출 및 영업'으로 둔다
    // 아래 1건만 제목에 "(GM 직접)" 태그가 없지만 id 지정으로 편입된다(2026-08-07 GM: "1번은 운영부
    // 매출이라 빼줘, 2번도 운영부" — 같은 top3 중 매출·회원서비스 2건은 제외, ERP/브로제이만 유지).
    '2026-08-08': 'IT·시스템·자동화', // 웰페리온 ERP/브로제이 도입·안정화
    // 아래 5건은 손표에 없어 카드가 "(분류 미정)" 으로 떠 있었다(2026-08-26 실측).
    '2026-08-41': '운영 정책',        // 외곽 인수인계 — 인계 기록 체계 전달
    '2026-08-42': '운영 정책',        // 하이디하우스 — 본연 업무 집중(업무 범위 정리)
    '2026-08-46': 'IT·시스템·자동화', // 매출·운영 보고 무인화 — 손입력 칸 제거
    '2026-08-47': '시설 및 환경',     // 헬스장 장비 도입 — 4사 비딩
    '2026-08-49': '시설 및 환경',     // 주차장 바닥보수공사 — 10/11 공사
    // 바디프렌드 유료 공유 안마의자 — 회사 비용 0, 매출 30% 배분 구조라 시설이 아니라 매출 축이다
    // (업무 SSOT 원장도 사전답사 건을 '[1] 매출 및 영업'으로 잡아 뒀다). GM 지시 2026-08-27.
    '2026-08-52': '매출 및 영업',
    // 상가(1F·B1) 관리 — 안내물·통합 소통방·관리비. 2026-08-27(아르모니움 소통 체계, 완료)에서 분리한
    // 후속 실행 건이라 같은 분류(운영 정책)를 쓴다. GM 지시 2026-08-28.
    '2026-08-53': '운영 정책'
  };
  function catFor(id) { return CATEGORY_BY_ID[id] || '분류 미정'; }
  // 판정 함수 단일 출처(약속 L01) — 월간운영계획.html의 "이달 핵심 과제 상세" 목록도 이 함수로 GM 직접 건을
  // 제외한다(2026-08-05 GM 지시). 두 화면이 각자 title.indexOf(...)를 다시 짜면 판정이 두 벌로 갈라진다.
  function isGmDirect(title) { return String(title || '').indexOf(GM_TAG) !== -1; }
  // top3 중 ERP/브로제이(id=2026-08-08) 1건만 "(GM 직접)" 제목 태그 없이도 GM 업무 화면에 실린다 —
  // 매출·회원서비스 2건은 운영부 소관이라 제외(2026-08-07 GM: "1번은 운영부 매출이라 빼줘, 2번도 운영부").
  // id 는 기존 필드 재사용(새 필드 추가 없음).
  var GM_TOP3_ERP_ID = '2026-08-08';
  function isGmTop3(o) { return o && o.id === GM_TOP3_ERP_ID; }
  // 'YYYY-MM' → '26.8.1~26.8.31' (해당월 전체 — 목표 자체에 시작·종료일이 없어 추적 대상 월 범위를 그대로 표기, 날짜 지어내지 않음).
  function monthRange(monthKey) {
    var m = String(monthKey || '').match(/^(\d{4})-(\d{2})$/);
    if (!m) return '일정 미정';
    var y = +m[1], mo = +m[2];
    var last = new Date(y, mo, 0).getDate();
    var yy = String(y).slice(2);
    return yy + '.' + mo + '.1~' + yy + '.' + mo + '.' + last;
  }
  // plan = status/monthly_ops_plan.json 파싱 결과, monthKey = 'YYYY-MM'. 반환 = []( 정상 0건 ) · [{...}](정상 n건).
  // plan 자체가 없으면 [] (호출부에서 조회 실패와 정상 0건을 구분해 처리).
  // 한 달치만 뽑는다. 아래 extract() 가 이 함수를 이번 달 + 이전 달들에 각각 돌려 합친다.
  function extractMonth(plan, monthKey) {
    var month = plan && plan.months && plan.months[monthKey];
    var objs = (month && month.objectives) || [];
    var schedule = monthRange(monthKey);
    return objs
      .filter(function (o) { return isGmDirect(o.title) || isGmTop3(o); })
      .map(function (o) {
        return {
          id: o.id,
          // 화면 제목에서 "(GM 직접)" 표식은 뗀다 — 이 목록 자체가 GM 직접 건이라 매 줄에 붙으면 군더더기고,
          // 카테고리도 소괄호로 붙어(2026-08-05 GM 지시) 괄호가 겹쳐 읽힌다. 표식은 정본(monthly_ops_plan.json)
          // 제목에 그대로 남아 판정(isGmDirect)은 계속 그 값으로 한다.
          title: String(o.title || '').replace(GM_TAG, '').replace(/\s{2,}/g, ' ').trim(),
          // direct=true → "(GM 직접)" 태그 건 / false → isGmTop3(ERP/브로제이)로만 편입된 전사 핵심과제(화면에서 배지로 구분).
          direct: isGmDirect(o.title),
          category: catFor(o.id),
          // 부서 — 목표 자체의 dept 를 그대로 넘긴다(GM 지시 2026-08-27 "중간에 부서도 붙여줘").
          // 여기서 추론하거나 표를 새로 만들지 않는다. 비어 있으면 화면이 '미정' 으로 그린다 —
          // 그 '미정' 목록이 곧 부서가 안 잡힌 건이고, TF팀 편성 후보다.
          dept: String(o.dept || '').trim(),
          schedule: schedule,
          target: o.target || '',
          status: o.status || '',
          progress: o.progress,
          // 본문(progress_note)·별첨 링크(doc)도 함께 넘긴다 — 2026-08-06 GM 지적("만들긴 했는데
          // 아무 내용이 없다"): 그동안 화면이 한 줄 target 만 그려, 정리해 둔 내용이 어디에도 안 보였다.
          // 좁은 3열 카드(월간운영계획 토글)는 그대로 두고 자세히보기 페이지에서만 펼친다.
          note: o.progress_note || '',
          doc: o.doc || '',
          // 마감일 원본 그대로 전달(가공 없음) — home 카드(GM 업무 기한 임박·기한 없음 집계)가 쓴다(2026-08-24).
          due: o.due || '',
          // 자료 링크 여러 개 — 2026-08-19 GM 지시로 회장님 보고건에 달려 있던 A3·검토서를 목표 쪽으로
          // 옮겼다("결국 내가 업무 진행하는 건들이니까 … GM업무쪽으로 통합"). 자리는 여기 하나뿐이다.
          docs: Array.isArray(o.docs) ? o.docs : []
        };
      });
  }

  // 이번 달 + 아직 안 끝난 지난 달 건을 함께 낸다 (GM 지시 2026-09-01 "다 그대로 나오게 해줘야해").
  // 왜 — 달이 바뀌면 화면이 그 달 목표만 비춰서, 8월에 진행 중이던 GM 직접 건 20개가 9/1 0시에 한꺼번에
  // 안 보이게 됐다. 데이터는 그대로 있는데 GM 눈에는 사라진 것으로 보인다.
  // 어떻게 — 목표를 9월로 복사하지 않는다(약속 L01 · 진실은 한 곳). 읽는 쪽에서 지난 달의 미완 건을
  // 이어 붙일 뿐이다. 끝난 건(status='완료')은 제외하고, 같은 목표가 두 달에 다 있으면 이번 달 것만 남긴다.
  var DONE = '완료';
  function extract(plan, monthKey) {
    var cur = extractMonth(plan, monthKey);
    var seen = {};
    cur.forEach(function (o) { seen[o.id] = 1; });
    var months = Object.keys((plan && plan.months) || {})
      .filter(function (k) { return /^\d{4}-\d{2}$/.test(k) && k < String(monthKey); })
      .sort();                                   // 오래된 달부터 — 화면에서도 그 순서로 이어 붙는다
    var carried = [];
    months.forEach(function (k) {
      extractMonth(plan, k).forEach(function (o) {
        if (seen[o.id]) return;                  // 이번 달에 같은 목표가 있으면 그쪽이 정본
        if (String(o.status || '').trim() === DONE) return;
        seen[o.id] = 1;
        o.carried = true;                        // 화면이 '지난 달에서 이어짐' 배지를 달 때 쓴다
        o.fromMonth = k;
        carried.push(o);
      });
    });
    return cur.concat(carried);
  }
  window.WellperionGmDirect = { extract: extract, extractMonth: extractMonth, isGmDirect: isGmDirect, isGmTop3: isGmTop3, GM_TAG: GM_TAG };
})();
