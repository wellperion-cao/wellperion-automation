/**
 * ⚠️ 폐기됨 (2026-06-23) — 이 파일은 더 이상 사용/배포되지 않습니다.
 *
 * 퍼널/문의 Survey GAS의 라이브 정본 = .deploy-funnel/Survey.js
 *   (clasp scriptId 1A77oDR… · 배포: cd .deploy-funnel && clasp push -f && clasp deploy -i <deploymentId>)
 *
 * 함수 수정·추가는 반드시 .deploy-funnel/Survey.js 에서 한다.
 * 과거 구버전 코드는 git 이력에 보존됨(이 파일 직전 커밋).
 * (이중 소스 드리프트로 #3가 죽은 사본에 잘못 들어간 사고 → 단일화)
 */
