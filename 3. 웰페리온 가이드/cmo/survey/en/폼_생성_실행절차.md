# 웰페리온 영문 구글폼 4종 — 생성 실행 절차

> 작성일: 2026-06-04 | 담당: AI CTO → GM 실행 1회 필요

---

## 1. 상황 요약

| 항목 | 내용 |
|---|---|
| GAS 파일 | `3. 웰페리온 가이드/cmo/survey/create_english_forms.gs` |
| clasp push | 불필요 (폼 생성은 함수 실행으로 완결 — 웹앱 배포와 무관) |
| clasp run | 현 환경 GCP OAuth 미구성으로 원격 실행 불가 |
| 필요 조치 | **GM이 GAS 에디터에서 `createEnglishForms()` 1회 클릭 실행** |

---

## 2. GM 실행 절차 (5단계)

### Step 1 — GAS 에디터 접속
1. 브라우저에서 [https://script.google.com](https://script.google.com) 접속 (cao@wellperion.com 계정)
2. 좌측 **"내 프로젝트"** → **"새 프로젝트"** 클릭

### Step 2 — 코드 붙여넣기
1. 기본으로 열리는 `코드.gs` 탭의 내용을 **전체 삭제**
2. 아래 파일 내용을 전체 복사해서 붙여넣기:
   ```
   파일 경로: 3. 웰페리온 가이드/cmo/survey/create_english_forms.gs
   ```
3. 프로젝트 이름 입력 (예: `Wellperion_EN_Forms`) → **저장** (Ctrl+S)

### Step 3 — 함수 실행
1. 에디터 상단 함수 선택 드롭다운에서 **`createEnglishForms`** 선택
2. **실행(▶)** 버튼 클릭
3. 최초 실행 시 **권한 승인 팝업** 발생:
   - "권한 검토" → "계속" → cao@wellperion.com 계정 선택
   - "Google에서 확인하지 않은 앱" 경고 → 좌하단 **"고급"** → **"(프로젝트명)(으)로 이동"** → "허용"
4. 실행 완료 후 하단 **"실행 로그"** 확인

### Step 4 — 폼 URL 4종 수집
실행 로그에서 아래 형식으로 URL이 출력됩니다:

```
=== 생성 결과 요약 ===
[성공] 멤버십 문의 (EN)
  공개 URL : https://docs.google.com/forms/d/e/XXXXX/viewform
  편집 URL : https://docs.google.com/forms/d/YYYYY/edit
[성공] 성인 강습 문의 (EN)
  공개 URL : https://docs.google.com/forms/d/e/XXXXX/viewform
  편집 URL : https://docs.google.com/forms/d/YYYYY/edit
[성공] 유소년 강습 문의 (EN)
  공개 URL : ...
[성공] 여름 특강 문의 (EN)
  공개 URL : ...
=== 완료 ===
```

**공개 URL(getPublishedUrl) 4개를 복사해 둡니다.**

### Step 5 — wp_inquiry_block_en.html 에 URL 삽입
`3. 웰페리온 가이드/cmo/inquiry/wp_inquiry_block_en.html` 파일을 열고 아래 placeholder를 실제 URL로 교체합니다:

| Placeholder | 교체할 URL |
|---|---|
| `__EN_FORM_URL_MEMBERSHIP__` | 멤버십 문의 공개 URL |
| `__EN_FORM_URL_ADULT__` | 성인 강습 문의 공개 URL |
| `__EN_FORM_URL_YOUTH__` | 유소년 강습 문의 공개 URL |
| `__EN_FORM_URL_SUMMER__` | 여름 특강 문의 공개 URL |

교체 후 파일 저장 → git commit → Netlify 자동 배포 확인.

---

## 3. 응답 시트 연결 확인

폼 생성 후 각 폼의 **응답 탭**에서 연결된 시트를 확인합니다:

| 폼 | 연결 시트 ID |
|---|---|
| 멤버십 문의 | `12AWcAlgmmYKr2nUbWmVpa71_z3zi0BaU4ZdnOwrI_7U` |
| 성인 강습 | `1b0XU1oTHlXzBhEzUOar5GEm44vjopdO25qfsh-awDXw` |
| 유소년 강습 | `1b0XU1oTHlXzBhEzUOar5GEm44vjopdO25qfsh-awDXw` |
| 여름 특강 | `1b0XU1oTHlXzBhEzUOar5GEm44vjopdO25qfsh-awDXw` |

시트 연결이 안 된 경우: 폼 편집 URL 접속 → 상단 **"응답"** 탭 → 스프레드시트 아이콘 → 기존 스프레드시트 선택 → 위 ID 입력.

---

## 4. Language 필드 안내

각 폼에 **"Language"** 텍스트 항목이 추가되어 있습니다.  
응답자가 직접 입력하거나, 폼 공유 시 prefill URL을 활용해 `EN` 값을 기본으로 넣을 수 있습니다.

prefill URL 생성 방법:
1. 폼 공개 URL에서 `/viewform` 뒤에 `?usp=pp_url&entry.FIELD_ID=EN` 추가
2. FIELD_ID는 폼 편집 화면에서 해당 항목 우클릭 → "사전 입력 링크 가져오기"로 확인

---

## 5. 폼 URL 기록란 (실행 후 여기에 기입)

| 폼 | 공개 URL | 생성일 |
|---|---|---|
| 멤버십 문의 (EN) | _(실행 후 기입)_ | |
| 성인 강습 (EN) | _(실행 후 기입)_ | |
| 유소년 강습 (EN) | _(실행 후 기입)_ | |
| 여름 특강 (EN) | _(실행 후 기입)_ | |

---

*예약제 운영 — 투어·상담 모두 사전 예약 필수. 워크인 불가.*

---

## 6. [긴급·치명] 기존 영문 폼 4종 — 익명 로그인벽 제거 (중복 생성 금지)

> 발견(2026-06-04 QA 실측): 라이브 영문 폼 4종 모두 **HTTP 401 / Google 로그인 강제**.
> 외국인 방문자가 영문 문의를 제출 불가 → 영어 퍼널 첫 관문 차단(치명).
> **원인**: 생성 스크립트가 `setRequireLogin(false)`를 호출하지 않아, Workspace 계정 생성 폼이
> 기본값(조직 도메인 로그인 필요)으로 만들어짐. (`setCollectEmail(false)`만으로는 로그인벽 안 풀림.)

### 조치 (폼을 새로 만들지 않고 기존 폼 설정만 변경)
1. `create_english_forms.gs` 전체를 (이미 수정 반영됨) GAS 에디터에 최신본으로 붙여넣기/저장
   - 4개 생성 함수에 `form.setRequireLogin(false);` 추가됨(향후 생성도 익명 허용)
   - **신규 함수 `updateEnglishFormsAccess()` 추가** — 기존 4개 폼을 제목으로 찾아 설정만 변경
2. 함수 드롭다운에서 **`updateEnglishFormsAccess`** 선택 → **실행(▶)** (폼 소유자 = cao@wellperion.com)
3. 실행 로그에서 각 폼 `requiresLogin: true → false` 확인
4. **재검증(필수)**: 로그아웃/시크릿 브라우저로 아래 4개 viewform 접속 → 로그인벽 없이 질문 노출 확인
   - 멤버십: `https://docs.google.com/forms/d/e/1FAIpQLSfOFxaqO5zSmoYa9TkfBlkDPbGjaTXm3duh7w5NwuBMhlJb6A/viewform`
   - 성인:   `https://docs.google.com/forms/d/e/1FAIpQLSdirIB05ecVGkqJCIfJaw5yn7nIwVQe8ZeKbzQzKqKg83AUNw/viewform`
   - 유소년: `https://docs.google.com/forms/d/e/1FAIpQLScqZum8zrhqKC0m2S0IKElYez-aJuPMQHM-JKF867UADF9X3A/viewform`
   - 여름:   `https://docs.google.com/forms/d/e/1FAIpQLSchg7_9bmLTjCA-gfSj_anjklaNr_OpYBcmHzQJCTndIzgJPA/viewform`

> `updateEnglishFormsAccess()`는 `FormApp.create`를 호출하지 않음 — Drive에서 정확한 제목으로
> 기존 폼을 찾아 `setRequireLogin(false)` + 설명문(CTA litt.ly→ko/inquiry) 갱신만 수행. **중복 폼 0.**
> ※ `clasp run` 불가(GCP OAuth·.clasp.json 미구성) → GM 에디터 1회 실행 필수.
