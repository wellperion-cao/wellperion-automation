# 웰페리온 ERP · AWS 장기 구조 결정 (두 번 일하지 않기)

작성: AI 시토 | 2026-09-03 | GM 지시 "장기적으로 봤을 때도 두 번 일하지 않게 시토가 잘 판단해서 진행"
정본 방향: 배101(2026-07-16 GM 확정 · 멀티테넌트 SaaS ERP · 웰페리온 = 1호 테넌트 · 직원 전원 로그인 · 핵심 3모듈부터)

| # | 갈림길 | 결정 | 왜 지금 정하나(나중에 바꾸면 두 번 일) |
|---|---|---|---|
| 1 | DB 엔진 | **PostgreSQL** — 지금 EC2 안에 설치(추가 비용 0), 나중에 RDS 로는 데이터만 옮김 | SQLite 로 화면·API 가 더 쌓이면 문법·동시쓰기 차이를 전부 다시 손봐야 한다 |
| 2 | DB 접속 | 코드 전체가 **`server/common/db.py` 하나**로만 DB 를 연다(접속 주소는 환경변수 `ERP_DB_URL`) | RDS 전환·비밀번호 교체가 "환경변수 한 줄"로 끝난다 |
| 3 | 테넌트 | 모든 업무 표에 **`tenant_id` 칸을 지금부터**(기본값 `wellperion`), 조회는 항상 tenant 로 거른다 | 배101 의 SaaS 전환 때 표 구조·인덱스·모든 쿼리를 다시 뜯는 일이 사라진다 |
| 4 | 계정 DB | 로그인 관문(auth)도 같은 PostgreSQL 로 — users 표에 tenant_id | 테넌트별 관리자·권한이 같은 자리에서 나온다 |
| 5 | 쓰기 경로 | 2단계(쓰기 이전)부터 **서버 API 가 유일한 쓰기 창구**, 시트는 서버가 되밀어 주는 사본 | 화면이 GAS 와 API 두 곳에 쓰는 기간을 만들지 않는다(중복 쓰기 사고 방지) |
| 6 | 열쇠 | 회원 = 회원번호(M번호) · 문의 = `유형|rowKey` · 모두 시트 행번호 아님 | 행 삭제 레이스(INC 실사고) 재발 차단 · DB 이전 뒤에도 같은 열쇠 |
| 7 | 홈페이지 | 워드프레스를 AWS 로 **옮기지 않는다** — 시모 새 홈(정적) 을 wellperion.com 에 올리고 워드프레스는 자료 회수용 | 옮겨 놓고 다시 새 홈으로 갈아엎는 두 번 일을 피한다 |
| 8 | 프론트 | 기존 정적 HTML 유지 · 새 틀(프레임워크) 도입 안 함 · 화면은 "ERP 도메인이면 API, 아니면 GAS" 한 가지 규칙 | 93개 화면을 다시 짜는 일을 만들지 않는다 |
| 9 | 백업 | 매일 03:00 `pg_dump` → S3(버킷 1개 · 30일 보관) | 파일 DB 복사 방식은 RDS 가면 못 쓴다 — 처음부터 덤프 방식 |
| 10 | 인프라 | `scripts/aws_bootstrap.py` 처럼 **서버 설정은 전부 저장소 코드**(nginx·서비스·크론) | 서버를 다시 세울 때 손으로 되짚지 않는다 |

## 적용 순서
1. 레인 G(회원 미러 마감) 끝나는 즉시 → PostgreSQL 전환 레인(1·2·3·4·9 한 번에)
2. 2단계 쓰기 이전은 5 규칙으로 문의부터
3. RDS 이전은 트래픽·동시쓰기가 실제로 늘 때(💰 결재) — 코드 변경 0

## 바꾸지 않는 것
- 구글 시트 = 실무진이 보는 사본으로 계속 유지(보고·인쇄) · GAS 는 쓰기 이전 전까지 그대로
- 카톡 발신은 GM PC 데스크톱 앱 의존 → 서버로 못 옮김(별도 과제)
