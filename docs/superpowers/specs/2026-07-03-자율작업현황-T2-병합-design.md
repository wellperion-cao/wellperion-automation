# 자율 작업 현황 ⟵ T2 병합 · 자율 체감 강화 — 설계

- **작성:** 시토(AI CTO) 2026-07-03 · **승인:** GM go (2026-07-03)
- **연결 배:** 배237 GM 보좌 자율화(체감·표현 축) · 구 T2 업무자동화SSOT
- **정본 페이지:** `3. 웰페리온 가이드/자율현황.html`

## 1. 목표
T2(업무자동화SSOT)와 자율현황.html이 "시토 자동화의 두 축"으로 분리돼 진입점이 2개다. GM: **자율작업현황이 더 직관적** → 한 보드로 병합(2층 구분) + **상시 자율 체계가 실제로 뭘 하는지 눈에 보이게**(지금은 안 와닿음).

근본 원인(실측): 자율실행 phase2는 ON이나 실제 적용 **0건**(`auto_applied:0`), 헬스체크·차등점검은 실행 로그 없어 보드에 "미측정". 체계는 살아있으나 **눈에 보이는 자율 결과물이 없어** 체감이 안 됨.

## 2. 현행 (병합 대상)
| | T2 | 자율현황.html |
|---|---|---|
| 위치 | `wellperion_guide(main).html` `article#T2` (L6316–6560) | `3. 웰페리온 가이드/자율현황.html` (standalone) |
| 성격 | "지금 **도는** 자동화"(정각 스케줄) | "무엇을 **자율로** 했나"(AI 판단) |
| 내용 | 시스템 현황·자동화 건강 점수판·10슬롯 타임라인·M5 파이프라인 | 오늘의 항로 top3·상시 자율 체계 카드·성과 요약·자율 처리 이력 |
| 렌더 | 손 작성 표 + 2개 라이브 위젯(erp_status.json) | 전부 status/*.json(l) 로그에서 클라 렌더 |

## 3. 설계 — 단일 보드 · 2층

정본 = `자율현황.html`(자율작업현황). 진입점 1개, 안에서 2층.

### 🔺 히어로 (최상단) — "자율이 오늘 한 일"
결과물 중심 1줄 요약: `오늘 자율 처리 N건 · 포착 N건 · 학습 반영 N건`. 측정 불가 값은 **"미측정"** 정직 표기(가짜 숫자 금지). 추상적 체계 설명이 아니라 **실제 결과물**을 맨 위에.

### 층 ① 자율로 한 일 (먼저·크게)
- 🧭 오늘의 항로 top3 (기이전 — 유지)
- 🕘 **최근 자율 처리 이력** — "AI가 이걸 했다"는 구체 사건 중심, **제안(대기) vs 실제 처리(완료)** 시각 구분. 체감의 핵심 → 상단 배치.
- 🔴 상시 자율 체계 — routine 카드를 '설명문'이 아니라 **마지막 실제 처리 시각 · 누적 처리 건수** 중심으로 재구성.
- 📊 성과 요약 — 미측정 정직 유지.

### 층 ② 돌아가는 자동화 (아래·접이식, 구 T2 흡수)
- 🖥️ 시스템 현황 + 🤖 자동화 건강 점수판 (`status/erp_status.json`)
- 🟢 지금 도는 자동화 타임라인(10슬롯) · 📸 M5 인스타 발행 파이프라인

### T2 탭 처리
- `wellperion_guide(main).html`의 `article#T2` 본문 = 층 ②로 이관(내용 보존). 나브 `data-id="T2"` 라벨은 유지하되 **자율작업현황 보드로 연결**(진입점 일원화). 구 T2 상세는 층 ②에 그대로 살아 있으므로 정보 손실 0.

## 4. 와닿게 — 가역 자율행위 실효화 (GM go 게이트)
| # | 행위 | 지금 | 실효화 방법 | 가역성 |
|---|---|---|---|---|
| 1 | 표류 배 'next' 자동보강 (phase2) | ON·0건 | `auto_applied:0` 원인 진단(임계·표류배 부재·버그) → 실제 발동 → 각 보강을 '자율 처리' 이력에 기록 | 가역(next 텍스트·git추적·되돌리기 쉬움) |
| 2 | 완료 배 자율 아카이브 | 6h 루틴 가동(숨김) | '자율 처리' 사건으로 **가시화**(현재 노이즈로 이력서 제외됨) | 가역 |

원칙: **라이브 발효 = GM go + 즉시 역롤백**(플래그 OFF). 새 자율 행위는 **가역 한정**(비가역=제안만, phase1 유지). 직접 실행 금지 원칙 유지(웰리 위임→담당 실행).

## 5. 데이터 소스 (기존 재사용 — 새 배관 최소)
`status/gm_aide_scan_log.jsonl`·`gm_observation_ledger.jsonl`·`northstar_log.jsonl`·`northstar_pending.json`·`learning_proposals.json`·`audit_registry.json`·`erp_status.json`·`_queue_archive.json`. 읽기=GAS read_file + raw-GitHub 폴백(no-store). 히어로 요약은 이 로그들 클라 집계(새 서버 배관 불필요).

## 6. 범위 밖 (혼동 방지)
- 자율 **범위 대확장**(배237 본방향, 신규 도메인 자율행위) = 별도 집중세션. 본 작업 = 보드 병합 + 가역 1~2개 실효화까지.
- 점검 페이지 화면(collectDashboardData) 서버정합 = 시우 ship14 별건.
- 항로 정리 소유(하이진) = 시우/웰리 경계 유지 — 본 작업의 아카이브 가시화는 '표현'이지 소유 이전 아님.

## 7. 성공 기준
1. 진입점 1개(자율작업현황)에서 자동화 현황 + 자율 결과물 **한눈에**.
2. 구 T2 정보 손실 0(층 ②에 보존).
3. 히어로·이력이 **실제 자율 결과물**을 결과물 중심으로 노출, 미측정은 정직 표기.
4. 가역 자율행위 최소 1개가 실제 발동해 **매일 자율 처리 이력이 쌓임**(체감 확보), 라이브 발효는 GM go.

## 8. 역롤백
- 페이지 병합 = git revert(단일 커밋 단위).
- 자율행위 실효화 = 플래그 OFF(GM_AIDE_AUTO_EXEC 등) 1초 원복.
