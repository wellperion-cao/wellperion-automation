# M1 정리안 — 과밀 해소(목차+4그룹 접이식+요금 이관)

- 날짜: 2026-07-09 · 발의: GM("M1에 너무 많은 양이 몰려있다·정리 필요")
- 소유: 시모(CMO) · GM 승인: 정리안대로 + 요금표 CPO 이관
- 상태: **승인·spec 확정. 실행 보류 = 메인 HTML 동시편집(미커밋 변경) 클리어 후.** (deep-interview ③ 프론트 모듈 캡슐 정리 본진)
- 원칙: 병합(M1=M1+M2+M3)은 유지, **읽기 쉽게 구조화만**. 되돌리지 않음. 기능·앵커·발행배선 무회귀.

## 진단 (실측 2026-07-09)
M1 article = 747줄 · **21개 h3 섹션 평면 나열** · 표 30 · 접이식 13 · 콜아웃 21 → 스크롤 과밀. 논리 4묶음 + 오배치 1건.

## 실행안
### 1. 상단 목차 캡슐 (M1 최상단, cmopub-section 위)
"이 모듈 = 콘텐츠 제작 ~ 발행" + 4개 묶음 바로가기(앵커 점프): ①제작 ②검수·발행 ③현황·전환 ④채널 규칙. M2 캡슐과 동일 톤(기존 변수 재사용·신규 색 0).

### 2. 4개 묶음 그룹핑 + 접이식(details)
21개 h3를 아래 4그룹으로 감싸고 각 그룹을 `<details>`(첫 그룹만 open) 처리:
- **① 제작 프로세스(9)**: 북극성 루프·6대 기둥·채널별 검수·종료 절차·폴더 명명·폴더 구조·output 분리·표기 규칙·발행 스크립트
- **② 검수·발행(구 M3)**: 검수·발행 현황(cmopub)·콘텐츠 검수 대기·공식 5채널 발행 규칙(+8 h4)
- **③ 현황·전환(구 M2 연결)**: 발행 후 현황·퍼널 한눈에·대시보드 열기·문의 진입점·채널 현황·IG 파이프라인 요약 (본문은 대시보드 링크로 축소 여지)
- **④ 기타**: 정기 보고·홈페이지 구성
- ⚠️ h3 id/앵커·`[data-id]`·스크립트 배선 보존(접이식 wrapper만 추가, 내부 DOM·id 무변경). 30개 표는 접힘 안으로.

### 3. 오배치 이관 — 📑 안내 가이드(요금·멤버십) → CPO
M1의 `📑 안내 가이드(예약·이용)` 섹션 8 h4(정회원 연회비·가족 멤버십·단체/법인·일일 입장료·개인 락커·등급 업그레이드·재등록 혜택·공통 정책) = **CMO 콘텐츠 모듈과 무관·CPO 멤버십 영역** → CPO 페이지(후보: `cpo/member/` 또는 `cpo/product/상품기획.html`)로 이관, M1에서 제거.
- ⚠️ 요금 = 공식값(canon) → **값 변경 0·위치 이동만**(비파괴·역가능). 목적지 CPO 페이지는 시포 소유라 이관 시 시포 배선·게이트 확인. GM 지시로 이동.

## 실행 가드 (동시편집 안전)
- 착수 전 `git status`로 메인 HTML **미커밋 변경 0 확인**(다른 세션 클리어). 아니면 대기.
- 최소 diff·재렌더 아님(HTML 구조만)·로컬 검증(id 중복0·orphan 앵커0·죽은 링크0·발행배선 보존·node --check).
- 커밋 직전 재확인·원샷 커밋·비파괴. 라이브 시크릿 크롬 검증(M1 렌더·접이식 동작·콘솔0).
- 요금 이관은 별 커밋(M1 정리 ↔ CPO 배치 짝).
