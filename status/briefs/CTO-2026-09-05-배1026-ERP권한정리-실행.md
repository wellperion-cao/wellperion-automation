# ERP 권한 정리 실행 (배1026 · 웰리 설계 그대로 실행)

설계 정본 = `status/briefs/CEO-2026-09-05-ERP-권한-정리-설계.md`. 순서 ④ → ①③ → ② 그대로 실행.

## ④ 앱 셸 정리 — OK (실무진 영향 0)

- `status/erp_module_desc.json` 에 `name`(id 키) 36개 추가 — 설계 §2 표의 짧은 이름 그대로.
- `scripts/erp_modules_build.py`: `APPGROUP_IDS`(회원 6·운영 18·점검 7·경영 10, 나머지 62는 문서함) + `clean_name()`
  (옛 용어 차단: 프라이빗→정원제, 레슨→강습, 피트니스→스포츠클럽, 현대하이페리온→웰페리온, 가이드허브→웰페리온 ERP)
  추가. 모든 카드에서 `roles`(전부 `["admin","staff"]` 동일 — 정보 0) 칸 삭제.
- `erp/index.html`: 카드 그룹핑을 `m.core?'핵심':group닉네임` → `m.appgroup`(회원/운영/점검/경영/문서함)로 교체.
- 재생성·검증: `python scripts/erp_modules_build.py --check` → 103건·없는 경로 0. `--selftest` 통과.
- 권한 판정(`group` 필드·account_perms.json 의 deny 목록)은 그대로 — `appgroup` 은 표시 전용 새 필드라 기존
  운영부/시설부/… 계정 권한에 영향 없음.

## ①③ 개인 계정 흐름 · 권한 관리 페이지 — OK

- **부서 템플릿 한 곳으로:** 기존 `PRESETS`(닉네임 그룹, 배951/978 시절)를 폐기하고 `DEPT_COMMON_MODULES`
  + `DEPT_ONLY_MODULES`(부서→정확한 모듈 id 목록, 설계 §2③ 매트릭스 그대로 옮김) + `dept_modules(dept)` 로 단일화.
  가입 승인(`/auth/google/finish`)과 관리자 화면 "부서 기본" 버튼이 전부 이 표 하나를 쓴다(벌 두 개 금지 원칙 준수).
- **권한 관리 페이지:** 새 페이지를 만들지 않고 기존 `/auth/admin` 에 "권한 매트릭스" 구획을 얹었다(관리자 전용,
  기존 잠금 그대로).
  - 행 = 활성 직원 계정(부서 배지 포함), 열 = 회원·운영·점검·경영·문서함 5그룹, 칸 = 그 그룹에서 지금 열려
    있는 화면 수(허용/전체).
  - "부서 기본 적용" 버튼 6개 — 그 부서 사람 전원에 `dept_modules()` 를 일괄 적용(잠금 걸린 사람은 건너뜀).
  - 사람별 "잠금" 토글 — 걸어두면 부서 일괄 적용에서 제외(개인 예외 보존).
  - "세부조정" 링크 — 기존 `/auth/admin/{uid}/perms`(그룹·모듈별 허용/거부 체크박스, 이미 있던 화면)로 이동.
  - 이름·계정·부서 검색 입력(클라이언트 JS 필터).
  - 저장 = DB `users.perms` 즉시 반영. 변경마다 `perms_history` 표(신설 · schema.sql)에 이전/이후 값을 남기고,
    "변경 이력(최근 20건)" 표 + "되돌리기" 버튼 제공. "마지막 저장 시각·누가"도 이 이력에서 표시.
  - 인쇄 = `@media print{@page{size:A3 landscape}}` — 버튼·검색창 숨기고 표만.
- **부족한 부분(정직하게 적음):**
  - 설계 §4-2 는 칸 하나하나(사람×모듈)를 3단계(○/△/—)로 클릭 순환하는 것을 그렸다. 지금은 **그룹 단위 요약
    (허용 수/전체)** 로 구현했다 — 사람 수 × 5그룹 × 모듈수를 전부 인라인 폼으로 깔면 페이지가 못 쓸 만큼
    무거워진다(ponytail 판단). 모듈 하나하나의 허용/거부는 "세부조정" 링크(기존 화면, 이미 완전히 동작)에서 한다.
  - **△(보기만) 3번째 상태는 못 만든다.** ERP 화면 자체에 읽기전용 렌더 모드가 아예 없다(허용/차단 2단계 관문
    구조) — 화면마다 읽기전용 모드를 새로 만들어야 하는 별도 과제라 BLOCKED.
  - "그 사람만 콕 집어" 방식은 매트릭스 화면이 아니라 "세부조정" 페이지의 모듈 체크박스로 한다(이미 존재).

## ② 개인 예외 3건 — OK (코드 관문으로, 이메일 하드코딩 없이)

부서·그룹 매칭으로는 **절대 안 열리고** `modules` 로 콕 집어야만 켜지는 목록(`EXCEPTION_ONLY_IDS`, `app.py`)을 만들었다:
`월간운영계획`(이경연 실장 개인) · `GM 업무`(김남욱 GM 개인) · `매출 현황/지출 현황/매출·지출 현황`·`인사 허브`·
`채용`(나우열M 개인) · `매출·회원 보고`·`자율현황`·`카톡 전송 관리`(경영진 전용, 아직 실무진 누구에게도 아님).

이메일을 코드에 못 박지 않은 이유 — 이경연 실장·나우열M 은 아직 개인 구글 계정으로 가입하지 않았다(배982 방금
켜짐). 실제 계정이 생기면 GM 이 매트릭스의 "세부조정" 링크에서 그 사람에게만 해당 모듈을 체크하면 된다 —
부서(운영부/경영지원) 템플릿을 아무리 다시 적용해도 이 목록은 절대 새지 않는다(자가점검 통과).

## 실측

- `scripts/erp_modules_build.py --check` → 103건·없는 경로 0. `--selftest` → OK.
- 서버(`ec2-user@15.164.151.105`) DB 스키마: `perms_history` 표 신설 확인(`\d perms_history`).
- `deploy_auth.sh` 배포 → `erp-auth.service` active, `curl /auth/check`(무쿠키) = 401(정상).
- 서버에서 `python3 app.py`(자가점검, 실제 DB 붙여서) → `self-check ok`.
- 관리자 세션(app.issue·app._admin_issue 로 발급, 실 비밀번호는 안 읽음) 으로 `/auth/admin` GET → 200,
  "권한 매트릭스" 구획 렌더 확인. (재배포 직후라 서버 `modules.json` 이 구버전이라 그룹 칸이 0/0 이었음 — 커밋·
  푸시 뒤 1분 cron pull 로 갱신되는 것까지 확인 예정, 아래 최종 실측에서 갱신본으로 재확인)
- git 커밋 `9afe9551e` → origin/master 반영 확인.

## 켜는 날 스위치 위치

- 개인 구글 로그인 자체(배982)는 **이미 라이브** — `GOOGLE_CLIENT_ID/SECRET` 이 서버 `/srv/erp/auth.env` 에
  있으면 켜져 있다. 끄려면 그 두 값을 비운다.
- 이번 배1026 은 스위치랄 게 따로 없다 — 앱 셸 그룹 정리·부서 템플릿 통합·권한 매트릭스·개인 예외 관문이
  전부 코드에 상시 적용이다(별도 on/off 플래그 없음). "켜는 날"이 필요한 유일한 지점은 **실무진에게 로그인
  방식이 바뀐 것을 알리는 안내 1통** — 이건 웰리 몫(설계 §4-1). 안내에 넣을 변경점:
  1. 이제 개인 구글 계정으로도 로그인 가능(부서 선택 → GM 승인 대기).
  2. ERP 홈 화면 카드가 회원/운영/점검/경영 4그룹 + 문서함으로 다시 묶였다(실무진이 매일 쓰는 화면 위치는
     안 바뀜 — 그룹 이름표만 정리).
  3. 카드 이름이 파일 제목 대신 짧은 이름으로 바뀐 곳이 있다(예: "강습 회원관리" → "강습 회원").

DONE: 9afe9551e · ④=OK · ①③=OK(매트릭스는 그룹 단위 요약 + 세부조정 링크, △ 3단계는 BLOCKED) · ②=OK · 스위치=없음(실무진 안내 1통만 웰리 몫)
