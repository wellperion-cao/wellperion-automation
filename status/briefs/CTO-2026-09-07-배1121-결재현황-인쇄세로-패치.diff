diff --git a/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html b/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html
index fcfd3430b..b38b8f285 100644
--- a/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html	
+++ b/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html	
@@ -998,12 +998,23 @@ function buildApprovalPrint(list) {
 }
 // 카드 1건만 인쇄 (2026-06-03 GM) — 같은 카드 인쇄 포맷(서명칸 포함)으로 해당 건만.
 // (전체 카드 일괄 인쇄 printApprovalCards는 삭제 — 트렐로 툴바 인쇄 버튼 제거, GM 확정 2026-07-18)
+/* 결재 보고서(카드·대시보드) 인쇄는 항상 세로 A4 로 고정한다 — 업무 현황 SSOT 의 printDocViewer 와 같은 원리.
+   print_options.js(캘린더 인쇄 용지/방향 선택기)가 localStorage 에 저장한 마지막 선택(A3·가로)이 전역 @page
+   스타일(#po-page-style)로 이 화면 인쇄까지 새어 들어와, 세로로 고쳐도 다음 인쇄에서 가로로 되돌아가던 원인
+   (2026-09-02 나우열M 진단 · 09-07 #185 "수정하면 계속 원복"). 인쇄 시작 때 더 나중(=더 우선)인 @page 를 주입하고
+   끝나면 되돌린다. 캘린더 인쇄(printApprovalCalendar)는 선택기 값을 그대로 쓰는 게 맞아 손대지 않는다. */
+function _printPortraitOverride(on) {
+  var ov = document.getElementById('approvalPrintPageOverride');
+  if (!ov) { ov = document.createElement('style'); ov.id = 'approvalPrintPageOverride'; document.head.appendChild(ov); }
+  ov.textContent = on ? '@page{size:A4 portrait;margin:10mm}' : '';
+}
 function printApprovalCard(id) {
   var t = (ALL_TASKS || []).filter(function(x){ return String(x.id) === String(id); })[0];
   if (!t) { alert('해당 건을 찾을 수 없습니다.'); return; }
   document.getElementById('approvalPrintArea').innerHTML = buildApprovalPrint([t]);
   document.body.setAttribute('data-print-mode', 'cards');
-  function _reset(){ document.body.removeAttribute('data-print-mode'); window.removeEventListener('afterprint', _reset); }
+  _printPortraitOverride(true);
+  function _reset(){ document.body.removeAttribute('data-print-mode'); _printPortraitOverride(false); window.removeEventListener('afterprint', _reset); }
   window.addEventListener('afterprint', _reset);
   window.print();
   setTimeout(_reset, 2000);
@@ -1879,8 +1890,11 @@ function renderApprovalDashboard() {
 
 function printApprovalDashboard() {
   document.body.dataset.printMode = 'approval-dashboard';
+  _printPortraitOverride(true);
+  function _reset(){ delete document.body.dataset.printMode; _printPortraitOverride(false); window.removeEventListener('afterprint', _reset); }
+  window.addEventListener('afterprint', _reset);
   window.print();
-  delete document.body.dataset.printMode;
+  setTimeout(_reset, 2000);
 }
 
 /* ═══ [결재-캘린더 인쇄] 캘린더 뷰 한 장 맞춤(scale-to-fit) — GM 요청 2026-07-18 ═══
