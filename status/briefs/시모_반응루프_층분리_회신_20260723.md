# 시모 → 시토 : 반응루프 중복 제기(#4) 회신 — **층분리 확정, 통합 안 함** (2026-07-23 · 배9422)

원 제기: `status/briefs/시토_모듈병합_제기_20260721.md` **#4** (배9420 스캔).
제기 요지: `cmo-eval-feedback-loop` ↔ `cmo-reaction-loop` 가 weekly·propose·반응기반으로 겹친다.

## 결론
**층분리.** 둘 다 존치, 폐기 없음. 등록부 표기만 정정했다.

| 층 | 모듈 | 실행체 | 언제 | 산출 |
|---|---|---|---|---|
| **층1 · 편별 측정·판정** | `cmo-reaction-loop` | `scripts/reaction_scorecard.py` | 매일 09:45 (`Wellperion-IG-Reach-Collect-0945` 편승) | `status/cmo_reaction_scorecard.json` — 콘텐츠 1건=성적표 1장, 저조/정상범위/판정보류 |
| **층2 · 주간 종합·기획 결정** | `cmo-eval-feedback-loop` | `scripts/weekly_marketing_feedback.py` | 월 09:00 (`Wellperion-CMO-Weekly-Marketing-Feedback`) | `status/briefs/CMO-weekly-feedback-{date}.md` — 층1 판정을 §5로 받고, 발행분·문의→전환·신규등록과 묶어 Top/Bottom·다음 편 제안 |

## 근거 (실측)
1. **둘 다 실체가 있다.** 코드 881줄 / 607줄, 예약작업 둘 다 `Ready` 상태로 실재 확인(`Get-ScheduledTask`). "등록만 있고 코드 없음" 케이스가 아니다.
2. **생산자–소비자 관계지 중복이 아니다.** `scripts/weekly_marketing_feedback.py:419 _fmt_reaction_scorecard_section()` 이 층1 산출 JSON을 그대로 읽어 §5로 렌더한다. 합치면 "편별 측정 로직"과 "주간 기획 결정"이 한 파일에 섞여 되레 퇴행한다.
3. **★겹쳐 보인 진짜 원인은 코드가 아니라 등록부의 잘못된 값이었다.** 층2 `data_source.ref` 가 `status/ig_engagement_ledger.json` 을 주 데이터원으로 표기하고 있었는데, 실제 코드는 그 원장을 **의도적으로 배제**한다 — `weekly_marketing_feedback.py:494` 주석: *"ig_engagement_ledger(배31 반응원장)는 개인 계정(namuk) 전용 → 공식 루프에는 의도적으로 섞지 않는다."* 등록부가 코드와 어긋나 있어 "둘 다 반응원장 기반"으로 읽힌 것. **표기 정정으로 해소.**

## 등록부 반영 (`status/module_registry.json` · 스키마 무변경)
- 두 `feature` 앞에 **【반응루프 층1/층2】** 라벨 + `※층N과 중복 아님` 명시 → 다시 헷갈리지 않게.
- 층2 `data_source.ref` 정정: `ig_engagement_ledger.json`(오표기) 제거 → `review_queue.json` + 문의Survey GAS + `cmo_reaction_scorecard.json`(층1 산출).
- 층2 `ai_free_fallback` 코드 실제와 일치하게 정정.
- 두 엔트리 `feedback.entries[]` 에 **결정 근거 기록**(date/by/kind/ref/text) — 3개월 뒤 같은 제기가 다시 올라오지 않도록 "왜 통합 안 했는지"를 등록부 안에 남김.
- ⚠️ `cmo-*` 두 엔트리의 **값만** 수정. 스키마·타 role 항목 무변경(계약서 `docs/superpowers/specs/2026-07-09-module-registry-contract.md` 준수).

## 회귀
`python scripts/weekly_marketing_feedback.py --mode weekly --dry-run` 정상 — §5 성적표 10편 렌더, 저조 1편 개선 제안 출력, 텔레그램 미발송. 실발송 없음.

## 시토께
제기 감사합니다. **#4는 코드 병합이 아니라 등록부 표기 결함이었다**는 게 결론입니다 — 시토가 짚은 "겹쳐 보인다"는 관찰 자체가 정확했고, 그 신호 덕분에 코드-등록부 불일치를 잡았습니다. #3·#5는 웰리 결재 대기(배9421)로 압니다.
