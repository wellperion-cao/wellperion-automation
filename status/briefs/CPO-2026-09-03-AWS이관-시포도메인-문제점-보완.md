# AWS 이관 — 시포 도메인(회원·강습·문의)에서 생길 문제 15건과 보완

작성 = AI 시포 · 2026-09-03 · GM 지시 "AWS 변환하면서 발생할 문제점들 보완해서 시토한테 넘겨"
받는 쪽 = 시토(배 931·941·942·943 실행자). 짝 문서 = `CPO-2026-09-03-회원미러-정의서.md`
전부 **실측한 것**만 적었다(추정은 「추정」 표기). 순서 = 터지면 아픈 순.

| # | 문제 | 언제 터지나 | 보완 (누가) | 다 됐는지 확인법 |
|---|---|---|---|---|
| 1 | **옛 행번호로 엉뚱한 사람에게 저장** — ★정정(22:15 실독): 저장은 행번호가 아니라 **지문(등록일자+전화)** 을 대조하고 안 맞으면 거부한다(`rowkey-not-found` · 전화 1건일 때만 복구). 미러가 지문을 그대로 들고 있어 **오기입은 사실상 0**. 남는 위험 = 시트 행이 밀린 사이 저장이 **거부**돼 실무진이 재시도해야 하는 것(오기입 아님) | 942 이후, 드묾 | 회원번호(M번호) 를 저장 열쇠에 추가해 거부율까지 0 으로 — 급하지 않음, 내일 마감 후 (시포 GAS) | 미러 rowIndex 를 +1 틀리게 보내도 거부 또는 맞는 사람 |
| 2 | **방금 저장한 사람이 5분간 안 보임** — 저장은 시트, 읽기는 서버 | 942 이후 매 저장 | 저장 응답의 그 한 명을 화면이 바로 그린다(지금 코드 재사용) + 저장 직후 서버 동기화 1회 즉시 호출(`POST /api/sync/members`, 로그인 뒤) (시토 942) | 등록 → 새로고침 → 즉시 보임 |
| 3 | **회원번호 없는 새 회원이 서버에서 사라짐** — 실측 6명(남정길 09-03 포함). 등기부 부여가 8-26 일괄 1회뿐 | 지금부터 매 등록 | 941 — 등록 쓰기 경로 뒤에 `member_registry_build` 재호출(멱등) · 미러 `unnumbered` 가 0 아니면 경보(이미 배선) | `/api/members/summary` unnumbered = 0 |
| 4 | **회원번호 충돌** — ✅해소(시토 22:0x 실측): 같은 번호가 유효회원·LOSS보관 양쪽에 있는 3건(M00225·M01103·M01180)은 **같은 사람의 이력**이었다. 미러 열쇠를 (회원번호, scope) 로 바꿔 둘 다 싣고 충돌 0 | — | 끝 | `members_collisions` = 0 (실측 0) |
| 5 | **유효/종료 판정이 두 벌** — GAS `_memberIsValid_`(잔여일 ≥0 또는 빈칸 + LOSS 태그 아님)로 가른다. 서버가 `end_date` 로 다시 판정하면 신규 등록 직후(기간 미입력) 회원이 종료로 빠진다(08-01 실사고 재현) | 943 집계 라우트 만들 때 | 서버는 판정하지 않는다 — 미러의 `scope`(GAS 가 이미 가른 값)만 쓴다. 신규/재등록도 `reg_class`+GAS 판정 결과만 | 서버 유효 건수 = GAS scope=valid 건수 |
| 6 | **강습 회원이 서버에 없다** — `members` 표는 멤버십 원장만. 강습 등록 = 문의 시트 SUC 행 + 등록 5칸(확정종목·등록일·횟수·시작·종료). 등록원장(`lesson_registry`)은 07-20 이후 멈춤 | 강습 회원관리 화면을 서버로 돌릴 때 | 강습 등록은 `inquiries` 표(type=성인강습·유소년강습, status=SUC·단기SUC)에서 뽑는다 — 별도 표 만들지 않음. 등록일 공란 264건은 접수일 폴백(지금 화면과 같은 규칙) | 화면 8월 등록 성인 4·유소년 9 와 일치 |
| 7 | **예약작업·집계가 GAS 를 계속 부른다** — 시포 모듈 4개(문의 스냅샷 3분·아침 집계·회원 롤업·미배정 알림) + 매출보고 기입기 + 실무진 피드백 감시 | 이관 뒤에도 GAS 부하·할당량 그대로, 화면(서버)과 보고(GAS) 숫자가 갈릴 수 있음 | 쓰기 API 전까지는 그대로 둔다(원천이 같아 값은 같음). 쓰기 API 뒤 순서 = 스냅샷 → 아침 집계 → 롤업 순으로 `/api/…` 로 교체(시포). 3분 스냅샷은 미러가 있으면 **없앤다**(net-zero) | 두 숫자 하루 대조 0 차이 |
| 8 | **GAS 캐시 워머(5분 시간 트리거)가 이관 뒤 쓸모없이 돈다** — 회원 명단·요약·문의 캐시 예열(GAS 안 트리거) | 942·943 뒤 | 그때 트리거 삭제(시포). 미리 끄면 지금 화면이 다시 11초 | 트리거 목록에 warm 없음 |
| 9 | **카톡 링크가 옛 주소(GitHub Pages)** — 실무진 전달문·피드백 회신·보고서 링크 전부 `wellperion-cao.github.io/…` | 도메인 합친 뒤 | 링크 정본 한 곳(`kakao_report_sender` 의 주소 상수)만 `erp.wellperion.com` 으로 바꾼다. 카톡은 쿼리를 지우므로 스텁 페이지 규칙 유지 | 카톡에서 링크 눌러 로그인 뒤 그 화면 |
| 10 | **카톡 인앱 브라우저는 로그인 쿠키를 따로 둔다** — 실무진이 카톡 링크 누를 때마다 로그인 (추정 — 실기기 미측정) | 관문 뒤로 간 첫날 | 로그인 쿠키 30일 + 「크롬으로 열기」 안내 한 줄을 첫 안내문에 (웰리 안내문 · 시토 확인) | 실무진 1명 실기기 확인 |
| 11 | **문의 스냅샷 공개 파일에 실명·전화** — `status/inquiry_snapshot_*.json` 이 GitHub raw 로 공개, 보고서 2페이지가 그걸 읽음(GM 수용 2026-08) | 서버로 옮기면 이유가 없어짐 | 943 에서 보고서가 `/api/inquiries` 를 읽게 되면 스냅샷 파일을 저장소에서 뺀다(GM 결정 1줄 필요 — 옛 수용을 되돌리는 것) | 저장소에 스냅샷 파일 없음 |
| 12 | **잔여일·결제는 브로제이 원천** — 시트 잔여일은 브로제이 값을 사람이 붙여넣은 수식. 서버 `remain_days` 는 그 순간 문자열 | 갱신 임박·LOSS 예측 화면 | 배908 브로제이 API 전까지는 미러 값 그대로 + 화면에 「시트 기준 N분 전」 표시 | 표시 있음 |
| 13 | **판정·집계 로직이 GAS 안에만** — 신규 판정 `_isNewRegistration_`, 유효 판정, LOSS 태그, 법인 탭 gid, 이름 정규화 | 쓰기 API 로 넘길 때 | 넘기는 순간 GAS 함수를 **지우고** 서버로 옮긴다(두 벌 금지). 옮길 목록 = 이 표 #5 + `_sportBuckets_`(강습 종목 두 벌 어휘) | GAS 에 같은 이름 함수 없음 |
| 14 | **되돌리기 준비** — 시트 7벌 백업은 8-26 기준(`status/_private/`) | 쓰기 API 전환 직전 | 전환 전날 백업 재실행(`cpo_member_registry_migrate.py --backup`) + 서버 DB 스냅샷(시토) | 백업 날짜 = 전환 전날 |
| 15 | **ERP 상단 카드(유효·법인·등록 —명)가 아직 GAS 집계** — 시토 캡처(942)에 빈 값. 문의 DB 는 오늘 서버로 돌렸고, 카드 5개(cpo_today_stats·member_active_summary·cpo_churn_stats…)는 943 보고서와 같은 원천 | ERP 첫 화면 | 943 에 만든 집계 라우트를 멤버십 회원관리 카드도 쓰게(시토·시포) | 카드에 숫자 |

## 넘기는 순서 (시토 배 4척에 이미 걸려 있음)
931·941·942·943 = 09-03 입항 → **#15 카드 원천(시토)** → #1 회원번호 열쇠(시포 · 내일 마감 후) → 쓰기 API(시토 ③ · #7·#8·#13·#14)

## GM 결정 1건
#11 — 문의 스냅샷 공개 파일을 서버 전환 뒤 저장소에서 뺄지(옛 수용 되돌림).
