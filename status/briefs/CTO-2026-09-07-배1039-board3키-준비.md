# 배1039① 공지·가이드·매뉴얼 3보드 — 서버 우선 전환 패치 (2026-09-07 준비 · 09-08 적용)

## 대상
`3. 웰페리온 가이드/wellperion_guide(main).html` — OPS_POLICY_BOARD(규정) · OPS_GUIDE_BOARD(가이드) · OPS_MANUAL_BOARD(요일별 업무) 3키. 지금은 세 키 모두 GAS(`action=board&key=`)로만 읽는다.

## 패치 내용
- membership.html의 `ERP_API_ON`(호스트가 erp.wellperion.com·15.164.151.105일 때만 true) + `apiFirst` 패턴을 그대로 재사용.
- `boardFirst(key)` 헬퍼 1개 추가: ERP 도메인이면 `/api/board/{key}` 먼저 호출, 실패하면 종전 GAS로 폴백. GitHub Pages(ERP_API_ON=false)에서는 그대로 GAS 직행.
- 3개 호출부(`loadDocPolicy`·`loadDocGuide`·`loadOpsBoard`)의 `jget(CHECK_API+"?action=board&key=...")` 를 `boardFirst('...')` 로 교체. 응답 모양(`{ok,board}`) 동일이라 화면 쪽 어댑트 없음.
- 새 패턴·새 파일 없음 — 헬퍼 1개 + 호출부 3줄 교체가 전부.

## 서버 라우트 확인
`server/erp_api/api_board.py` — `GET /api/board/{key}` 가 `board_cache`(sync_board.py 가 5분마다 GAS를 그대로 떠온 것)를 `{ok, board, synced_at, _source}` 로 돌려준다. GAS 응답과 1:1 동일 모양 — 어댑트 불필요.
⚠️ 이 라우트는 포트 8001(`erp-api.service`)에서만 뜬다. 포트 8000은 `erp-auth.service`(로그인 게이트)라 거기로 확인하면 전부 404 — 헷갈리기 쉽다.

## 적용
```
git -c core.autocrlf=false apply status/briefs/CTO-2026-09-07-배1039-board3키-패치.diff
```
⚠️ `-c core.autocrlf=false` 필수 — 이 리포는 전역 `core.autocrlf=true`라 이거 없이 적용하면 파일 전체가 CRLF로 바뀐다(3키 부분만 바뀌는 게 아니라 1070KB 전체가 diff에 걸림). `git apply --check`(플래그 없이)는 통과하지만 실제 적용은 반드시 위 명령으로.

## 검증 절차
1. `node --check` 는 .html 을 직접 못 읽음 — 적용 후 `<script>` 블록 전부를 뽑아 이어붙여 `node --check` (구문 오류 유무만 확인, 이미 패치본 기준으로 사전 확인 완료 · 통과).
2. ERP 도메인(erp.wellperion.com, 시크릿 크롬)에서 해당 3보드 화면을 열어 규정·가이드·요일별 업무 카드 내용이 GAS 버전과 동일한지 육안 대조.
3. 브라우저 개발자도구 Network 탭에서 `/api/board/OPS_POLICY_BOARD` 등 3건이 실제로 나가는지, 실패 시 콘솔에 `[OPS_POLICY_BOARD] API 실패 → GAS 폴백` 경고가 뜨고 화면이 끊기지 않는지 확인.

## 유실 0 증거 — GAS ↔ 서버 미러 md5 대조 (2026-09-07 10:05 KST 실측, 읽기 전용)
| 키 | GAS md5 | 서버(포트 8001) md5 | 일치 |
|---|---|---|---|
| OPS_POLICY_BOARD | 411a32d9…39fb8 | 411a32d9…39fb8 | ✅ |
| OPS_GUIDE_BOARD | b911ee80…35dd1 | b911ee80…35dd1 | ✅ |
| OPS_MANUAL_BOARD | 37a6259c…dff0bd | 37a6259c…dff0bd | ✅ (양쪽 모두 board:null — 아직 실제 데이터 없음, 화면은 시드값으로 폴백. GAS·서버 값이 같으므로 전환해도 손실 없음) |

방법: GAS는 `action=board&key=` 직접 호출(공개), 서버는 `ssh -i ~/.aws/wellperion-sito.pem ec2-user@15.164.151.105 curl -s localhost:8001/api/board/{key}` (읽기만, 쓰기 없음). 양쪽 `board` 필드를 JSON 정규화 후 md5 비교.

## DONE
패치·브리프 준비 완료. 오늘(09-07) 적용 금지 — 09-08 아침 러너가 위 `git apply` 명령으로 반영.
