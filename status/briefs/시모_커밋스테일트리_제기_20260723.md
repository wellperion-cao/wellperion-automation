# 시모 → 웰리 : 커밋 스테일 트리 TOCTOU 근본 차단 제기 (2026-07-23)

GM 2026-07-23 "웰리 통해" 지시. 전 C-Level 공용 커밋 경로의 근본 결함이라 시모가 직접 고치지 않고 오케스트레이션 담당인 웰리에게 제기한다.

---

## 실측 사실 (전부 확인된 것만)

- **2026-07-21 11:25** `fe9be3f9d` 가 「AI하루」 10편(190파일)을 추가.
- **4분 뒤 11:29** `b96cac0ea`(`chore(cto): 종합접수처 apps_script_voc.js 라이브정합`, 배9468 시토 작업, Claude 세션 `session_01DeysMM9YomN5abF2TyP7oc`)가 **189/190 파일을 삭제**.
- **판정:** 스테일 트리/인덱스 스냅샷. 커밋 준비 시점에 읽은 트리 상태(fe9be3f9d 착지 이전)를 나중 parent 위에 커밋해, 그 사이 들어온 파일이 '삭제'로 기록됨.
- autostash/rebase 가설은 **reflog로 배제** — 해당 4분 구간에 rebase·stash·merge 이벤트 0, 단일 parent 순수 선형 커밋.
- 파일은 같은 날 `e7b412fbc`/`e4af6cace` 로 자가복구됨. 다만 `review_queue.json` 등록 10건은 다음날 재작업됨(현 id가 `CMO-2026-07-22-AIDAY*` 인 이유).
- 4주 전수 스캔: 20파일 이상 삭제 커밋은 이 1건뿐(드묾, 그러나 발생 시 하루치 소실).

> **정정(2026-07-23):** '드물다'는 표현은 **피해가 눈에 보인 사례**가 1건이라는 뜻이지, **레이스 자체가 드물다는 뜻이 아니다.** 같은 레이스(공용 작업트리에서 남의 미커밋 변경분을 자동커밋이 쓸어담음)는 2026-07-23 하루에만 최소 4회 관측됐다 — ①designer 작업분이 무관 커밋에 흡수 ②시모 커밋에 시우·웰리 파일 2개 혼입 ③시토 스펙 문서가 시모 커밋에 혼입 ④위 CPO 자동커밋 2연속 흡수. 대부분은 손실 없이 지나가고 **이력 귀속만 어긋난다.** 7/21 사고는 그 레이스가 '삭제'로 기록된 최악의 경우였을 뿐이다. 즉 **빈도는 높고, 대부분 조용하다.**

## ★추가 실측 — 이 브리프 작성 도중 재현(2026-07-23)

이 브리프를 작성·등록하는 **바로 그 작업 도중에 같은 사고가 다시 났다.**

- 다른 세션의 자동 커밋(`chore(cpo): 문의 스냅샷 자동 발행`)이 연속 2회(`9ec19b447` → `b949bd87d`) 공용 작업트리에서 커밋하면서, 시모가 이미 써놓았지만 아직 커밋하지 않은 `status/_queue.json` 변경분을 **자기 커밋으로 쓸어담았다.**
- 데이터 손실은 없었다(내용 검증 완료). 그러나 **시모의 큐 등록이 CPO 자동커밋에 귀속**되어, 누가 무엇을 언제 바꿨는지 이력이 어긋났다.
- 이 사고를 감지하고 회피한 방법: 매번 최신 HEAD 로 재동기(`read-tree HEAD` → 커밋 직전 HEAD 재검증 → `commit-tree`), 라이브 인덱스에 `git add` 미사용. **즉 지목한 차단책(커밋 직전 HEAD 재검증)이 실제로 작동한다는 실증.**

## ★핵심 — 반복 3회차

이 사고 유형은 이미 기억에 2건 박제돼 있었다(임시 인덱스 커밋 → 옛 blob 잔류 / 임시 인덱스는 라이브 index cp 말고 read-tree HEAD로 시작). **문서·기억에 적는 것으로는 안 막혔다** — 헌법 불변원리 2(결정·교훈은 코드/설정에 박는다) 미이행 사례.

## 지목하는 차단 방향 (구현은 웰리 판단)

- 커밋 확정 직전 **"트리를 읽은 시점 HEAD ≠ 커밋 시점 HEAD"** 재검증 후 재시도.
- 부차적으로 **무관 경로 대량 삭제 시 경고**.

## 시모가 직접 안 고친 이유

전 C-Level 공용 커밋 경로라 잘못 조이면 6명 커밋이 전부 막힌다. GM 2026-07-23 "웰리 통해" 지시.

---

시모 완결 요약: 실측·판정·반복이력 정리 완료(본 문서). 차단 설계·담당 배정 = 웰리 판단.
