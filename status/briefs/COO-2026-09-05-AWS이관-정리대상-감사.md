# 시우(COO) 영역 — AWS 이관 뒤 정리대상 감사 (읽기 전용 · 2026-09-05)

대상: `3. 웰페리온 가이드/coo/**` · scripts 중 coo/reception/check/ops/kiosk 관련 · `status/module_registry.json` owner_role=coo. 파일 수정·삭제 없음.

## 결론 먼저

- **"두 벌처럼 보이는 접수 화면들은 실제로는 두 벌이 아니다.** 청중이 다르다 — ERP(`erp.wellperion.com/coo/reception/*.html`, `종합접수처_현황.html`·`lost_found_*.html`)는 **직원용**, `wp_*_block*.html`은 워드프레스에 주입되는 **회원 공개용** 원본이다. `erp/modules.json`이 전자를 모듈로 등록하고, `docs/superpowers/specs/2026-09-03-wordpress-aws-migration.md`·`wordpress_admin_playwright.py`가 후자를 라이브 재주입 대상으로 관리한다. 삭제 대상 아님.
- `check_reception_wp_drift.py`(팀장 가설: `wordpress_admin_playwright.py --mode drift`와 중복)는 **직접 확인 결과 다른 역할**이다 — 전자는 pre-commit 훅(경고전용, staged 파일 감지), 후자는 `coo_page_guard.py`가 아침에 부르는 실제 페이지 대조기. 둘 다 살아있고 서로 다른 트리거. 가설 기각.
- 진짜 정리 대상은 아래 D(디렉터리에 낀 OMC 세션 잔해·백업 1건)와 E(주소 3벌 중 낡은 것) 두 곳.

## 표

| 항목 | 종류 | 근거(경로·참조수) | 제안 | 삭제 전 검증 방법 |
|---|---|---|---|---|
| `coo/reception/wp_lost_found_gallery_block_en.html` vs `wp_lost_found_gallery_block.html` vs `lost_found_gallery.html` | A(겉보기만) | 각각 참조 5·7·21건. `운영모듈_GAS_AWS_DB_이전_정의서_20260904.md` GAS 인벤토리 표에 셋 다 별개 화면으로 등재(EN/KO 워드프레스용 · ERP직원용) | 유지 | 해당없음 |
| `coo/reception/reception_block.html` vs `wp_reception_block_en.html` | A(겉보기만) | KO/EN 언어별 원본, 둘 다 워드프레스 라이브 재주입 대상(post 8434/8751, `배984` 문서에 실측 완료 기록) | 유지 | 해당없음 |
| `public/ko/reception.html`·`public/en/reception.html`·`public/ko/lookup.html`·`public/en/lookup.html`·`public/ko/lost-found*.html` | A(미래 중복 가능성 있으나 아직 아님) | `배984`(2026-09-05) ③항 "공개 화면 서버 직접 서빙 — 미완료(BLOCKED)": 지금은 vc_raw_html 조각이라 정적 서빙 안 됨, 실제 공개 트래픽은 아직 워드프레스가 받는다 | 유지·재방문(배984 후속 완료 시 wp_*_block*.html과의 관계 재판정) | 배984 후속 배 완료 여부로 재확인 |
| `scripts/check_reception_wp_drift.py` | B(가설이었으나 기각) | pre-commit 훅(2참조: `.omc/plans/2026-07-15-lost-found-gallery-plan.md`·본인 docstring) — `wordpress_admin_playwright.py --mode drift`(coo_page_guard 트리거)와 역할 분리 확인 | 유지 | `.git/hooks/pre-commit`에 훅 등록 확인(직접 조회 못 함·간접 grep으로 참조만 확인) |
| `scripts/verify_reception_flow.py` | B(참조 적음) | `docs/superpowers/specs/2026-09-03-wordpress-aws-migration.md`에 "5회 사용" 기록·schtasks 예약작업 없음 → 수동 검증 도구로 추정 | 유지·확인 요망(수동 도구인지 시토 확인 필요) | 시토에게 마지막 실행일·용도 질의 |
| `scripts/fix_reception_design.py` | B(참조 있음, 이름은 일회성 함정) | 11회 참조 — 이름은 "고정"이지만 마이그레이션 문서가 현재도 참조 중이라 완결된 1회성인지 재사용 도구인지 이 감사에서 확정 불가 | 유지·확인 요망 | 시토에게 1회성 여부 질의 후 재실행 불필요 확인되면 archive |
| `coo/reception/.omc/state/**`(agent-replay-*.jsonl·session 상태 6개 폴더), `coo/chairman/.omc/state/**`, `coo/check/.omc/state/**`, `coo/todo/.omc/state/**`, `*/tmp/hud_role_cache.json` | D(디렉터리에 낀 세션 잔해) | Git 미추적(`git ls-files` 0건) — `.gitignore`가 `.omc/`·`tmp/`를 이미 차단 중. 즉 저장소엔 안 남지만 **작업 디렉터리엔 그대로 쌓여 있다**(가이드 콘텐츠 폴더에 에이전트 세션 부산물이 낀 상태) | 로컬 정리(디스크 청소) — 저장소·git 영향 없음 | 이미 gitignore 대상이라 삭제해도 git에 흔적 없음, 안전 |
| `coo/chairman/GM업무.html.bak_20260815` | D(백업 파일) | Git 미추적(`.gitignore`의 `*.bak_*` 패턴 매치) — 살아있는 `GM업무.html`이 별도 존재 | 로컬 정리(디스크 청소) | 이미 gitignore 대상, 삭제해도 git 영향 없음 |
| 종합접수처 라이브 주소 3벌 — GitHub Pages(`wellperion-cao.github.io/wellperion-automation/coo/reception/...`) / ERP(`erp.wellperion.com/coo/reception/...`) / 워드프레스(`wellperion.com/ko,en/reception,lost-found,lookup`) | E | 카톡 발신로그 실측: `logs/kakao_sent-2026-09-02.log`·`09-03.log`는 전부 GitHub Pages 주소 사용(직원 방 6건) → `logs/kakao_sent-2026-09-04.log`부터 전부 `erp.wellperion.com` 주소로 전환(직원 방 3건, 09-04 07:58부터). 회원 공개 제출은 워드프레스가 계속 받음(`배984` 실측: `/ko/reception/` post 8434 라이브 재주입 완료) | **직원용 = ERP 주소로 완전 전환 완료(09-04부터), GitHub Pages 주소는 이제 낡음(닫기 후보). 회원용 = 워드프레스 유지(공개 화면 서버 직접 서빙 아직 BLOCKED이므로 대체 불가)** | GitHub Pages 배포(`wellperion-cao.github.io`)를 끄기 전에: ① 카톡 발신 스크립트(`kakao_report_sender.py` 계열)가 실제로 ERP 주소만 만드는지 코드로 재확인 ② 과거 대화 스크롤로 남아있는 GitHub Pages 링크가 클릭될 가능성(구 메시지 재열람) 있으니 GitHub Pages 자체를 끄기보다 리다이렉트만 걸어 안전망 유지 |
| `coo-*` module_registry 4건(`coo-check-status`·`coo-work-approval`·`coo-reception`·`coo-notice`) | C | 전부 heartbeat 오늘(09-05) 갱신 확인(`coo-check-status.json` 09:39·`coo-work-approval.json` 09:39·`coo-reception-health.json` 15:31) · `coo_registry.py`는 16개 파일에서 호출 중(살아있음) | 유지 — 폐기 대상 0건 | 해당없음 |
| `coo-schedule-ssot`·`coo-monthly-ops`(id 접두 `coo-`, owner_role은 이미 `ceo`로 이관됨, 08-15 결정) | C(표기 혼선 소지, 실질 폐기 아님) | 등록부 `_owner_note`에 이관 경위 명시 — id 접두는 배선 보존 목적으로 의도적 유지 | 유지(이미 문서화된 의도적 상태, 재정리 불필요) | 해당없음 |

## 위험해서 손 안 댄 것

- `coo/chairman/**`의 회장님·대표님 개인 지시사항·미팅록·검토 문서 다수 — 사람 기록이라 종류 판정·삭제 제안 모두 보류, 유지·확인 요망.
- `server/erp_api/sync_reception.py`의 5분 주기 `_replace()`가 GAS 시트에서 전체 삭제 후 재적재 — 오늘 다른 레인(`CTO-2026-09-05-서버변경-검수-findings.md` C1)에서 이미 치명 결함으로 별도 보고됨. 이 감사 범위(중복·무의미) 밖이라 표에 안 올리고 참고만 남김 — 정리 대상 판단에 영향 줄 수 있어 시토 레인과 조율 필요.
- `coo/리셉션 업무/` 폴더(별도 GAS `AKfycbxcPsm-`·`AKfycbyyN0I7`) — 이번 종합접수처(REG GAS)와 이름은 비슷하나 다른 시스템(라커관리 포함). 중복 여부는 이번 grep 범위 밖, 유지·확인 요망.

## 확인 못 한 것

- 키오스크 첫 화면이 거는 링크 — 저장소 내 `*.html`에 "kiosk"가 포함된 파일이 0건이라 실물을 못 찾음(별도 시스템이거나 다른 명명일 가능성). E항 결론은 카톡 로그만 근거로 함.

DONE: status/briefs/COO-2026-09-05-AWS이관-정리대상-감사.md