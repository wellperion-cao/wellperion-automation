# 배946 — 시포 도메인 서버 보완 5건 (시토 실행)

작성 = AI 시토 · 2026-09-05. 정본 = `status/briefs/CPO-2026-09-03-AWS이관-시포도메인-문제점-보완.md` #2·#6·#10·#14·#15.
읽고 나서 확인한 것 — **#2·#10·#14·#15 는 이미 배포돼 살아 있었다**(다른 배에서 이미 구현). 실측으로 확인만 하고 코드는 안 건드렸다.
새로 만든 건 **#6 하나**(`/api/lesson/members`). 같은 시각 다른 레인이 `server/erp_auth/app.py`·`server/erp_api/sync_inquiries.py` 를 고치고 있어 그 두 파일은 손대지도, 배포하지도 않았다(공용 `deploy_api.sh` 대신 바뀐 파일만 개별 scp).

## #2 저장 직후 동기화 1회 — 이미 됨(배961)
`server/erp_api/api_write.py` 의 `MIRROR_SYNC["member_active_update"]="sync_members.py"` + `_schedule_sync`(3초 디바운스) 가 회원 쓰기가 GAS 에 ok 로 저장된 직후 `sync_members.py` 전체 동기화를 자동으로 돌린다. 서버 배포본에 같은 코드 확인(`grep MIRROR_SYNC /srv/erp/api/api_write.py`). write_log 에 09-04 18:01 `member_active_update ok` 실행 이력도 있다.
- 실제 값 조회 방식(`POST /api/sync/members` 로 명시 호출)과는 구현이 다르지만 결과는 같다 — 쓰기 응답이 ok 인 순간 배후에서 미러가 갱신된다. 새 엔드포인트를 또 만들지 않았다(중복).

## #6 강습 회원 = inquiries 표 SUC — 신규 라우트
문제: 기존 `/api/lesson/registry`(배922)는 GAS `lesson_registry_list` 를 그대로 미러링하는데, 그 원천 시트(등록원장)가 07-20 이후 갱신이 멈췄다(정의서 실측).
조치: `server/erp_api/api_lesson.py` 에 `GET /api/lesson/members?type=성인강습|유소년강습&from=&to=` 신설 — 그 등록원장을 거치지 않고 이미 5분마다 동기화되는 `inquiries` 미러를 `status IN (SUC,단기SUC)` 로 직접 읽는다. 등록일 우선순위는 화면 헬퍼 `_lessonRegDateStr`(membership.html)와 동일: 등록일(regDate) 없으면 접수일(timestamp) 폴백. 새 표는 안 만들었다(정의서 지침).
- 배포·재기동·`--selftest` 통과 확인.
- 실측(8월, 서버 실측 값 — inquiries 미러 원본에서 직접 셈):
  | 종목 | count |
  |---|---|
  | 성인강습 | 7 |
  | 유소년강습 | 13 |
- ★정의서 확인법(성인4·유소년9)과 다르다 — 그 숫자가 어디서 나온 값인지(등록원장·문의 어느 쪽 집계인지) 확인이 안 됐다. 화면 헬퍼가 실제로 쓰는 규칙(등록일→접수일 폴백)을 그대로 옮긴 결과를 그대로 적는다(추정치로 끼워맞추지 않았다). 시포 확인 필요.

## #10 카카오 인앱 쿠키 — 이미 됨, 코드 미변경
`server/erp_auth/app.py` 배포본 실측(575줄 대):
```
SESSION_DAYS = 30
r.set_cookie(COOKIE, ..., httponly=True, samesite="lax", path="/", secure=https)
```
- 30일 세션(정의서 요구값과 일치) · `SameSite=Lax`(카카오 인앱 웹뷰의 최상위 GET 이동에서 쿠키가 살아남는 값 — Strict 였으면 깨졌을 것) · `Secure`는 `X-Forwarded-Proto: https`(nginx) 조건부로 정확히 켜짐.
- **이 파일은 지금 다른 레인이 동시에 고치고 있어(같은 시각 uncommitted 변경 확인) 손대지 않았다.** 코드를 고칠 필요도 없었다 — 이미 요구값과 일치.
- 「크롬으로 열기」 안내 한 줄은 서버 코드가 아니라 안내문 문구라 시포/웰리 몫으로 남긴다.
- 실기기(카카오 인앱) 확인은 못 했다(추정 아님 — 실측 안 됐다는 뜻).

## #14 서버 DB 일일 스냅샷 — 이미 됨(배922), 정의서보다 넓은 방식
`/etc/cron.d/erp-pg-backup`(KST 03:00) → `/usr/local/bin/erp-pg-backup.sh` → `pg_dump` → S3 `wellperion-erp-backup`(30일 보관, 정의서가 말한 로컬 7일·`/srv/erp/backup` 보다 오프사이트+긴 보관이라 상위호환).
`/srv/erp/backup.log` 실측 — 최근 이틀 연속 성공:
```
2026-09-03 18:00:06 백업 완료 erp/erp-20260904-0300.sql.gz 1172129B
2026-09-04 18:00:07 백업 완료 erp/erp-20260905-0300.sql.gz 1209004B
```
S3 버킷 자체 리스팅은 인스턴스 역할이 PutObject 전용이라 막혀 있음(의도된 최소권한) — 로그가 실측 증거.

## #15 멤버십 상단 카드(유효·법인·등록) — 이미 됨(배943), 라우트만 확인
카드 재료는 이미 서버가 준다 — 새로 만들 라우트가 없었다:
| 카드 | 라우트 | 서버 실측(2026-09-05) |
|---|---|---|
| 유효 | `GET /api/members/summary` (`by_scope.valid`) 또는 `GET /api/report/member_active_summary`(`validTotal`) | 995 |
| 법인 | `GET /api/members/summary`(`by_scope.corp`) 또는 `/api/report/cpo_today_stats`(`memberCorp`, 종료일 지난 것 제외) | 32 / 31 |
| 등록(이번달) | `GET /api/report/cpo_today_stats`(`monthReg`) | 4 |
남은 건 화면 배선(시포 몫) — 서버 쪽엔 더 할 일이 없다.

## 배포
- 바뀐 파일: `server/erp_api/api_lesson.py` (신규 라우트 + selftest)만 배포(scp 개별 · `deploy_api.sh` 미사용 — 이유는 위 사유).
- 서버 `erp-api` 재기동·`systemctl is-active erp-api` = active 확인.
- 커밋 대상: 이 문서 + `server/erp_api/api_lesson.py`(`server/erp_api/sync_inquiries.py`는 다른 레인 소유라 미포함).

DONE: 5건 = #2 OK(기배포 확인) · #6 OK(신규 배포·실측) · #10 OK(기배포 확인·코드 미변경) · #14 OK(기배포 확인) · #15 OK(기배포 확인)
