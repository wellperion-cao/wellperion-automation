# 알림 점검 — 담당·상태 필터 (2026-08-25)

결론: 지정 7개 알림 전부 원천 값 분포와 코드 판정 조건을 직접 대조했다. 현재 실 데이터 기준으로 조용히 걸러지는 값은 못 찾았다(전부 정상). 과거 같은 유형 사고(dept 매칭 08-17, LOSS사유 칸명 08-21)는 이미 수리돼 반영 중이었다.

| 알림 id | 원천 값 분포 | 코드 판정 조건 | 걸러지는 값 | 판정 | 고치는 법 한 줄 |
|---|---|---|---|---|---|
| kakao-sla-unassigned-24h | owner 실측(강습+멤버십 2124행): 임정은 668·박민서 519·이형주 195·…·웹 자동접수 14·공백 22 (자동값·공백 외 전부 실명, orphan 없음) | `_is_assigned_owner`(report_stream_1_impl._AUTO_OWNER_VALUES = "웹 자동접수"/"자동접수"/"-") — 이 3값이면 미배정 | 없음 | 정상 | — |
| kakao-ops-done-immediate | 업무 SSOT 완료(87건) 담당자: 나우열M 49·이경연 실장 19·최준용M 12·윤병현AM 2·김남욱 GM 2·공백 2·이정헌 소장 1 | `_ops_done_rows`(send_ops_digest.py) — OPS_STAFF=("최준용M","이경연 실장","윤병현AM") 부분문자열 포함 | 없음 — 33건(이경연 실장19+최준용M12+윤병현AM2) 전부 매칭, 나우열M 등은 운영부 아니라 의도적 제외 | 정상 | — |
| kakao-intake-relay-15min | 접수(121건) dept 분포: 운영부 61·시설부 38·지원부(여) 9·지원부 8·P.T팀 2·수영팀 2·지원부(남) 1 | `_intake_room_for`(report_stream_2b_reception.py) — "강습" 포함 또는 "팀"로 끝나면 ★부서장, 그 외 기본방 | 없음 — 8/17 실측 근본수리(11개 부서값 재정의) 반영돼 현재 분포 전부 정확 라우팅. `_aging_block`도 이 함수 하나만 재사용(약속 L01) | 정상 | — |
| tg-0750-cpo-sheet-contract | 6개 시트 라이브 헤더·값(inquiry_2026·lesson_adult·lesson_wsc·lesson_register·member_active·member_hold) | `check_sheet` — req 칸 존재·allowed 값·공란율·형식·최근7일 0건 | 없음 — `--dry-run` 실행 결과 활성 위반 4건(진행현황 '미정'·'중복(삭제)', 미등록사유 레거시값, lesson_adult 진행상황 '대기', member_active 미등록사유 '고민중'·'심사대상') 전부 정확히 잡아내는 중(신규 0건=이미 알려진 위반이라 재알림만 안 감, 침묵이 아님) | 정상(담당/상태 필터형이 아니라 칸 계약 검사기 자체 — 기능 확인 목적으로 함께 실행) | — |
| kakao-depthead-inquiry | kakao-sla-unassigned-24h와 같은 owner 데이터·같은 로직 재사용(report_stream_1_impl) | 위와 동일(`_is_unassigned_active`) | 없음 | 정상 | — |
| tg-digest-1-inquiry | 위와 동일 데이터·로직 | 위와 동일 + `_selftest_uncontacted()` 자가검사(담당만 채워지고 컨택없는 케이스 회귀 방지) 존재 | 없음 | 정상 | — |
| win-0730-ops-digest | ① 완료건 담당자 = kakao-ops-done-immediate와 동일(OPS_STAFF) ② 회신 부탁 절(열린 요청) owner 실측(_digest_ledger.json): 공백 38·이경연 실장 34·나우열M 30·이정헌 소장 19·임정은M 2·윤병현AM 1 | ① 동일 ② `build_reply_nudge_items` — `_NUDGE_MEMBERS`=["이경연 실장","이정헌 소장","나우열M"] 완전일치, 밖이면 `_NUDGE_MEMBERS[0]`(이경연 실장)로 폴백 | ①없음 ②윤병현AM·임정은M은 명단 밖이지만 드롭이 아니라 이경연 실장 묶음에 "(원담당 건)" 태그로 노출됨(윤병현AM은 코드 주석상 약속 L24 의도적 설계, 임정은M도 같은 폴백 경로로 안 사라짐) | 정상(②는 결함 아님 — 폴백이 표시는 보장, 인원 구분만 흐릿해짐) | 필요시: `_NUDGE_MEMBERS`에 임정은M 등 실제 등장 담당 전원을 넣으면 묶음 대신 개별 표시 가능(현재도 유실은 아님) |

## 점검 방법
각 항목은 `C:/Python314/python.exe`로 실 데이터를 직접 불러 `Counter`로 원천 값 분포를 세고, 코드의 판정 함수(상수 리스트·정규식)를 나란히 대조했다. cpo_sheet_contract는 `--dry-run`으로 실제 함수를 호출해 재현했다(발송 함수는 호출하지 않음). 실행은 전부 읽기 전용 — `send`·`kakao_report_sender`·`notify_*` 등 발신 함수는 호출하지 않았다.

DONE: status/briefs/260825_알림점검_담당상태.md
