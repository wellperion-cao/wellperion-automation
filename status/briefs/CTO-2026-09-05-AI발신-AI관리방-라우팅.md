# AI 내부 텔레그램 → AI관리 방 라우팅 (GM 지적 2026-09-05)

## 원인 (한 문장)
`notify_gm_progress.py`의 AI관리 방 일일 한도(DAILY_CAP=40)가 15:19에 소진되자, 이후의 모든 L18 완료 보고가 "조용한 소멸 방지" 폴백 규칙(2026-07-30)을 타고 **GM 개인 업무관리 방**으로 샜다. 완료 보고는 `alert_router.py`의 자체 분류로도 GM 손이 필요한 `gm_action`이 아니라 AI 살림 `tech_check`이므로, cap 초과가 GM 방으로 갈 이유가 없었다.

## 오늘 실측 — 업무관리(8254867551)로 간 30통 분류
| 발신원 | 건수 | 판정 |
|---|---|---|
| L18 완료 보고 (cap 초과 폴백, 15:26~17:02) | 17 | ★버그 → 이번에 고침 |
| 카톡 에이전트 답장(다이어트캠프) | 1 | 정상(현실 업무) |
| 🧭 오늘의 항로 진행·대기 46척 (09:39) | 1 | 정상 — office_report는 설계상 업무관리(아침 보고 재발송 부산물, 라우팅 버그 아님) |
| CMO 마케팅 월간 보고 | 1 | 정상(GM 보고) |
| 카톡 사본 [테스트] 하루 시작/마무리 | 2 | 정상(동기화 테스트) |
| 러너 모호 배 4건 (세션 인터뷰 요청) | 1 | 정상 — `alert_router.GM_ACTION`(세션 열어야 끝나는 요청) |
| AWS 루트 MFA 3단계 | 1 | 정상(GM 보안 액션) |
| 매출·운영 현황(report_stream_3_mgmt) | 1 | 정상(GM 보고) |
| 매출 사진 보고(send_photo) | 1 | 정상(GM 보고) |
| 작업 원칙 박제 배치 | 1 | 세션 1회성 수동 발신(지속 스크립트 아님 — 코드 수정 대상 없음) |
| [테스트] 하루 시작 카톡 미발신 경보 | 1 | 잔여 후보(아래) |

"작업: Claude Code 작업 원칙 박제"는 반복 실행되는 스크립트가 아니라 그 세션이 즉석에서 보낸 1회성 메시지라 고칠 호출부가 없다.

## 고친 것
1. `wellperion-agents/telegram_notifier.py` — `TelegramNotifier.send()`에 `room` 인자 추가. 주면 `status/telegram_rooms.json` 키로 그 발신 1건만 방을 바꾼다(해소 실패 시 기존 기본값 그대로 폴백 — 조용한 소멸 없음).
2. `wellperion-agents/scripts/clevel_post_action.py` — `send_telegram()`에 `room` 인자 추가하고, `send_ai_progress_report()`의 두 폴백 호출(모듈 임포트 실패·notify_gm_progress cap/게이트 초과)을 전부 `room="AI관리"`로 고정. L18 완료 보고는 이제 어떤 경로로도 업무관리로 새지 않는다.
3. `status/notify_registry.json` — `clevel-l18-completion-report` 항목의 `room`을 옛 "업무관리 / AI관리"(2026-08-04에 이미 폐기된 audience 분기 표기)에서 실코드에 맞춰 "AI관리" 단일로 정정.

## 확인한 것
- `clevel_post_action.py --dry-run`: 정상 경로 목적지 AI 진행현황방(AI관리) 그대로.
- daily_cap 초과 상황을 모의(`notify_gm_progress.notify_or_fallback`을 `reason=daily_cap`으로 스텁)해 `send_ai_progress_report()`를 직접 호출 → 캡처된 발신 chat_id = **-5498808140(AI관리)**, 이전엔 8254867551(업무관리)이었을 자리.
- `C:/Python314/python.exe scripts/notify_registry_check.py` → 불일치 0건.
- AI관리 방에 실제 [테스트] 1통 발송 — `logs/telegram_sent-2026-09-05.log`에 `chat_id: -5498808140` 기록 확인(message_id 10085).

## 잔여 목록 (이번엔 안 건드림 — 다음 정리용)
- `daily_scheduler.py`의 "[테스트] 하루의 시작 카톡 미발신" 류 경보: 미발신 감시는 `alert_router.TECH_CHECK` 성격이라 AI관리가 더 맞을 수 있음. 오늘 것은 `[테스트]` 라벨이 붙은 시험 발신이라 실제 경보 트래픽은 아직 미확인 — 실경보 누적되면 재검토.

## 재발 조건
`notify_gm_progress.DAILY_CAP`(40건)을 넘는 완료 보고가 많은 날엔 여전히 cap 자체는 걸린다 — 다만 이제는 넘친 몫도 GM 방이 아니라 AI관리로 간다. cap을 더 올릴지는 이번 스코프 밖(요청받지 않은 변경).
