diff --git a/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html b/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html
index e252a02a5..c1a861c5f 100644
--- a/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html	
+++ b/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html	
@@ -453,6 +453,9 @@ html,body{background:var(--bg);color:var(--text);font-family:"Pretendard","Apple
 <script src="../../_assets/erp_write.js"></script>
 <script>
 const TODO_API_URL = 'https://script.google.com/macros/s/AKfycbxDwFkrxK1YIaEoSNcuw2MiHiZQ-7o5N6311ytksSyeEd86ZFOhLknOWqQgNArQvZ-7/exec';
+// ★2026-09-05 배985 — ERP 도메인에서는 서버 미러(/api/todo)를 먼저 읽는다. 이 화면은 GM 담당 결재 건도
+// 봐야 해서(위 include_gm=1&gmkey) 미러 쪽도 같은 쿼리를 그대로 넘긴다 — 서버가 같은 게이트로 판정한다.
+var ERP_API_ON = /^(erp\.wellperion\.com|15\.164\.151\.105)$/.test(location.hostname);
 
 const CAT_COLORS = {
   '[1] 매출 및 영업':  {bg:'var(--blue-bg)',   color:'var(--blue)'},
@@ -546,9 +549,26 @@ async function loadTasks() {
     // [2026-09-03 시우] GM 기안(담당자=김남욱GM)이 결재 SSOT에서 안 보이던 원인 — 서버(todo_list)가 GM 담당 행을
     //   기본 제외하는데(실무진용 업무 현황 규칙 2026-06-01) 이 화면도 같은 조회를 써서 GM 본인 결재 건이 통째로 빠졌다.
     //   결재 SSOT는 GM 이 결재하는 자리라 GM 행을 포함해 받는다(사내 게이트 뒤 · GM업무 담당 보드와 같은 키).
-    const qs = 'action=todo_list&include_gm=1&gmkey=1531';
-    const r = await fetch(TODO_API_URL + '?' + qs, { redirect:'follow' });
-    const res = await r.json();
+    const gas = async function () {
+      const qs = 'action=todo_list&include_gm=1&gmkey=1531';
+      const r = await fetch(TODO_API_URL + '?' + qs, { redirect:'follow' });
+      if (!r.ok) throw new Error('HTTP ' + r.status);
+      return r.json();
+    };
+    let res;
+    if (!ERP_API_ON) {
+      res = await gas();
+    } else {
+      try {
+        const r = await fetch('/api/todo?include_gm=1&gmkey=1531&limit=1000', { cache: 'no-store' });
+        if (!r.ok) throw new Error('api ' + r.status);
+        const d = await r.json();
+        res = { ok: true, data: (d && d.rows) || [] };
+      } catch (e) {
+        console.warn('[결재SSOT] /api/todo 실패 → GAS 폴백:', e && e.message);
+        res = await gas();
+      }
+    }
     if (res.ok) {
       ALL_TASKS = (res.data || []).map(normalizeTask);
       applyUrlParams();
diff --git a/3. 웰페리온 가이드/coo/todo/업무 현황 SSOT.html b/3. 웰페리온 가이드/coo/todo/업무 현황 SSOT.html
index 21db39c04..b7eb367a2 100644
--- a/3. 웰페리온 가이드/coo/todo/업무 현황 SSOT.html	
+++ b/3. 웰페리온 가이드/coo/todo/업무 현황 SSOT.html	
@@ -798,6 +798,22 @@ body{font-family:"Pretendard","Apple SD Gothic Neo","Malgun Gothic",system-ui,sa
    ═══════════════════════════════════════════ */
 const TODO_API_URL = 'https://script.google.com/macros/s/AKfycbxDwFkrxK1YIaEoSNcuw2MiHiZQ-7o5N6311ytksSyeEd86ZFOhLknOWqQgNArQvZ-7/exec';
 
+// ★2026-09-05 배985 — ERP 도메인에서는 todo_list 읽기를 서버 미러(/api/todo, GM업무.html 리더현황과
+// 같은 패턴)로 먼저 읽는다. 기본은 GM 행 제외(GAS 와 동일 규칙) — 이 화면은 원래도 GM 행을 안 썼다.
+// 실패·GitHub Pages 는 종전 GAS 그대로.
+var ERP_API_ON = /^(erp\.wellperion\.com|15\.164\.151\.105)$/.test(location.hostname);
+function fetchTodoList() {
+  var gas = function () {
+    return fetch(TODO_API_URL + '?action=todo_list', { redirect: 'follow' })
+      .then(function (r) { if (!r.ok) throw new Error('HTTP ' + r.status); return r.json(); });
+  };
+  if (!ERP_API_ON) return gas();
+  return fetch('/api/todo?limit=1000', { cache: 'no-store' })
+    .then(function (r) { if (!r.ok) throw new Error('api ' + r.status); return r.json(); })
+    .then(function (d) { return { ok: true, data: (d && d.rows) || [] }; })
+    .catch(function (e) { console.warn('[업무SSOT] /api/todo 실패 → GAS 폴백:', e && e.message); return gas(); });
+}
+
 const MEMBERS = ['이경연 실장','나우열M','최준용M','임정은M','윤병현AM','백승화 사원','진수아 사원','이정헌 소장'];
 
 const CATEGORIES = [
@@ -1164,7 +1180,7 @@ async function loadTasks() {
   const el = document.getElementById('mainContent');
   el.innerHTML = '<div class="loading">불러오는 중...</div>';
   try {
-    const res = await apiCall({ action: 'todo_list' });
+    const res = await fetchTodoList();
     if (res.ok) {
       const incoming = (res.data || []).map(normalizeTask);
       const cached = loadFallback();
@@ -3915,7 +3931,6 @@ window.ssotShowTasks=ssotShowTasks;
    화면이 따로 세면 여는 사람마다 숫자가 달라져 아무도 안 믿는다.
    두 판은 모수·사람 집합이 다르므로 절대 합치지 않는다. */
 (function () {
-  var TODO_API = 'https://script.google.com/macros/s/AKfycbxDwFkrxK1YIaEoSNcuw2MiHiZQ-7o5N6311ytksSyeEd86ZFOhLknOWqQgNArQvZ-7/exec';
   // 표 순서 고정 — 건수순 정렬 금지(많이 맡은 사람이 위로 올라가면 그게 곧 순위가 된다).
   // MEMBERS 가 있으면 그쪽을 쓰고(대시보드와 동일 명단), 없을 때만 이 목록으로 떨어진다.
   var ORDER = ['나우열M', '이경연 실장', '최준용M', '윤병현AM', '임정은M'];
@@ -4203,8 +4218,7 @@ window.ssotShowTasks=ssotShowTasks;
     var delays = [0, 1500, 3500, 6000];
     return new Promise(function (rs) { setTimeout(rs, delays[i]); })
       .then(function () {
-        return fetch(TODO_API + '?action=todo_list', { redirect: 'follow' })
-          .then(function (r) { return r.json(); }).catch(function () { return null; });
+        return fetchTodoList().catch(function () { return null; });
       })
       .then(function (j) {
         if (j && j.ok) return renderQeval(j);
