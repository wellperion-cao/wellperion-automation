# 하루 양끝(시작·마무리) 카톡↔텔레그램 동기화 (GM 지시 2026-09-05)

GM 원문: "지금 알림 한장이 하루의 시작 / 하루의 마무리 이렇게 셋업이 안되어있네? 텔레그램이랑
카카오톡이랑 일단 동기화가 되어야할 것 같고, 카카오톡이 안되었을 때 텔레그램이 알림까지도
떠야하는데"

## 실측 — 손대기 전 현황
- 하루의 시작 = 카톡 07:40 ★운영+시설+지원+주차(`scripts/send_ops_digest.py`) · 하루의 마무리 =
  카톡 평일 22:30/휴일 20:00 같은 방(`telegram_bot/daily_scheduler.py` KAKAO_GO_STREAM2 블록,
  머리글 이미 「🌙 하루의 마무리 — 점검·접수」로 바뀌어 있었는데 등록부만 옛 이름 `[오늘 하루 정리]`
  로 남아 어긋나 있었다).
- 텔레그램 쪽 = 08:00 오늘의 항로(`win-0800-ceo`) · 23:00 하루 마감(`ceo_evening_wrap.py`,
  예약작업 `Wellperion-CEO-Evening-Wrap-2300-Live`, 실측 Last Result=0)인데 이 23:00 통이
  등록부에 항목 자체가 없었다.
- 카톡 실패 경보(`kakao_report_sender._notify_send_failure`)는 "발신 시도 후 실패"만 잡는다 —
  스크립트가 아예 안 돌거나 발신 전에 죽으면 관문을 못 지나 아무 경보도 없다(2026-09-05 07:30
  종료코드 1 실사고, 발신 자체는 로그상 됨).

## 한 일
1. **등록부**(`status/notify_registry.json`) — `kakao-ops-stream2` what 표기를 코드와 맞춤
   (「🌙 하루의 마무리 — 점검·접수 M/D(요일)」·★부서장 몫 "오늘 접수"로 갱신) · 텔레그램 23:00
   항목(`win-2300-ceo-evening`) 신설 · 4항목(07:40 카톡·08:00 텔레·22:30 카톡·23:00 텔레)에
   `"bookend": "start"|"end"` 공통 태그 부여.
2. **자율현황 「알림 한 장」**(`3. 웰페리온 가이드/자율현황.html`) — 요약줄 바로 아래 「🌅🌙 하루
   양끝」 2×2 소표(카카오톡 현장/텔레그램 GM × 시작/마무리) 신설. bookend 태그 항목만 골라
   기존 `notifySeverity`·`alertLastSentCell` 재사용(새 판정 로직 0) — `renderBookendBoard()`.
3. **동기화 사본** — 카톡 하루의 시작·마무리 본문을 텔레그램 업무보고방(8254867551)에도
   `📋 카톡 사본 · <라벨>` 제목으로 1통씩(`_send_telegram_copy()`, `send_ops_digest.py`·
   `daily_scheduler.py` 양쪽에 동일 헬퍼) — 카톡 발송 성공 여부와 무관하게 시도(best-effort).
   등록부에 `tg-copy-morning-start`·`tg-copy-evening-end` 2항목 추가.
4. **미발신 감시** — `daily_scheduler.py`에 `_bookend_watch()` + apscheduler 잡 3개
   (07:55 / 20:15·22:45(휴일 게이트, `run_daily_digest`와 동일 패턴)) 신설. 오늘자
   `logs/kakao_sent-YYYY-MM-DD.log`에서 해당 방·머리글 마커가 `ok:true`로 없으면 업무보고방에
   경보(예약작업 `schtasks` 마지막 결과 포함) · 하루 1회 dedup(`status/bookend_watch_notified.json`).
   등록부에 `tg-0755-morning-watch`·`tg-2245-evening-watch` 2항목 추가.

## 실측 버그 1건 — schtasks 콘솔 인코딩
`_schtask_last_result()` 최초 구현은 `encoding="utf-8"`로 읽어 "Last Result" 라벨을 못 찾았다
(이 PC 로캘의 schtasks 콘솔 출력은 cp949 — 라벨도 한국어 "마지막 결과"). `encoding="cp949"` +
한국어/영어 라벨 동시 매칭으로 수정, 실측 재확인(`Wellperion-Ops-Morning-Digest-0730` → "1",
`WellperionDailyScheduler` → "0").

## 검증
- `notify_registry.json` — json 파싱 통과(71→68건, 동시에 다른 레인이 v1 죽은잡 3건 삭제·
  나우열M 1건 추가 중이었음을 git log로 확인) · `notify_registry_check.py` 재실행 결과 내 항목
  8개(위 4 원본 + 신규 4개) 전부 UNREGISTERED_JOB/MISSING_IN_REALITY 무해.
- 자율현황 소표 — Node로 `renderBookendBoard()` 원문을 격리해 실제
  `notify_registry.json`+`erp_status.json`으로 실행: 4칸 모두 실제 시각·칩·마지막 발송이
  정확히 그려짐(하루의 시작 🟡 오늘 종료코드1 · 하루의 마무리 🟢 · 08:00/23:00 정상).
- 텔레그램 사본·미발신 경보 — 업무보고방에 `[테스트]` 접두 3통 실발송·수신 확인
  (message_id 확보) · `_bookend_sent_ok()`를 오늘자 실제 로그로 호출해 morning=True(이미
  발신됨) / evening=False(아직 시간 전) 정상 판정 확인.

## 안 한 것 · 남은 것
- daily_scheduler.py 재기동 안 함(브리핑대로 다음 자동 재기동 때 07:55/20:15/22:45 잡 발효).
- 다른 레인이 진행 중인 등록부 드리프트 13건 정리(v1 잡 삭제·나우열M 라우팅 등)는 손대지
  않음 — 남은 4건(MISSING_IN_REALITY 1·UNREGISTERED_JOB 3, gm_checkin 계열)은 그 레인 소관.

DONE — 코드·등록부·화면 3곳 반영 완료, 위 4항목 실측 검증 완료.
