# AWS 이관 — 실무진 안내 체계 (배 11215)

작성 AI 웰리 · 2026-09-03 · 발송 관문 `kakao_report_sender` (초안 → GM 승인 → 발송)

전환 실행은 시토 배 11182·11220 이 맡는다. 이 문서는 **사람 쪽 안내**만 다룬다.
전환 날짜는 시토가 모듈별로 정하는 대로 이 표에 채워 넣는다 — 날짜가 정해지기 전에는 안내를 보내지 않는다.

---

## 1. 모듈별 — 바뀌는 것 / 안 바뀌는 것

| 모듈 | 쓰는 사람 | 바뀌는 것 | 안 바뀌는 것 | 전환 날짜 |
|---|---|---|---|---|
| **종합접수처** (접수·습득물·휴회·VOC) | 리셉션(접수 등록·처리 완료 표시) · 4부서 반장·팀장(본인 부서 접수 확인·완료 표시) · 회원(QR 셀프 접수 · 습득물 갤러리 열람) | 직원용 현황 화면 주소가 erp.wellperion.com 으로 바뀌고 회사 구글 계정으로 한 번 로그인한다(비밀번호 커튼 없어짐) · 접수·휴회 목록을 서버가 5분마다 미리 읽어 두어 첫 화면이 빨라진다(09-03 가동) · 휴회 접수 칸에서 처리 끝난 건이 자동으로 접힌다 | 저장(접수 등록·처리 완료·습득물 등록)은 지금처럼 구글 시트로 바로 간다 · 회원 셀프 접수 QR·폼 주소(wellperion.com)·입력 칸 전부 그대로 · 처리 완료 버튼·처리 메모 그대로 · 카톡·텔레그램 접수 알림 그대로 · 습득물 등록·갤러리·폐기 공지 그대로 · 사진은 지금처럼 구글 드라이브에 저장(서버로 안 옮김) · 구글 시트 원장 그대로 남아 시트에서 봐도 같은 값 | **1호** — 시토 확정 대기 |
| **점검** (지원·시설·운영·주차) | 4부서 조별 담당(키오스크·폰에서 제출) · 반장·팀장(제출 확인) · GM(08:00 통 보고) | 주소와 로그인만 — erp.wellperion.com 에서 열면 회사 구글 로그인 한 번 · 점검 기록 저장은 아직 구글 시트 그대로(서버 이전 순서 미정 · 정해지면 이 칸 갱신) | 점검 화면·항목·조별 배정·제출 버튼·키오스크 QR 전부 그대로 · 08:00 통 점검 보고 그대로 · 시트 원장 그대로 | 미정 |
| **업무·결재 SSOT** | 중간관리자(업무 올림·담당 지정) · 부서장(1단계 결재) · GM(최종 결재) | 주소와 로그인만 — erp.wellperion.com 에서 열면 회사 구글 로그인 한 번 · 저장·결재 알림은 지금처럼 구글 시트·GAS 에서 나감(서버 이전 순서 미정 · 정해지면 이 칸 갱신) | 업무 올리는 법·결재선(부서장 → GM)·부서장 PIN·결재 알림·화면 전부 그대로 · 시트 원장 그대로 | 미정 |
| **회원 관리** (멤버십·강습) | 임정은M(멤버십·강습 등록·상태 변경·LOSS 처리) · 이경연 실장(조회·정정) · 파트너팀(강습 배정 조회) | 들어가는 주소가 erp.wellperion.com 으로 바뀌고 회사 구글 계정으로 한 번 로그인한다 · 회원 명단이 서버에서 바로 열린다(첫 화면 65초 → 3초, 09-03 완료) · 저장은 서버를 거쳐 시트로 그대로 전달되고 누가 언제 저장했는지가 서버에 남는다(09-03 가동 · 실제 저장분 대조 중) · 회원마다 회원번호(M번호)가 자동으로 붙는다 | 등록·상태 변경·LOSS 처리·미등록사유 입력 손 동작 전부 그대로 · 화면·칸·버튼 그대로 · 구글 시트 원장은 그대로 남아 시트에서 직접 봐도 같은 값 · 강습 배정·문의 목록 보는 법 그대로 · 저장 직후 방금 저장한 한 명이 안 보이는 일 없음(저장 뒤 6분은 시트에서 직접 읽는다) | 미정 — 서버 저장 대조 5종(회원 칸 수정·문의 상태·강습 상태·LOSS 복귀·피드백 접수)이 끝나면 시토가 잡는다(시포 배 963) |
| **문의** (홈페이지 문의 폼 → 문의 원장) | 홈페이지 방문자(작성) · 리셉션 · 임정은M(처리) | 문의 원장이 서버 DB로 (읽기는 이미 서버) · 문의 한 건마다 「어느 게시물에서 왔는지」가 함께 저장된다(유입 채널 11종 + 게시물 번호, 지난 문의는 소급 안 함 · '기록없음'으로 채움) | 문의 폼 주소·입력 칸·카톡 알림 · 폼의 「알게 된 경로」 고르는 칸은 그대로 있고 **선택지만 늘어난다**(인스타 개인/공식 구분 등 — 방문자가 보는 유일한 변화, 시모 → 웰리 통보 2026-09-04) | 읽기 완료 · 쓰기 미정 (시토 배 925·960) |
| **콘텐츠 접수** (강사·직원 콘텐츠 접수 · GM의 일요일) | 강사진 · 직원 · GM | 접수 내용·사진이 서버 DB에 저장된다(지금은 구글시트) · 15분마다 걷어 검수큐에 올리는 자동화가 서버를 읽는다 | 접수 화면·QR·입력 칸 · 텔레그램 승인 카드 · 검수 → 발행 순서 | 미정 (시토 배 944·960) |
| **스케줄·휴무 / 매출·지출** | 나우열M | 없음 — 이번 회차 대상 아님 | 전부 그대로 | 해당 없음 |

**전 화면 공통으로 바뀌는 것 두 가지**

1. **주소** — 기존 주소도 당분간 그대로 열린다. 새 주소는 `erp.wellperion.com`.
2. **로그인** — 화면마다 비밀번호를 넣던 것이 없어진다. **회사 구글 계정으로 한 번 로그인하면 그날은 모든 화면이 그냥 열린다.**

---

## 2. 카톡 3통 초안

발송 방 = ★중간관리자 · ★부서장 · 4부서방 (공지 3방 기준).
아래는 **1호(종합접수처)** 문안이다. 다음 모듈은 모듈 이름과 날짜만 바꿔 쓴다.

### ① 전환 하루 전 (D-1)

```
[웰페리온 ERP] 접수 화면이 회사 서버로 옮겨집니다 — MM/DD(요일)

1️⃣ 하시는 일은 그대로입니다
   접수 주소·QR·화면·적는 칸 전부 같습니다.
2️⃣ 바뀌는 것은 로그인 한 가지
   비밀번호 대신 회사 구글 계정으로 한 번 로그인하시면 됩니다.
3️⃣ 옮기는 동안 MM/DD 오전 O시~O시 잠깐 멈춥니다
   그 시간에 들어온 건은 옮긴 뒤 그대로 남아 있습니다.
👉 준비하실 것은 없습니다. 회사 구글 계정 비밀번호만 알고 계시면 됩니다.
```

### ② 전환 당일 (완료 직후)

```
[웰페리온 ERP] 접수 화면 이전이 끝났습니다

1️⃣ 지금부터 새 주소로 열립니다
   erp.wellperion.com — 기존 주소로 들어오셔도 그대로 연결됩니다.
2️⃣ 처음 여실 때 구글 로그인 한 번
   회사 계정(@wellperion.com)으로 들어가시면 그다음부터는 바로 열립니다.
3️⃣ 확인 한 가지만 부탁드립니다
   접수 한 건 넣어 보시고 목록에 뜨는지만 봐 주십시오.
👉 안 되는 게 있으면 이 방에 그대로 적어 주시면 바로 봅니다.
```

### ③ 다음 날 (D+1)

```
[웰페리온 ERP] 어제 옮긴 접수 화면 — 불편하신 것 있으셨습니까

1️⃣ 어제 하루 접수 N건 정상 처리됐습니다
2️⃣ 사진도 그대로 올라가고 있습니다
   저장되는 곳만 회사 서버로 바뀌었습니다.
👉 느리거나 안 열리거나 이상한 게 있으면 이 방에 적어 주십시오. 오늘 중 봅니다.
```

---

## 3. 지키는 것

- **손대는 것은 안 바뀐다고 먼저 말한다.** 바뀌는 건 뒤(서버)뿐이다.
- **연락처는 한 곳** — ★중간관리자 방. "안 되면 예전 링크로" 같은 이중 안내를 하지 않는다.
- **날짜가 정해지기 전에는 보내지 않는다.** 시토가 모듈 전환일을 잡으면 이 문서 표에 먼저 적고, 그다음 D-1 안내가 나간다.
- 카톡 한 통 10줄 안 · 빈 줄 없음 (전달문 규칙 = `wellperion-gm-report` §4-2-2).
- 모듈별 표의 빈 칸은 담당이 채운다 — 운영 4부서 = 시우 · 회원 = 시포 · 문의 = 시모.
