# AWS 이관 — 박제물 폐기 목록 (배 1039)

작성 AI 웰리 · 2026-09-05 · GM 지시 "AWS 이관하면서 굳이 박제 안 해도 되는 것들 정리"

판정 기준선 = `status/briefs/CEO-2026-09-03-AWS이관-실무진안내.md` 표 + 서버 코드 실측
(`server/erp_api/origin_switch.py` · `api_write.py` · `api_intake.py` · `3. 웰페리온 가이드/_assets/erp_write.js`).

---

## 1. 요약 숫자

| 구분 | 건수 |
|---|---|
| 조사한 박제물 | **181** (메모리 120 · GAS 의존 스크립트 41 · GAS 배포 폴더 15 · 규칙문서 4 · GAS 설계서 1) |
| 지금 지운 것 | **4** (메모리 3 = 은퇴 폴더로 이동 · 파일 1 = 삭제) |
| 표 전환 때 지울 것 | **97** (§4 묶음별) |
| 그대로 유지 | **80** |

**한 줄 결론 — AWS 이관만으로 지금 폐기되는 박제물은 0건이다.**
쓰기 경로가 전부 `dual`(서버 원장에 적고 그대로 GAS 로 넘김)이라 GAS·시트 지식은 한 줄도 안 죽었다.
지운 4건은 AWS 와 무관하게 **가리키는 대상이 이미 없어진** 죽은 포인터다.

### 왜 아직 아무것도 안 죽었나 — 실측 근거

- `server/erp_api/origin_switch.py` — 스위치 기본값 `DEFAULT = "dual"`. 파일이 없거나 깨지면 전부 `dual`.
  전환 단위 6영역 = `write_reception` · `write_todo` · `write_check` · `write_schedule` · `write_proc` · `write_member`,
  공개 폼 4종 = `inquiry` · `instructor` · `sunday` · `reception`.
- `origin_switch.NO_SERVER` — `reception` 은 **`server` 로 못 바꾼다**. 접수번호 형식이 `L260904-101010` 과 `RECEPTION-n` 으로 달라 손님이 받아 간 번호를 직원이 시트에서 못 찾는다. 번호 모양이 같아지기 전엔 전환 대상이 아니다.
- `_assets/erp_write.js` — 서버에 못 닿으면 **종전 GAS 직접 경로로 폴백**한다. GitHub Pages 에서는 `ERP_ON` 이 false 라 종전 그대로다.
- `server/erp_api/gas_key.py` — 서버가 GAS 를 호출할 때 붙일 열쇠를 관리한다. 즉 **서버가 GAS 의 고객**이지 대체재가 아니다.

이 세 가지가 살아 있는 한 GAS 배포 폴더 15개도 GAS 함정 메모리도 전부 현역이다.

---

## 2. 인벤토리

### 2-1. GAS 배포 폴더 15개

| 항목 | 종류 | 의존 | 유효? | 폐기 시점 | 조치 |
|---|---|---|---|---|---|
| `.deploy-reception` (`1_jF5yhXZfBg…`) | GAS 사본 | 종합접수처 시트 | 유효 · 쓰기 원장 | `write_reception` + `reception` 전환 — **접수번호 형식 통일이 선행** | 유지 |
| `.deploy-intake` (`1zmXkpox9nuV…`) | GAS 사본 | 문의 시트 | 유효 · 쓰기 원장 | `inquiry` 전환 (시토 배 925·960) | 유지 |
| `.deploy-todo` (`1VUMgK-vJvxC…`) | GAS 사본 | 업무·결재 시트 | 유효 · 쓰기 원장 | `write_todo` 전환 | 유지 |
| `.deploy-check` (`1FLQAzjq6IME…`) | GAS 사본 | 점검 시트 | 유효 · 쓰기 원장 | `write_check` 전환 | 유지 |
| `.deploy-schedule` (`1oKqTMHVtoe-…`) | GAS 사본 | 전사일정 시트 | 유효 · 쓰기 원장 | `write_schedule` 전환 | 유지 |
| `.deploy-procurement` (`1pCMppQK15Ej…`) | GAS 사본 | 지출·구매 시트 | 유효 · 쓰기 원장 | `write_proc` 전환 | 유지 |
| `.deploy-funnel-v2` (`1BezMSW_rGi5…`) | GAS 사본 | 회원·퍼널 시트 | 유효 · 라이브 정본 | `write_member` 전환 | 유지 |
| `.deploy-funnel` (`1A77oDRaa21K…`) | GAS 사본 | 회원·퍼널 시트 | **은퇴본** — 200버전 상한으로 2026-07-18 v2 승계 | 이미 은퇴 | **보류** — v2 와 나란히 둬야 diff 가 된다. §4 회원 묶음에서 함께 |
| `.deploy-instructor` (`1Q5RiwzqX3Ro…`) | GAS 사본 | 강사 접수 시트 | 유효 · 쓰기 원장 | `instructor` + `sunday` 전환 | 유지 |
| `.deploy-forms` (`1o8EfB_jMeX0…`) | GAS 사본 | 폼 안내·영문탭 | 유효 | 폼 서버화 뒤 | 유지 |
| `.deploy-sales` (`1Zx0PKWNjrvv…`) | GAS 사본 | 매출 시트 | 유효 | **해당 없음** — 매출·지출은 이번 회차 대상 아님 | 유지 |
| `.deploy-salesops` (`1u6A7aeedJh1…`) | GAS 사본 | 매출운영 시트 | 유효 | 해당 없음 | 유지 |
| `.deploy-salesreport` (`139S_3Q6Z-DL…`) | GAS 사본 | 매출보고 · GM 개인계정 | 유효 · 09:30 카톡 보고 | 해당 없음 | 유지 |
| `.deploy-welly-voice` | GAS 사본 | 없음 — `.clasp.json` 부재 | **배포된 적 없음** | 미정 | **보류** — GM 음성 응답기 미배포본. 폐기인지 착수 전인지 확인 필요 |
| `.deploy-salesops-probe` | 잔해 | 없음 | GAS 아님 | 즉시 | **보류** — git 미추적 잔해(`tmp/hud_role_cache.json` 하나뿐). 커밋 대상이 아니라 이번에 안 건드림 |

### 2-2. GAS 의존 스크립트 41개

라이브 예약작업 36개 중 GAS 를 타는 것은 아래 12개뿐이고 전부 **읽기 집계·보고**다. 쓰기 스크립트는 없다.

| 묶음 | 스크립트 | 만지는 표 | 서버 대체 | 폐기 시점 | 조치 |
|---|---|---|---|---|---|
| 예약 라이브 12 | `cpo_inquiry_snapshot` · `ops_daily_digest` · `send_ops_digest` · `ops_digest_sheet_push` · `monthly_check_report` · `monthly_ops_report` · `monthly_ops_sync` · `monthly_marketing_report` · `weekly_marketing_feedback` · `kakao_report_sender` · `telegram_health_check` · `generate_sales_report_image` | 문의·점검·업무·매출 | 일부만 — `/api/inquiries` · `/api/members` | 해당 영역 전환 뒤 **읽기 소스만** 서버로 교체 | 유지 |
| 상시 유틸 13 | `kpi_collector` · `integration_health` · `erp_status_publisher` · `hangro_board` · `gm_aide_scan` · `coo_registry` · `cpo_report` · `support_check_summary` · `report_stream_2_check` · `page_score_extract` · `qa_inject_inquiry` · `verify_reception_flow` · `safe_commit` | 전 영역 | 부분 | 영역별 전환 뒤 | 유지 |
| GAS 운영 도구 3 | `gas_deploy_guard` · `gas_version_cleanup` · `gas_version_monitor` | GAS 자체 | 없음 | **GAS 0 도달 시** | 유지 — 맨 마지막에 지울 것 |
| 1회성 13 | `cpo_end_date_align_20260812` · `cpo_loss_restore_20260808` · `cpo_lesson_coach_fix_FB260825` · `cpo_member_reg_update_FB260825_170934` · `cpo_fb260902_feedback_update` · `cpo_jungsuk_inquiry_check` · `cpo_member_registry_migrate` · `cpo_member_domain_survey` · `apply_member_cal_url` · `_cto_member_wait_auto_release_dryrun` · `_intake_api_bench` · `verify_member_dup_delete` · `verify_member_dedup_guard` | 회원·문의 | — | 이미 소임 종료 | **보류** — 예약 미등록·참조 0 이지만 **회원 서버 저장 대조 5종(시포 배 963)이 진행 중**이라 되돌림 도구로 쓸 수 있다. 대조가 끝난 뒤 일괄 |

### 2-3. 메모리 120건

| 묶음 | 건수 | 의존 | 유효? | 폐기 시점 | 조치 |
|---|---|---|---|---|---|
| GAS 함정·조작법 — `reference_appsscript_korean_write` · `reference_clasp_webapp_redeploy` · `reference_gas_post_curl_no_follow_redirect` · `reference_gas_funnel_version_limit_200` · `reference_gas_projects_span_two_google_accounts` · `reference_drive_move_delete_via_gas` 등 | 21 | GAS | **전부 유효** — 서버가 GAS 를 호출하므로 함정이 그대로 산다 | GAS 0 도달 시 | 유지 |
| 시트 원장 구조·칸 함정 — `reference_inquiry_sheet_write_read_column_mismatch` · `reference_row_index_delete_race_deleted_real_customers` · `reference_check_ledger_sheet_sot_9kb_ceiling` · `reference_lesson_main_importrange_source_no_rowdelete` 등 | 34 | 시트 | 유효 — 시트가 여전히 원장 | 해당 표의 `origin_switch` 가 `server` 가 된 뒤 | 유지 |
| PC 예약작업·윈도우 — `reference_powershell_scheduledtasks_limitation` · `reference_bot_elevated_restart` · `reference_pc_auto_shutdown_2330` · `reference_danggn_profile_lock_blocks_scheduled_tasks` 등 | 26 | 이 PC | 유효 — 예약작업 36개가 이 PC 에서 돈다 | 예약작업을 서버 cron 으로 옮길 때 | 유지 |
| 워드프레스 — `reference_wordpress_site_structure` · `reference_wp_page_inject_draftpage_not_publishpage` · `reference_wp_published_page_edit_goes_live_immediately` · `reference_floating_inquiry_button` 등 | 12 | WP | 유효 — 홈·문의 폼은 WP 그대로 | **해당 없음** — 이번 회차 대상 아님 | 유지 |
| 조직·사람·브랜드·보고형식 | 24 | 없음 | 유효 | 해당 없음 | 유지 |
| 죽은 포인터 | 3 | — | 무효 | 즉시 | **§3 에서 은퇴 처리** |

### 2-4. 규칙 문서 4종 — GAS·시트 전제 조항

| 문서 | GAS·시트 전제 | 유효? | 조치 |
|---|---|---|---|
| `ssot/약속.json` — lessons 26개 전수 | **0건** | — | 손댈 것 없음 |
| `ssot/CONSTITUTION.md` | **0건** | — | 손댈 것 없음 |
| `.claude/skills/wellperion-boot/SKILL.md` | 3건 — L9 매출보고 시트 실물 · L11 `todo_list` 생성자 훑기 · L144 `queue_dispatch` 코드정본 | 전부 유효 | 유지 |
| `CLAUDE.md` §3-1 | 1건 — "업무현황 SSOT(S3 · GAS `todo_list`)" | 유효 — `write_todo` 가 아직 `dual` | `write_todo` 전환 시 **문구 교체** (§4) |

### 2-5. GAS 설계서

`docs/superpowers/specs/` 안에서 GAS 를 주제로 삼은 설계서는 `2026-09-03-gas-to-server-migration-plan.md` **1건**이며 이번 이관의 실행 계획서다. 유지.

---

## 3. 지금 지운 것 4건

**메모리 3건은 삭제가 아니라 이동이다.**
이동처 = `C:\Users\jjky0\.claude\projects\C--Users-jjky0-welperion-automation\memory\_retired_2026-09-05\`
되돌리기 = 그 폴더에서 상위 `memory\` 로 파일을 다시 옮기면 끝이다. 폴더에 `_왜_옮겼나.md` 를 같이 넣었다.
회수 훅 `scripts/memory_recall.py` 는 `memory/*.md` 만 읽으므로 하위 폴더는 프롬프트에 안 올라온다.
**MEMORY.md 색인은 손대지 않았다** — 오늘 주제 지도로 개편되면서 이 3건이 이미 이름으로 안 적혀 있다.

| 파일 | 왜 지웠나 — 3조건 |
|---|---|
| `reference_northstar_reissues_completed_measurement.md` | ① 대상 `scripts/northstar_recommender.py` 가 GM 지시로 삭제됐다. 근거 = `scripts/voyage_module_map.py:8` "구 소비자 scripts/northstar_recommender.py 는 06:30 추천 카드 기능 폐기로 삭제됨 — 2026-07-27 GM" ② 남은 참조는 주석·독스트링뿐이다(`gm_aide_scan.py:30` · `model_router.py:67` · `schedule_generator.py:16` 스키마 예시) ③ 사람 원본도 사고 원장도 아니다 |
| `project_hangro_review_destination_brain.md` | ① 적어 둔 산출물 5종이 전부 없다 — `scripts/hangro_review.py` · `scripts/hangro_review.bat` · `status/hangro_decisions.jsonl` · 계획서 · `status/module_registry.json` 의 `ceo-hangro-review` 항목 ② 참조 0 ③ 사고 원장 아님. 「휴면 중」으로 읽히지만 실제로는 삭제된 모듈이라 오히려 오해를 만든다 |
| `project_ceo_verify_watcher.md` | ① 실행체가 저장소에 한 번도 없었다. 근거 = `scripts/fix_task_windows.ps1:40` "CEO-Verify-Watcher: orphan (target script ceo_verify_watcher.py never in git". 예약작업 36개 목록에도 없다 ② 참조 0 ③ 남길 가치가 있던 git 커밋 교훈은 `feedback_git_commit_atomic_vs_watcher_reset.md` 에 그대로 있다 |

**파일 1건은 git 삭제다.**

| 파일 | 왜 지웠나 |
|---|---|
| `launchers/hangro_review_hidden.vbs` | 부르는 대상 `scripts/hangro_review.bat` 이 없어 실행하면 그냥 실패한다. 이 vbs 를 쓰는 예약작업도 없고 참조도 0 이다 |

되돌리기(파일) = `git revert <아래 커밋 해시>` 또는 `git checkout <해시>^ -- launchers/hangro_review_hidden.vbs`.

---

## 4. 표 전환 때 지울 묶음 — 97건

각 묶음은 **`/srv/erp/status/origin_switch.json` 의 해당 열쇠가 `server` 로 바뀌고 3일 대조가 무결일 때** 연다.
전환 전에 지우면 폴백 경로(`erp_write.js` · `api_write`)가 GAS 로 되돌아갈 때 아무 지식도 안 남는다.

| 묶음 | 전환 열쇠 | 지울 것 | 건수 |
|---|---|---|---|
| **종합접수처** | `write_reception` + `reception` · **접수번호 형식 통일 선행** | `.deploy-reception` · 접수 시트 구조 메모리 8 · `verify_reception_flow` · `gas_key.py` 의 접수 열쇠 분기 | 11 |
| **문의** | `inquiry` | `.deploy-intake` · 문의 시트 칸 함정 메모리 9 · `qa_inject_inquiry` · `cpo_inquiry_snapshot` 의 GAS 읽기 경로 | 12 |
| **업무·결재 SSOT** | `write_todo` | `.deploy-todo` · 업무·결재 GAS 메모리 7 · `CLAUDE.md` §3-1 문구 교체 · `project_todo_api_open_security_gap` — 무인증 개방 과제가 서버 로그인으로 소멸 | 10 |
| **점검** | `write_check` | `.deploy-check` · 점검 GAS 집계 함정 메모리 11 — 9KB 천장·한글칸 변환·완료율 클램프 포함 | 12 |
| **전사일정** | `write_schedule` | `.deploy-schedule` · `reference_schedule_ssot_gas_is_source_json_is_seed` 외 2 | 4 |
| **지출·구매** | `write_proc` | `.deploy-procurement` · 관련 메모리 2 | 3 |
| **회원·강습** | `write_member` | `.deploy-funnel-v2` · `.deploy-funnel` 은퇴본 · 회원·강습 시트 메모리 18 · 1회성 스크립트 13 | 33 |
| **강사·콘텐츠 접수** | `instructor` + `sunday` | `.deploy-instructor` · 관련 메모리 3 | 4 |
| **GAS 자체 은퇴** | 위 전부 `server` + GAS 프로젝트 삭제 | `.deploy-forms` · GAS 조작법 메모리 5 · `gas_deploy_guard` · `gas_version_cleanup` · `gas_version_monitor` | 9 |

**해당 없음 — 이번 회차 밖**
매출·지출·인사·스케줄 라인은 나우열M 소관이라 이 표에 넣지 않는다(`.deploy-sales` · `.deploy-salesops` · `.deploy-salesreport` + 관련 메모리).
워드프레스 메모리 12건도 홈·문의 폼이 WP 에 남으므로 대상이 아니다.

---

## 5. 이관과 별개로 걸린 것

- `scripts/__pycache__/` 에 삭제된 `hangro_review` · `northstar_recommender` 의 `.pyc` 가 남아 있다. `.gitignore` 대상이라 이번에 안 건드렸다.
- `project_daily_northstar_recommender.md` 는 모듈이 삭제됐는데도 **유지**했다. GM 의 「브릿지 = 화면이 아니라 과정」 정의와 최상급 4박자가 그 안에만 있고 살아 있는 메모리 4건이 이 개념을 인용한다. 본문에서 삭제된 스크립트 문단만 걷어내는 축소가 맞다. 별건으로 둔다.
