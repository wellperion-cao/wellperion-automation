# 배990·980 — 서버 4건 배포 + erp 실측 (시토 2026-09-05)

## 서버 4건 — 전부 배포 완료

### ① `/api/todo` GM 행 게이트 + `{ok,data}` 봉투
- 파일: `server/erp_api/api_todo.py`
- GAS `.deploy-todo/업무&결재 현황.js` `_gmKeyOk_`(2356행)·todo_list 필터(2427행) 실측 결과, GM 행 판정 칸은 **담당자(owner)** — 생성자 아님. 초안(creator 기준)을 정정해 owner 기준·부분일치(`owner NOT LIKE '%김남욱GM%'`)로 구현.
- `include_gm=1&gmkey=<GM_TODO_KEY>` 일 때만 포함(env `GM_TODO_KEY`, 서버에 이미 존재 확인). 응답에 `ok:true, data:[...]` 추가(`rows` 기존대로 유지 — GM업무·_owner_directive.js 가 이미 읽는 칸).
- 실측(배포 서버):
  | 호출 | total |
  |---|---|
  | 기본(include_gm 없음) | 178 |
  | include_gm=1&gmkey=틀린값 | 178 (게이트 안 열림 확인) |
  | include_gm=1&gmkey=실제값 | 200 (22건 차이 = GM 담당 행) |
- selftest: `python3 api_todo.py --selftest` — `_gm_ok` 4가지 케이스(정상/틀린키/게이트없음/빈키) + 기존 work_stats 통과.

### ② `sync_board.py` BOARD_KEYS 3개 + vendor_list 미러
- `BOARD_KEYS` 에 `ALLDEPT_VENDOR_CATEGORIES`(전사_거래업체) · `GM_TASK_CHECKS`(GM업무 체크칸) · `CHAIRMAN_ITEM_CHECKS`(회장님 지시사항 체크칸) 추가.
- `vendor_list`(GAS action=vendor_list)는 board 가 아니라 **같은 CHECK_GAS_URL 의 별도 액션**(초안의 `OPS_BOARD_GAS_URL` 신규 env 는 불필요 — 이미 있는 `CHECK_GAS_URL` 재사용, GAS ID 실측 AKfycbyXw4Za 동일 확인) → `sync_misc.py` `GAS_ENV["ops"]="CHECK_GAS_URL"` · `ACTIONS["ops"]=("vendor_list",)`.
- 배포 직후 서버에서 1회 수동 동기화: `sync_board.py` 10/10건 갱신, `sync_misc.py` 3/3건 갱신(renewal·ops·schedule) — cron(5분)도 그대로 유지.
- 실측: `/api/board/ALLDEPT_VENDOR_CATEGORIES`·`/api/board/GM_TASK_CHECKS`·`/api/board/CHAIRMAN_ITEM_CHECKS`·`/api/misc/ops/vendor_list` 전부 200.

### ③ 전사일정 읽기 거울
- `sync_misc.py` `GAS_ENV["schedule"]="SCHEDULE_GAS_URL"`(기존 env 재사용) · `ACTIONS["schedule"]=("load_schedule",)`.
- `vendor_list`·`load_schedule` 모두 **순수 JSON**(`{ok:true,...}`, callback 없음 — `.deploy-schedule/전사_일정.js`·`.deploy-check/지원팀 일일점검.js` 실측 확인) — renewal 전용 JSONP 경로와 갈라 `JSONP_GAS={"renewal"}` 플래그로 분기(파일 하나 유지). `gas_call()` 하나가 두 모드를 다 처리.
- 화면은 `/api/misc/schedule/load_schedule` 로 읽는다. 정본은 여전히 GAS(전사일정 SSOT 규칙 그대로) — 이건 읽기 거울만.
- `deploy_misc_remote.sh` 필수 env 점검 목록에 `CHECK_GAS_URL`·`SCHEDULE_GAS_URL` 추가(둘 다 서버에 이미 존재 확인) + curl 점검 3줄 추가.

### ④ erp 도메인 실측
관리자 계정(서버 `/srv/erp/auth.env` ERP_ADMIN_EMAIL, 값은 로그인에만 쓰고 화면·기록에 남기지 않음)으로 로그인 후 agent-browser 로 3장 + 980 요청 1장 확인.

| 화면 | 콘솔 오류 | /api 요청 | 값 대조 |
|---|---|---|---|
| GM업무 | 0 | `/api/board/GM_TASK_OWNERS·GM_TASK_CHECKS·CHAIRMAN_REPORTED`·`/api/todo`·`/api/misc/schedule/load_schedule` 전부 200 | 오늘 일정 3건·리더 현황 3명(이경연 9/4·이정헌 0/0·나우열 15/1) — **erp·GitHub Pages 같은 시각 완전 일치**. 경영보고건(대기26/완료19 vs 27/21)·대표님(13/19)은 이 화면 자체가 `/api/todo` 를 안 쓰고 `LEADER_TODO_API_URL`(TODO GAS) 을 매번 직접 불러 세는 구조라 — 코드 확인(1301행) — 내 미러와 무관, 두 도메인 다 직접조회라 몇 분 사이 실제 시트 변경분만큼 차이(정상, 미러 범위 문제 아님) |
| 회장님 지시사항 | 0 | `/api/board/CHAIRMAN_ITEM_CHECKS·CHAIRMAN_REPORTED` 200 | 24건(대기1·완료23) — **기준값과 일치** |
| 브로제이 업무분장 | 0 | `/api/board/BROJ_TASK_BOARD` 200 | 부서별 배지 CHRO+CFO 1/6·COO 0/29·CPO 0/1·이경연실장 0/7·리셉션관리 0/9 — **erp·Pages 완전 일치**(체크박스 52개=배지 합계와 일치) |
| (980) 종합접수처_현황 | 0 | `/api/reception/board·lost·hold·scoreboard` 전부 200 | 콘솔 오류 0·404/5xx 요청 0건 — 링크 깨짐 없음 |

특히 GM업무의 "경영보고건" 숫자 차이는 사전에 우려했던 `/api/todo` approvals 누락이 **아니다** — 그 배지 자체가 애초에 내 미러를 거치지 않는 별도 직결 경로였다(코드로 확인, 추측 아님).

## 980 — 서버 API 이전 순서 확정

시우 제안(배936 회신) 그대로 채택. GAS 호출 개수·부서 실무진 사용 빈도 기준.

| 순서 | 화면 | 비고 |
|---|---|---|
| 1 | 지원부 체계 | 이미 배선 완료(배922) — 시우 몫 |
| 2 | 시설부 체계 | 이미 배선 완료(배922) |
| 3 | 종합접수처_현황(쓰기 잔여 13액션) | 읽기는 이미 서버 우선, 쓰기만 남음 |
| 4 | 업무·결재 현황 SSOT | 오늘 ①로 서버 쪽 막힘 해소 — 다음 배선 대상 |
| 5 | 주차관리부 체계 | |
| 6 | 전사_일정 | 오늘 ③로 읽기 거울 마련 — 정본은 GAS 그대로 |

첫 화면(지원부 체계) 배선 자체는 시우 몫 — 시토는 서버 쪽만 담당.

## 커밋
- 서버 3건: `server/erp_api/api_todo.py`·`server/erp_api/sync_board.py`·`server/erp_api/sync_misc.py`·`server/deploy_misc_remote.sh`
- 이 문서

DONE
