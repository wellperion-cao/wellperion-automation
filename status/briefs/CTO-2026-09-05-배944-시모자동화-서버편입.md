# 시모(CMO) 자동화 7+1개 AWS 서버 이관 — 편입 계획 (배944)

작성 AI 시토 · 명세 정본 = `status/briefs/CMO-AWS이관-명세-20260903.md` · 서버 = AWS 15.164.151.105 (`/srv/erp/repo`, 배포방식=`server/deploy_auth.sh` 재사용)

## 1차 — 순수 파이썬·GAS·Graph API (2026-09-05 서버 등록 완료·병행 2주)

| # | 잡 | 서버 cron(KST) | 실행 | 병행 상태 |
|---|---|---|---|---|
| 1 | 접수→검수큐 | 15분 | `scripts/cmo_intake_to_review.py --dry-run --once` | dry-run(발신 안 함) |
| 2 | IG 도달 수집 | 09:45 | `scripts/ig_reach_collector.py --dry-run` | dry-run(저장 안 함) |
| 3 | IG 토큰 갱신 | 월 06:10 | `scripts/ig_token_refresh.py --dry-run` | dry-run(네트워크 호출 없음) |
| 4 | 일일 마케팅 카드 | 21:00 | `scripts/weekly_marketing_feedback.py --mode daily --dry-run` | dry-run(발신 안 함·브리프는 파일로 남음) |
| 5 | 주간 브리프 | 월 09:00 | `scripts/weekly_marketing_feedback.py --dry-run` | dry-run(발신 안 함·브리프는 파일로 남음) |
| 6 | 월간 보고서 | 매월 1일 09:00 | `scripts/monthly_marketing_report.py --dry-run` | dry-run(신규 플래그 추가 — 배944, 원래 무플래그·즉시 실발신) |

GM PC 예약작업(작업 스케줄러)은 그대로 유지 — 실발신은 계속 PC 담당, 서버는 로그만 쌓는다. 3일 연속 산출물 확인 후 시모가 PC 값과 대조 → 서버 원본 전환 여부 판단.

## 2차 — 시리즈 슬라이드 (07:30 · PIL·한글 폰트) — 미착수
서버에 브랜드 한글 폰트 미설치 확인 필요. 1차 병행검증 끝난 뒤 착수.

## 3차 — 발행 워처 (telegram bot 내부 15분 루프) — 미착수
네이버·카페·당근 Playwright 로그인 프로필 이관이 최대 위험. GM PC 병행 2주 원칙 그대로 적용 예정. 1·2차 완료 후 착수.

## 4차 — 힉스필드 CLI (배939) — 보류
연동 모듈이 정본 규격으로 굳은 뒤 추가(명세 원문 그대로).

## 의존성 등록 결과
- Python: 서버 시스템 `python3.9`(3.14 아님) — 6개 스크립트 전부 `--dry-run` 1회 실행 성공 확인. `ig_reach_collector.py`는 `dict | None` 함수 어노테이션이 3.9에서 즉시평가 오류 → `from __future__ import annotations` 1줄 추가로 해결(배944, 커밋 포함).
- Pillow: `pip3 install --user Pillow` 완료(11.3.0). requests: 시스템 기본 설치(2.25.1) 재사용.
- 저장소: `/srv/erp/repo` (기존 배852 체크아웃, sparse-checkout을 `scripts`·`wellperion-agents`·`status`·`logs`·`ssot`·`telegram_bot`·`ops`·`ig` 추가해 확장 — 기존 `3. 웰페리온 가이드`는 유지, 삭제 없음).
- 비밀값: `scripts/.env`(IG 토큰)·`telegram_bot/.env`(봇 토큰·INTAKE_READ_TOKEN)를 scp로 복사(내용 미노출) · 권한 600.
- 시각: 서버는 UTC — crontab에 `CRON_TZ=Asia/Seoul` 선언으로 KST 고정(cronie 1.5.7 지원 확인).
- 이중 발신 방지: 6개 전부 `--dry-run`으로 등록 — 텔레그램 발송은 전부 생략. 4·5번(`weekly_marketing_feedback.py`)은 `--dry-run`이어도 브리프 파일(`status/briefs/CMO-*-feedback-*.md`)은 그대로 생성하는 구조라, 서버 cron 줄 끝에 `git checkout -- status/briefs/ · git clean -fdq status/briefs/`을 붙여 실행 직후 되돌린다 — PC가 같은 파일명으로 커밋·푸시할 때 서버의 5분 주기 `git pull --ff-only`와 충돌하지 않게 막는 조치.
- **BLOCKED 없음** — 1차 6개 전부 등록·1회 실행 성공.
