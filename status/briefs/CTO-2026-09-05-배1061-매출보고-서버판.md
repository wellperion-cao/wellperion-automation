# 매출보고서 서버 판 1차 — 업무보고방 병행 발송 (배1061 · 시토 · 2026-09-05)

원칙 = 기존 경로(시트 → GAS `매출보고_자동발송.js` → 09:00 텔레그램 · 09:30 카톡 3방 ·
`scripts/generate_sales_report_image.py`) 무접촉·계속 정본. 서버 판은 **업무보고방(8254867551)
한 곳에만** 09:20 KST 병행 발송한다.

## 만든 것
| # | 파일 | 역할 |
|---|---|---|
| 1 | `server/erp_api/sales_report_render.py` | 시트 미러(`deptrep/dump` · sync_sales.py 가 5분마다 갱신) + 회원현황 5칸 서버 독립계산(`members_report.py`) → 22칸 대조 → Pillow(Pretendard 폰트, 서버에 이미 있는 자산 재사용) 표 PNG 렌더 |
| 2 | `scripts/sales_report_server_send.py` | 킬스위치 확인 → 렌더 호출 → 업무보고방 sendPhoto(`scripts/tg_outbound_log.py` 기존 발신 관문 재사용 · 페이싱·재시도·로깅 자동 편입) |
| 3 | `status/sales_report_server_switch.json` | `{"mode": "parallel"}` 기본값 — parallel 외에는 발송 안 함(3방 전환 자체는 아직 구현 안 함) |

배포: `server/erp_api/`는 서버 repo 클론의 sparse-checkout 밖이라 실행 시 `/srv/erp/api`(기존
`api_sales.py` 등과 같은 배포 자리)를 우선 찾도록 폴백 처리. cron 스크립트는 `scripts/`가
sparse-checkout에 포함돼 있어 `git pull`만으로 갱신된다.

## 22칸 정의
정본 = `status/briefs/CPO-2026-09-05-매출보고서-22칸-정의.md`(시포). 22칸 = 총매출·회원권·옵션·
팀별강습 8줄(11칸, 금일값) + 회원현황 5칸(N2~N6) + 신규/재등록/환불/로스자 6칸 = 22. 회원현황
5칸만 서버 독립계산(`member_active_summary`·`cpo_today_stats`)이라 실제 대조 대상이고, 나머지
17칸은 서버 판도 시트 미러 값을 그대로 쓰므로 1단계에서는 항상 일치 — 브로제이 결제 데이터가
들어오면 그 칸들도 하나씩 독립계산으로 바뀐다.

계산식(서버):
- 총 회원수 = 유효회원(`validTotal`) + 법인(`memberCorp`)
- 정회원 수 = 유효회원 − 중단기 − 대기
- 중단기 / 법인 / 대기 = `typeCounts['중단기']` / `memberCorp` / `waitingCount`

## 실측 (2026-09-05 16:47 KST · 서버에서 수동 1회 실행)
- 발송 성공 · 업무보고방 message_id = **10071**
- PNG 샘플: `qa_screenshots/sales_report_server_sample.png`(로컬 · gitignore 대상이라 git 미포함)
- 22칸 대조: **20/22 일치**

| 칸 | 시트(9/4 확정본) | 서버(9/5 16:47 실시간 스냅샷) | 판정 |
|---|---|---|---|
| 총 회원수 | 1,027 | 1,026 | 불일치 |
| 정회원 수 | 988 | 988 | 일치 |
| 중단기 회원 수 | 3 | 2 | 불일치 |
| 법인 회원 수 | 31 | 31 | 일치 |
| 대기 회원 수 | 5 | 5 | 일치 |

불일치 2건은 시포 검산 표가 미리 짚은 **날짜 정렬 차이** 그대로다 — 보고서는 "어제(9/4) 마감"
기준, 서버 계산은 "지금(9/5 16:47) 스냅샷" 기준이라 그 사이 변동분(법인 1건 종료·중단기 회원
1명 종료 — 시포 문서의 "박원정 종료" 사례)만큼 어긋난다. 실제 로직 오류가 아니라 기준 시각 차이.
매일 09:20 발송분은 자정 직후~09:20 사이 변동만 남으므로 지금보다 좁혀질 것으로 본다.

## cron
서버(ec2-user) crontab에 `CRON_TZ=Asia/Seoul` 아래 등록:
```
20 9 * * * cd /srv/erp/repo && python3 scripts/sales_report_server_send.py >> logs/sales_report_server_send.log 2>&1
```

## 다음
- 3일 연속 22/22 일치를 세기 시작(오늘은 1차 실측이라 카운트 제외 — 첫 정식 카운트는 9/6 09:20부터).
- 22칸 정의·판정 기준은 시포 소유(정의서 갱신 시 이 렌더러도 같이 맞춘다).
- 3일 무결 + GM 확인 뒤에만 킬스위치 `mode`를 `live`로 바꾸는 실제 3방 전환 로직을 구현한다(지금은 값만 읽고 parallel 외엔 발송하지 않는 안전장치만 있음).
