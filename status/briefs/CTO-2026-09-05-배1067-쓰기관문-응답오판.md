# 배1067 — 서버 쓰기 관문 점검저장 응답 오판(success→gas-error) 수리·행 복구·대조 재실행

## 원인 (한 문장)
`server/erp_api/api_write.py:224`가 GAS 응답의 `ok` 칸만 보고 성공을 판정해, `ok` 칸 없이 `{success:true}`만 돌려주는 점검 체계 저장(action=save) 응답을 매번 `gas-error`로 오판했다 — 같은 응답을 `pushback.py`의 `judge`는 `data.get('ok', True)`로 성공으로 쳐, 두 관문이 같은 응답을 서로 다르게 읽었다.

## 조치
1. **판정 통일** — `api_write.py`에 공용 함수 `resp_ok(data)` 신설(ok 칸 우선 → 없으면 success 칸 → 둘 다 없으면 성공, 명시적 false만 실패). `pushback.py`는 이 함수를 그대로 import(중복 정의 금지). 두 파일 모두 자체점검(`python3 api_write.py`, `python3 pushback.py --selftest`) 통과 확인 후 서버(`/srv/erp/api`)에 배포, `erp-api.service` 재기동 확인(정상 기동 로그 확인).
2. **행 복구** — 서버 DB에서 대상 53행을 `write_log_bak_20260905` 테이블로 백업 후, `gas_status='gas-error' AND gas_response->>'success'='true'` 조건에 맞는 53행을 `gas_status='ok'`로 UPDATE. 복구 후 같은 조건 재조회 결과 잔여 0건.
3. **대조 재실행** — `reconcile_dual_write.py`를 서버에서 즉시 재실행. write 영역 mismatch:
   - 2026-09-03: 1건 (개발 프로브 `__probe_no_such_action__` — 존재하지 않는 액션 테스트 호출)
   - 2026-09-04: 10건 (update unauthorized 2 · ohnutti_team_list 접속코드 오류 2 · staff_feedback_submit bad-token 1 · reg_delete/lf_delete 존재하지 않는 ID 삭제 시도 2 · save 시트범위초과 1 · save_schedule 급감가드 1 · saveBoard 알 수 없는 action 1)
   - 2026-09-05: 4건 (save 시트범위초과 3 · save forward-failed 1)
   전부 실제 실패(개발/테스트 호출·권한 오류·존재하지 않는 대상·서버 쪽 조건)만 남았고, 이번에 고친 응답 오판으로 인한 가짜 mismatch는 0건.

## 시트범위초과("범위 좌표가 시트 크기를 벗어납니다") 원인 — 한 줄
`3. 웰페리온 가이드/coo/check/apps_script_v3.js`의 `_handleSaveV2Compat`(실제 운영 트래픽이 타는 경로 · 약 456~457행, `handleSave`의 zone 지정 경로도 약 358~359행에 동일 패턴)가 그날 아직 없던 새 `itemId`를 `sheet.getRange(sheet.getLastRow()+1, ...).setValues()`로 이어붙이는데, 시트의 실제 최대 행수(getMaxRows)가 그 자리보다 적어 벗어난다 — 행을 미리 늘리는 시트 행 추가가 정답으로 보이나, GAS 쪽 수리는 이번 배 범위 밖이라 후속 배로 남긴다.

## 검증
- 로컬·서버 양쪽에서 `api_write.py`/`pushback.py` 자체점검 통과(신설 케이스 포함: `{success:true}`만 있는 응답 → ok 판정).
- 관문 실측: 오늘 실제 저장된 지원_여성구역 op1 행(2026-09-05 · 이연희 · 오전조)을 동일 값 그대로 재제출(내용 변경 없음, `X-Erp-User: TEST-배1067-검증@internal`) — GAS 가 `{"success":true,"saved":1}`(ok 칸 없음)로 응답, `write_log` id 110 이 `gas_status='ok'`로 기록됨을 확인 후 그 테스트 행만 DB 에서 삭제(시트 쪽은 내용 변경이 없어 별도 정리 불필요).
- `write_log_bak_20260905`에 원본 53행 그대로 보존(롤백 가능).

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01FhpdS81eJykyiHCDp4xe4Q
