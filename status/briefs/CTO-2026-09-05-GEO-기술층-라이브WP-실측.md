# GEO 기술층 — 라이브 wellperion.com(WP) 실측 (배1000 · 시토 · 2026-09-05)

설계 정본 = `status/briefs/CEO-2026-09-05-GEO-브랜딩-셋업-설계.md` §2 1층. 용어는 §5 표(정원제 스포츠클럽·강습) 그대로 반영.

## 결과

| 항목 | 했나 | 라이브 확인 | 막힌 이유 |
|---|---|---|---|
| ① JSON-LD 3종(Organization·SportsActivityLocation·FAQPage) 헤더 삽입 | 완료 | `curl http://wellperion.com/` 본문에 `SportsActivityLocation`·`FAQPage`·"정원제 스포츠클럽" 문자열 확인(5건 매치) | — |
| ② llms.txt 루트 업로드 | 못함 | `curl http://wellperion.com/llms.txt` → 404 | wp-admin 안에서 접근 가능한 파일관리자(WP 파일 관리자 플러그인)가 워드프레스 설치 폴더(`/wp/`) 안쪽만 보여준다 — 실제 문서 루트(도메인 루트, robots.txt 가 있는 곳)는 무료판에서 접근 불가(루트경로 변경=PRO 전용, 확인함). 호스팅 FTP/cPanel 필요 |
| ③ 사이트맵 복구(robots 가 가리키는 주소가 200) | 못함 | `curl http://wellperion.com/ko/wp-sitemap.xml` → 여전히 404. 단, 실제 살아있는 사이트맵 `http://wellperion.com/sitemap_index.xml` 은 이미 200 확인 — robots.txt 의 Sitemap 줄만 고치면 됨 | robots.txt 가 실제 문서 루트에 있는 실물 파일인데, Yoast SEO 의 "파일 편집기" 도구가 이 파일의 존재 자체를 못 봄("You don't have a robots.txt file, create one here") — 워드프레스가 서브디렉터리(`/wp/`)에 설치되어 있어 Yoast 의 홈 경로 계산이 실제 서빙 경로와 어긋나는 것으로 판단됨. "여기서 만들기"를 눌러도 실측 결과 라이브 robots.txt 는 변경되지 않았다(확인함 — 되돌릴 필요 없음, 실제 반영 자체가 안 됨). 호스팅 FTP/cPanel 필요 |
| ④ Yoast 메타 설명·og:description | 완료 | `curl http://wellperion.com/`에서 `<meta name="description">`·`<meta property="og:description">` 둘 다 canon_values `official_one_liner_kr` 문장으로 확인 | — |
| ⑤ robots.txt AI 크롤러 허용(GPTBot·ClaudeBot·PerplexityBot·Google-Extended·NaverBot) | 못함 | `curl http://wellperion.com/robots.txt` → 기존 내용(`Disallow: /wp/wp-admin/` 만) 그대로, AI 크롤러 허용 줄 없음 | ③과 동일 원인(robots.txt 실물 파일에 wp-admin 경로로는 못 씀) |

## 완료/막힘 요약

- **완료 2/5**: ①JSON-LD 3종, ④메타 설명·og:description — 둘 다 실측(curl)으로 라이브 반영 확인함.
- **막힘 3/5**: ②llms.txt·③사이트맵 포인터·⑤AI 크롤러 robots — 전부 같은 근본 원인(문서 루트 파일 직접 접근 불가). wp-admin 안에서 시도 가능한 경로(Yoast 파일 편집기·WP 파일 관리자 플러그인) 둘 다 확인해봤으나 안 됨. 호스팅 FTP·cPanel 접근이 있어야 해결된다.
- 구글 리치결과 테스트 통과 캡처는 이번 실행에서 하지 않음(브라우저 스크린샷 별도 작업 필요 — 이번 배는 curl 실측까지).

## 적용 방법(재현 가능)

- JSON-LD 는 "Insert Headers and Footers" 플러그인(활성화 확인됨, `options-general.php?page=insert-headers-and-footers`) 헤더 칸에 기존 구글 애널리틱스 스크립트 아래로 추가함(기존 내용 보존).
- 메타 설명은 홈 정적 페이지(한글, post=6)의 Yoast SEO 메타박스 `yoast_wpseo_metadesc` 필드를 canon 값으로 바꾸고 Update.
- 재현 스크립트: `scripts/wp_geo_apply.py` (세션은 `scripts/wordpress_admin_playwright.py`의 기존 로그인 프로필 재사용).

## 다음(호스팅 접근 확보 시)

- FTP/cPanel 로 문서 루트에 `llms.txt` 업로드(내용 = `3. 웰페리온 가이드/home/llms.txt` 그대로).
- 문서 루트 `robots.txt` 를 아래로 교체:

```
User-agent: *
Disallow: /wp/wp-admin/
Allow: /wp/wp-admin/admin-ajax.php

User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: anthropic-ai
User-agent: PerplexityBot
User-agent: Google-Extended
User-agent: Bingbot
User-agent: Yeti
Allow: /

Sitemap: http://wellperion.com/sitemap_index.xml
```

## 배1000 후속 — FAQPage 14문답 동기 (2026-09-05 11:18)

- 라이브 헤더의 FAQPage `mainEntity`(기존 7문답)를 시모가 라이브로 올린 「자주 묻는 질문」(`http://wellperion.com/ko/faq/`, WP post 103) 14문답과 문장 그대로 동기했다. 저장소 홈 골격 `3. 웰페리온 가이드/home/index.html`은 이미 14문답으로 갱신돼 있어 그대로 두고, 이번 배는 라이브 헤더만 교체했다.
- 확인 방법: `curl http://wellperion.com/` → `"@type": "Question"` 매치 14건, "가입은 어떻게 하나요"·"용산구에 정원제로 운영하는 회원제 스포츠클럽이 있나요" 등 신규 문항 포함 확인(재현: `scripts/wp_geo_apply.py`).
- `wp_geo_apply.py`의 `step1_header_inject`는 이제 배1000 마커가 이미 있으면 append 대신 기존 블록을 통째로 새 `JSON_LD`로 교체하도록 바꿨다(다음 갱신도 같은 스크립트로 재적용 가능).

## 배1000 후속 2 — 홈 메타 설명 3곳 + JSON-LD description 동기 (2026-09-05 11:20)

- `ssot/canon_values.json`의 `official_one_liner_kr`이 GM 확정으로 갱신됨(수영·골프·P.T·체조·스쿼시·G.X → 수영·P.T·필라테스·골프·스쿼시·발레·바레). `wp_geo_apply.py`의 `HOME_META_DESC`·JSON-LD `Organization.description`·`SportsActivityLocation.description` 3곳 모두 새 값으로 맞추고, `step1_header_inject`(교체)+`step3_home_meta`(Yoast 메타박스) 순서로 라이브 재적용.
- 확인: `curl http://wellperion.com/`에서 `<meta name="description">`·`<meta property="og:description">`·`<meta name="twitter:description">` 3곳 + JSON-LD 2곳(Organization·SportsActivityLocation) 전부 새 문장과 글자 그대로 일치(문자열 매치 5건).

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01FhpdS81eJykyiHCDp4xe4Q
