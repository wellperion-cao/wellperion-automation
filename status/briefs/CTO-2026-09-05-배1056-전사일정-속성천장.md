# CTO(시토) — 배1056 전사일정 GAS 저장 실패 수리 (2026-09-05)

## 원인 (한 문장)
전사일정 GAS가 백업 3벌 롤링을 스크립트 속성창(PropertiesService)에 **일정 JSON 전체(152KB)를 4중 복제**해 저장하는 구조라, 항목이 211건으로 자라며 스크립트 전체 속성 총량 500KB 천장(766KB/512,000B 실측)을 넘겨 `save_schedule`이 거부됐다. 지시받은 "개별 속성값 9KB 천장" 가설은 실측으로 반증됐다 — SCHEDULE_SSOT 자체가 이미 152,881바이트짜리 단일 속성값으로 저장돼 있었고 정상 읽혔다(개별 9KB 한도는 이 프로젝트에서 실제로 안 걸림).

## 확인 근거
- 진단용 `props_usage_` 액션을 임시 추가·배포해 실측: 키 9개 합계 766,139B(한도 512,000B). `SCHEDULE_SSOT` 152,881B, `SCHEDULE_SSOT_BAK`/`_1`/`_2`/`_3` 각 ~152,000B(총 4벌 복제).
- 수리 전 `push_to_live`(211건) 재현 → 동일 오류 재현 확인.

## 수리
1. `propGet_`/`propSet_` 청크 헬퍼 추가 — 8000자 단위로 잘라 `key__0..`·`key__n` 저장, 옛 평문 키는 다음 저장 때 자동 정리(하위호환). `load·save·backup·droppedLog·syncCalendar` 전 호출부 교체. (지시받은 원안 — 안전망으로 유지, 회귀 없음. 다만 이것만으로는 총량 문제는 안 풀림)
2. **진짜 수리**: 백업 3벌 롤링을 속성창 대신 **Drive 파일**(`웰페리온 전사일정 백업/schedule_backup_1~3.json`)로 이관 — 이 파일에 이미 있던 증빙사진 Drive 패턴(`evidenceFolder_`)과 동일 구조 재사용. 백업 3벌이라는 안전망(GM 지적 2026-08-12 유실 방지) 자체는 그대로 유지, 저장 위치만 옮김.
3. 옛 속성 백업 키 4개(`SCHEDULE_SSOT_BAK`, `_1`, `_2`, `_3` · 총 456KB) 삭제 — `saveSchedule_` 시작부에서 매 저장마다 정리(있으면 지움·없으면 통과, 안전).
4. `scripts/gas_version_monitor.py` `_PROJECTS`에 `schedule` 프로젝트 등록(기존 instructor/forms/intake/sales/procurement과 동일 사각지대 — 미등록이면 배포 관문 `gas_deploy_guard`를 못 거치고 raw clasp deploy 우회로가 된다).

## 실측
- 배포: `.deploy-schedule/` clasp push + `clasp deploy -i AKfycbyHY37y5Cu2OGkqoODbygg5-Q-5ouCOqSOVu_HMFPlKXgudJMtiLXEtstTs3Ow4xvUn`(웹앱 @12) — 배포 관문(`gas_deploy_guard.py schedule`) 경유, drift 없음.
- `scripts/schedule_ssot.py push_to_live()`로 212건(로컬 원본) 저장 → `{'ok': True, 'total': 212}`.
- `load_schedule` 재조회 → 212건, rev 부여 확인.
- `props_usage` 재확인 → 총량 159,693B(한도 512,000B 이내), 청크 18조각(`SCHEDULE_SSOT__0~13`, `__n`) 정상 분산, 옛 백업 4벌 소멸.
- `schedule_dropped`(삭제 로그) 재조회 → 정상 응답(count 33).

## 남은 것
- 배포 후 첫 저장이 매번 `purgeOldPropBackups_`를 부르지만 옛 키가 사라진 뒤로는 매 호출이 빈 delete라 비용은 무시할 수준.
- 일정 항목이 앞으로 더 늘어 SCHEDULE_SSOT 청크 총량 자체가 500KB에 다시 근접하면(드롭로그·현재값만으로도 한계), 시트 SoT 전환(배165 점검원장 전례)까지 갈 수 있음 — 지금은 152KB 수준이라 아직 여유(약 3배 여유).
- `.deploy-check/`에는 이번에 참고한 청크 헬퍼가 실제로 없었다(점검 GAS는 시트 SoT 전환으로 다른 방식 수리) — 확인 후 새로 설계, 재사용 아님.

## 참고
- Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
