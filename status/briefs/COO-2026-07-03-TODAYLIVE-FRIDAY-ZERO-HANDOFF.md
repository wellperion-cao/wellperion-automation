# today_live 금요일 분모 0 이상치 — 시토 핸드오프 진단서 (배239 통합)

작성: 시우(COO) · 2026-07-03 · GM 라우팅 결정("서버 이상치부터, 시토 배관, 배239 통합")

## 한 줄
지원부 점검 `today_live`가 **오늘(2026-07-03, 금, dow=5) 분모(total)를 전부 0으로 반환**한다. 인접 평일은 정상. 이게 배14(화면 분모 단일출처 전환)의 선결이자, 배239 `kpi_collector` null("rows 없음")의 **상류 원인(같은 뿌리)**이다.

## 실측 증거
라이브 GAS `action=today_live&dept=support` 직접 호출(GET, 인증 게이트 없음):

| 날짜 | 요일 | dow | 서버 amTotal/pmTotal/closeTotal (분모) | done |
|---|---|---|---|---|
| 2026-06-30 | 화 | 2 | 정상 (byGender m 25/14/13 · f 21/13/10) | — |
| 2026-07-02 | 목 | 4 | 정상 (m 25/14/13 · f 19/15/0) | — |
| **2026-07-03** | **금** | **5** | **0 / 0 / 0** (m·f·all 전부 total=0) | 0 |

- `done=0`(아침 미시작)이 아니라 **분모(amTotal)=0** — 즉 오늘 마스터 항목 집합이 비어서 반환됨.
- 원본: scratchpad `today_live.json`(date=2026-07-03, dow=5, ok=true, 전 필드 0).
- `kpi_values.json` coo: `지원부_점검완료율=null`, `_note="rows 없음(점검 미시작 또는 GAS 오류)"` — 동일 증상.

## 진단 리드 (시토용)
- 로컬 소스 `.deploy-check/지원팀 일일점검.js`의 금요일 처리는 **정상으로 보임**:
  - `_TL_DOW_TOKEN[5]='fri'` (2886줄), `_tlPassesSched`(2899): `days` 비었으면 통과 → **공용(빈 sched) 항목은 요일 무관 항상 통과**해야 함.
  - 그런데 금요일 total=0 = **공용 항목마저 0** → 요일 sched 필터만으론 설명 불가.
- 배포 낡음(clasp push≠배포)만으론 설명 안 됨: **같은 라이브 엔드포인트가 목요일엔 정상** → 전 요일 깨짐이 아니라 **금요일/오늘 특정** 경로.
- 유력 가설(시토 확정 필요):
  1. `handleTodayLive`의 `week`(몇째주) 계산이 오늘 값에서 이상 → weeks 필터가 전 항목 탈락시킴(단, 공용=빈 weeks면 통과해야 하므로 이 가설은 공용까지 0인 걸 설명하려면 week 계산 예외/throw 동반).
  2. `_buildTodayMaster`가 오늘 조건에서 예외로 빈 마스터 반환(시트 read/파싱 경로).
  3. 배포된 코드가 로컬과 달라(과거 회귀분) 금요일 분기 버그 잔존.
- 확인 순서 제안: 라이브 `?action=today_live&date=2026-07-04(토)/07-06(월)` 등 여러 요일 스윕 → 금요일만인지 오늘만인지 확정 → 배포코드 vs 로컬 diff(에디터 실코드 확인) → week 계산 로그.

## 경계·주의
- **기술 배관(서버 GAS 로직·배포) = 시토.** 지표 의미·검수 = 시우.
- 라이브 GAS 배포 = GM go + 역롤백(현 안정판 기준). 로컬 모의 검증 후 1회 배포 원칙(설계서 COO-2026-06-25-CHECK-SINGLE-SOURCE-DESIGN.md).
- 회귀 금지: CHKNOTI dedup·이슈집계·제출 영속.

## 이 수리가 열어주는 것
- today_live 금요일 정상화 → ① 배14 화면 분모 전환 전제 성립(화면이 서버에 붙어도 금요일 0 안 뜸).
- 동시에 배239 coo 점검완료율 실측 개통(collector가 today_live 정상값 읽음) → 헌법 북극성 지도 coo 카드 라이브 % 점등.

## 별건(배14 ②에서 확인된 것 — 서버 수리와 별개)
- 항목 구성 드리프트: 화면 하드코딩 배열 **34개** vs 시트 **49개**(시트에 요일별 청소항목 15개 더). ①(숫자만) 전환해도 항목 세트는 여전히 배열 기준 → 장기적으로 항목 렌더도 시트化 필요(배14 ② 후속, 이번 세션 범위 확정=①+②이나 서버 선결 후).
- localStorage custom/hidden = **이미 서버 단일 진실**(HTML 1630줄 "서버 성공=단일 진실, 캐시 완전 교체"). 유실 위험 낮음 — "폐기"=낡은 캐시 제거 수준.

## [2026-07-03 배포 후 갱신 — 시우] 재배포가 금요일 today_live를 부분 변화시킴
- 배208 monthly_report 배포를 위해 clasp push -f + `clasp deploy --deploymentId AKfycbyXw4ZaA6hLK567GC7NY33Y8SvNPW6kNtrXFz2OsSdFVBmCnZP-2oD-RQiX0IpekBu1`(=/exec가 쓰는 deploymentId, @134→@135) 실행.
- **부작용**: /exec 서빙 버전이 134→135로 올라가며 금요일 today_live 동작 변화.
  - 배포 전(served @134): 금 2026-07-03 amTotal/pmTotal/closeTotal = **0/0/0**.
  - 배포 후(served @135=최신 저장 코드): 금 = **~40/0/0** (am 회복, pm·close 여전히 0). 값 40 전후로 호출마다 약간 변동.
  - 인접 요일 정상: 수 48/28/24, 목 44/29/13.
- **해석 갱신**: 금요일 버그는 **부분적으로 stale deployment**였음(served 구버전@134가 최신 저장코드보다 뒤처짐 — clasp push≠배포 함정 실증). 최신 코드는 금요일 am은 정상화하나 **금요일 pm/close=0은 잔존**(진짜 코드 로직 버그 or 데이터).
- ⚠️ 주의(진단 대비 오해 방지): 앞선 "로컬 소스 정상으로 보임"의 근거였던 clasp pull은 **최신 저장 코드**(=@135 될 코드)를 받은 것이지 served @134가 아니었음. 즉 서빙본과 최신본이 달랐던 게 핵심.
- **시토 진단 대상 좁혀짐**: 금요일(dow=5) pm/close 버킷만 0. am은 회복. handleTodayLive/_buildTodayMaster의 pm·close 회차 버킷이 금요일에만 비는 경로(회차 prefix 필터·sched·week) 추적. am/pm/close 로직 차이 비교가 단서.
