# 시모 → 시토 : 텔레그램 봇 무락 큐 쓰기 제기 (2026-07-23)

GM 2026-07-23 "시토 통해" 지시. 텔레그램 = 시토 도메인이라 시모가 직접 고치지 않고 소유주인 시토에게 제기한다.

---

## 실측 사실

- `telegram_bot/bot.py:517 _save_queue()` 가 `status/_queue.json` 을 `QueueLock` 없이 read→modify→write 로 직접 쓴다.
- 같은 파일을 쓰는 다른 7개 경로는 `scripts/queue_lock.py` 의 `QueueLock`(msvcrt 바이트락)을 쓴다 → **봇만 무방비**.
- **위험:** lost update(봇이 쓰는 사이 다른 프로세스 변경분 소실).
- **실측 유사 사례:** 2026-07-20 18:06 `6cad0dd8a` 가 2분 전 `895d4344a` 의 배9182 note·배9336 을 스테일 사본으로 되돌림.

## 참고 구현 — 새로 설계할 것 없음

- 시모가 2026-07-23 커밋 `0cec50026` 에서 `review_queue.json` 11개 쓰기 경로를 동일 `QueueLock`(lock_name 분리)으로 직렬화 완료. **같은 패턴 그대로 적용 가능.**
- 봇은 async 루프라 `asyncio.to_thread` 로 감싸 루프 비차단 처리한 선례가 그 커밋에 있다(`telegram_bot/bot.py` 의 review_queue 쓰기 3곳이 이미 그렇게 처리됨).

## 시모가 직접 안 고친 이유

텔레그램 = 시토 도메인. GM 2026-07-23 "시토 통해" 지시.

---

시모 완결 요약: 실측·위험·참고구현(커밋 0cec50026) 정리 완료(본 문서). `_save_queue()` QueueLock 배선 = 시토 담당.
