# 시토님께 — CFO 화면(매출지출현황) 구글 의존 전수 이관 요청 (3차)

**보내는 곳** CFO(나우열M) · **작성** 2026-09-14
**한 줄 요약** 매니저 지시 「구매요청뿐 아니라 CFO 탭 내용 전부 서버로 이관」. 구매요청 읽기 3종은 오늘 서버로 붙였고(킬스위치 해제 15:2x), **남은 구글 의존을 화면 코드에서 전수로 뽑았습니다.** 서버 통로가 생기는 순서대로 화면은 제가 붙입니다.

---

## 0. 오늘 끝난 것

| 항목 | 상태 |
|---|---|
| 진행중 목록 `mode=active` → `/api/proc/list` | ✅ 가동 |
| 완료·반려 목록 `mode=done`(from/to) → `/api/proc/list?mode=all` | ✅ 가동 (2차 반영분) |
| 구매 집계 `proc_summary` → `/api/sales/proc/proc_summary` | ✅ 가동 |
| 매출 집계 4종 `sales_month·sales_ops·sales_dept·labor_time` → `/api/sales/salesops/…` | ✅ 이미 서버 우선(거울) |
| 강사별 매출 `sales_instr_pub` → `/api/sales/proc/…` | ✅ 이미 서버 우선 |
| 14:06 긴급 차단 원인 | ✅ 해소 — 되밀기 TypeError(웰리 수리 `e8aa79018`) + 화면 캐시 v2 |

전부 ERP 도메인에서만, 실패·401 은 종전 GAS 폴백입니다. ⚠️ `/api/*` 는 로그인 관문 뒤라 **서버 실측은 제가 못 잽니다** — 매니저 브라우저 Network 탭 수치가 필요합니다.

---

## 1. 남은 구글 의존 — 화면 코드 전수 (2026-09-14 실측)

### A. 구글 시트 gviz 직접 읽기 5개 (서버 거울 없음 — `server/` 에 시트 ID 검색 0건)

| 순위 | 화면 요소 | 시트 | 용도·무게 | 요청 |
|:--:|---|---|---|---|
| 🔴 1 | 강사 인건비 마스터 | `1uAmZXX0…` (46행×12개월) | **첫 화면 블로킹 로드** · 총지출·KPI·부서별 전부의 뿌리 | 거울 + `GET /api/sales/sheet/labor` (gviz 응답 모양 그대로면 화면은 주소만 교체) |
| 🔴 2 | 부서별 손익 탭 | `1gCQNny8…` gid `1837105712` (26년 총 지출 분석) | 9개 서브테이블 파싱 · 시트 갱신 즉시 반영 기대 | 거울 + 통로 (5분 cron 이면 충분) |
| 🟡 3 | 최저가검토(자동) 시트 | `1rUjnf…` | 품의 💡배지 · 읽기 전용 | 거울 + 통로 |
| 🟢 4 | 매출마스터(폴백) | `18VRvDEY…` | `sales_dept` 실패 시만 | **거울 불필요 — 서버 안정 확인 후 폴백 자체 제거**(정적·부정확 시트) |
| 🟢 5 | GM 「2026년도 매출」(폴백) | `1gCQNny8…` gid `195790960` | `sales_ops` 실패 시만 | 4와 동일 처리 |

> 판정 기준: gviz 는 브라우저→구글 직행이라 ERP 서버 로그에 안 보입니다. 1·2 가 끊기면 이 화면의 구글 트래픽 대부분이 사라집니다.

### B. GAS 직행 읽기 3개

| 순위 | 항목 | 현재 | 요청 |
|:--:|---|---|---|
| 🟡 6 | 자산대장 `asset_list` | 운영요약 GAS | `proc_items` 와 같은 방식의 **자산 원장 거울** + `GET /api/proc/assets` (쓰기 `asset_*` 는 이미 `/api/write` 통과) |
| ⚪ 7 | 비품 원장 `SUPPLY_API` board | 지원부 점검 GAS(**지원부 체계와 보드 공유**) | 제 단독 판단 불가 — GM/시우 판단 후 |
| 🟢 8 | 「26년 매출 분석」 `sales_monthly` | 폴백 전용 GAS | 4·5 와 함께 폴백 제거로 소멸 |

### C. 쓰기 — `dual` 유지가 불가피한 이유와 해법

| 액션 | 화면이 쓰는 응답값 | 서버 상태 | 요청 |
|---|---|---|---|
| `add` | `no`(품의번호) | ✅ 서버 채번 있음 | — |
| `add` | **`instant`(AI 최저가 즉시 안내)** | ❌ GAS 로직 | **🔴 제출 느림의 본체** — GAS 가 네이버쇼핑 조회를 **동기**로 돌려 제출 1건이 수 초. 요청: 서버 `add` 가 `no` 를 즉시 돌려주고, 최저가 조사는 **비동기(큐)** 로 돌려 `proc_items` 또는 검토 거울에 기록 → 화면은 목록 재조회 때 💡배지로 표시. 네이버 키는 서버 env |
| `asset_issue` | `labels` | 라벨 채번 서버 있음(aws_migration 표) | 응답에 `labels` 실어주시면 전환 가능 |
| `asset_del` | `blocked·deleted` | ? | 응답 모양 GAS 와 동일하게 |
| `labor_zero`·`sales_probe` | 관리자 액션(연 수회) | — | 후순위 |

---

## 2. 제가 할 일 (통로가 생기는 순서대로)

1. A-1·A-2 통로 → 화면 로더 교체(`loadData`·`loadDeptPnl`), 실패 시 gviz 폴백 → 실측 비교
2. A-3·B-6 교체 · C 의 `instant` 서버 이전 시 제출 UX 를 "번호 즉시 → 배지 후속" 으로 변경
3. 서버 안정 확인 뒤 A-4·A-5·B-8 **폴백 3종 제거**(정적·부정확 소스 은퇴)
4. `status/aws_migration.json` 「매출·지출 원장」 줄 갱신은 시토님 표이므로 결과만 드리겠습니다

## 3. 참고 — 오늘 발견한 서버 쪽 사항 2건

- 이 PC(나우열M PC) 자동 push 가 `_queue.json` 머지에서 계속 실패하던 원인 = 머지 드라이버가 `python` 을 부르는데 이 PC 는 `py` 만 있음(스토어 스텁이 "Python" 만 출력). 로컬 설정만 `py` 로 고쳤습니다 — `install_hooks.sh` 가 `py` 우선 탐지하면 재발 없음.
- 웰리의 이름 예외(`_committer_is_nawoolm`) 뒤로 `precommit_nawoolm_domain_guard.py --selftest` 가 이 PC 에서 실패합니다(① cfo 파일 수정이 안 막혔다). 테스트가 `GIT_COMMITTER_NAME` 을 다른 이름으로 고정하면 통과할 것으로 봅니다.

| 순위 | 내용 | 담당 |
|:--:|---|---|
| 🔴 | 인건비 마스터·26년 총지출분석 시트 거울 + 통로 | 시토 |
| 🔴 | `add` 최저가 조사 비동기화(제출 즉시 `no`) | 시토 |
| 🟡 | 최저가검토 거울 · 자산 원장 거울 | 시토 |
| ⚪ | 비품 원장 이관 여부 | GM/시우 |
| — | 통로 생기는 즉시 화면 교체·실측 | CFO |
