# 시토님께 — CFO 화면(매출지출현황) 구글 의존 전수 이관 요청 (3차 · 매니저 결정 반영판)

**보내는 곳** CFO(나우열M) · **작성** 2026-09-14 16:3x
**한 줄 요약** 매니저 결정(2026-09-14): 「**최저가검토 시트만 빼고 CFO 화면의 나머지 구글 의존은 전부 서버로**」. 구매요청은 오늘 끝났고(읽기 3종 + 쓰기 서버 단독 + 거울 즉시 반영, 15:43 이후 실물품명 제출·삭제가 시트에 1분 내 반영 실측), **남은 것은 서버 거울 4개**입니다. 통로가 생기는 순서대로 화면은 제가 붙입니다.

---

## 0. 오늘 끝난 것 (참고)

| 항목 | 상태 |
|---|---|
| 구매요청 목록 `active`·`done(from/to)` → `/api/proc/list` | ✅ 가동 |
| 구매 집계 `proc_summary` → `/api/sales/proc/proc_summary` | ✅ 가동 |
| 매출 집계 5종(`sales_month·sales_ops·sales_dept·labor_time·sales_instr_pub`) | ✅ 서버 거울 우선 |
| 구매요청 쓰기 6종 서버 단독 + `mirror_proc` 즉시 반영(시토 15:43) | ✅ 실측: #138 제출→시트 1분 내 도착·삭제→1분 내 제거 |

---

## 1. 요청 — 서버 거울 4개 (우선순위 순)

| 순위 | 대상 | 현재 화면 경로 | 무게·용도 | 요청 |
|:--:|---|---|---|---|
| 🔴 1 | **강사 인건비 마스터** | 구글 시트 `1uAmZXX0…` gviz 직접(46행×12개월) | **첫 화면 블로킹 로드** · 총지출·KPI·부서별 지출 전부의 뿌리 | 시트 거울(5분 cron) + `GET /api/sales/sheet/labor` — **gviz 응답 모양(`table.rows[].c[].v`) 그대로**면 화면은 주소만 교체 |
| 🔴 2 | **26년 총지출분석** | 구글 시트 `1gCQNny8…` gid `1837105712` gviz 직접 | 📈 부서별 손익 탭(9개 서브테이블) | 거울 + 통로, 같은 모양 |
| 🟡 3 | **자산대장** `asset_list` | 운영요약 GAS 직행 | 🏷 자산관리 탭 · 라벨 발급·부착 현황 | `proc_items` 와 같은 방식의 **자산 원장 거울** + `GET /api/proc/assets` (쓰기 `asset_*` 는 이미 `/api/write` 서버 채번) |
| 🟡 4 | **비품 원장** `SUPPLY_API` board | 지원부 점검 GAS 직행 (**지원부 체계와 보드 공유**) | 비품관리 탭 · 구매진행 입고 판정 | 매니저 결정 = 이관 대상. 단 지원부 화면도 같은 보드를 읽으므로 **시우(COO) 화면 영향 통보 후** 진행 — 판정·순서는 시토님께 |

### ⛔ 제외 — 최저가검토 시트 (`1rUjnf…`)
매니저 결정으로 **이관하지 않습니다.** 현행(gviz 읽기·9시 정밀 격상 루틴 기록) 유지. 즉시조사(`instant`)는 쓰기가 서버 단독이 되면서 이미 소멸 — 화면은 "번호 즉시 → 배지 익일" UX 로 제가 정리합니다.

### 🟢 폴백 3종 — 통로 아님, 제거 예정
매출마스터(`18VRvDEY`)·GM「2026년도 매출」·「26년 매출 분석」GAS 는 서버 실패 시 폴백으로만 남아 있습니다. 위 1·2 가 붙고 일주일 안정이면 **제가 화면에서 제거**합니다(정적·부정확 소스 은퇴). 시토님 작업 없음.

---

## 2. 통로 모양 부탁

- 응답은 **원본과 같은 모양**으로(gviz 는 gviz JSON, GAS 는 GAS JSON) — 칸 이름을 새로 짓지 않으면 화면은 주소 한 줄만 바뀝니다(오늘 `list`·`proc_summary` 와 같은 원칙).
- `_source`·`_synced_at` 덧붙이는 건 환영(화면 syncMeta 에 🟢서버 표시).
- 실패·401 은 종전 구글 경로 폴백을 화면이 유지합니다.

## 3. 제가 할 일

1. 1·2 통로 → `loadData`·`loadDeptPnl` 서버 우선 + gviz 폴백 → 실측 비교 공유
2. 3·4 통로 → `loadAssets`·`supFetch` 교체
3. 안정 후 폴백 3종 제거 · `status/aws_migration.json` 「매출·지출 원장」 줄 갱신은 시토님 표이므로 결과만 드림

## 4. 참고 — 오늘 발견한 서버 쪽 2건

- 이 PC 자동 push 가 `_queue.json` 머지에서 실패하던 원인 = 머지 드라이버가 `python` 을 부르는데 이 PC 는 `py` 만 있음. 로컬 설정만 `py` 로 고쳤습니다 — `install_hooks.sh` 가 `py` 우선 탐지하면 재발 없음.
- 웰리의 이름 예외(`_committer_is_nawoolm`) 뒤로 `precommit_nawoolm_domain_guard.py --selftest` 가 이 PC 에서 실패(① cfo 파일 수정이 안 막혔다). 테스트가 `GIT_COMMITTER_NAME` 을 다른 이름으로 고정하면 통과할 것으로 봅니다.

## 5. 정리

| 순위 | 내용 | 담당 |
|:--:|---|---|
| 🔴 | 인건비 마스터 · 26년 총지출분석 시트 거울 + 통로 | 시토 |
| 🟡 | 자산 원장 거울 · 비품 원장(시우 통보 후) | 시토 |
| ⛔ | 최저가검토 — 제외(매니저 결정) | — |
| — | 통로 생기는 즉시 화면 교체·실측 · 폴백 3종 제거 | CFO |
