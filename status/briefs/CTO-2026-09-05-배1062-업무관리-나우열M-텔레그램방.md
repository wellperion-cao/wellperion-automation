# 배1062 — 텔레그램 「업무관리-나우열M」 방 등록·라우팅 (시토 · GM 지시 2026-09-05)

## 원인/배경
GM: "텔레그램 업무관리 방 만들었는데, 나우열M 관련된 업무들은 다 여기서 전달해서 정리해도 좋을 듯."
실측: 봇 로그 16:41 chat_id=-5492623600 · getChat = group 「업무관리」(GM·나우열M·봇 3명).
기존 키 「업무관리」= 8254867551(GM 개인 봇방 · GM 보고 정본)와 이름이 겹쳐 새 키로 등록.

## ① 방 등록
- `status/telegram_rooms.json` — 새 키 `"업무관리-나우열M": -5492623600` 추가(기존 `"업무관리": 8254867551`은 무변경).
- `_purpose` 에 한 줄 추가: "나우열M 관련 업무(인사·재무·지출·강습·파트너팀·업무 SSOT 안내) 전달·정리 방 · 발신 웰리".
- `status/notify_registry.json` — `tg-0750-nawool-work-relay` 항목 추가(07:50 · telegram · room=업무관리-나우열M).

## ② 아침 통 분리
- `scripts/send_ops_digest.py`
  - `NAWOOL_WHO = "나우열M"` · `_split_by_who()` — relay_items·nudge_items 에서 `who=="나우열M"`(contacts chro/cfo="나우열M" 또는 staff_to="나우열M")만 갈라낸다.
  - `build_mgr_daily_brief()` 가 분리된 두 그룹으로 ①카톡 `build_asks_section()`(나우열M 제외) ②`build_nawool_telegram_message()`(나우열M 전용, §4-2-1 형식: ▪ 제목·빈 줄 없음·맨 끝 👉)을 각각 만든다. 반환값이 3-tuple → 4-tuple(message, relay_current, reply_hits, nawool_message)로 확장.
  - `send_mgr_brief()` — 같은 07:50 회차에서 카톡 발송(`mgr_msg` 있을 때만) + 텔레그램 발송(`nawool_msg` 있을 때만)을 독립 시도, 두 채널 다 성공했을 때만 스냅샷·하트비트 기록(배536 "방마다 독립" 원칙 유지). 새 예약작업·새 스냅샷 파일 없음.
  - `send_nawool_telegram()` — `notify.telegram_send.send()` 재사용(새 발신기 없음).
- 실측(--mgr-preview): 카톡 「확인 부탁드릴 것」 통에 "나우열M" 문자열 0건(이경연 실장·이정헌 소장만 남음), 같은 회차 텔레그램 미리보기에 나우열M 4건 정상 렌더.

## ③ 회신 수신
- 봇은 이미 모든 그룹 채팅 수신 원문을 `status/_ceo_log.jsonl`(event=GROUP_MSG)에 적재하고 있었다(`telegram_bot/bot.py:_log_group_message`, authorized 게이트보다 먼저 호출). 실측: 16:41 이 방 chat_id 로 사람 발화(우열, "들어왔슴다") 1건 이미 로그에 있음.
- 새 로그 파일(`logs/telegram_in-*.jsonl`)을 새로 만들지 않고 **이 기존 로그를 그대로** 재사용(약속 L21) — `send_ops_digest._nawool_telegram_human_lines()` 가 chat_id 일치·is_owner=false 행만 읽어 `_mgr_room_human_lines()`(kakao ★중간관리자 코퍼스)와 합친다(`build_relay_message()` 1곳).
- 효과: 나우열M 이 이 방에서 답하면 `_reply_match()` 가 kakao 방 회신과 동일한 방식으로 잡아 그 배 note 에 `[회신 감지 ...]` 자동 기록(`_record_reply_hits` 재사용, 발송 성공 후에만). 봇 재기동은 하지 않음(다음 재기동은 예약된 자동 재기동에서 자연히 반영 — 이번 변경은 send_ops_digest.py 쪽뿐이라 봇 코드는 무변경, 재기동 불필요).

## ④ 실측
- `--mgr-preview` dry-run: 카톡 통 나우열M 문자열 0건 · 텔레그램 본문 미리보기 4건 정상.
- 새 방에 실제 텔레그램 발송 테스트: `[테스트] 업무관리-나우열M 방 연결 확인` 1통 전송(SEND_OK=True), 확인용 핑 메시지 message_id=10073 으로 배달 확인.
- 셀프체크: `_selfcheck_nawool_split()`, `_selfcheck_asks_section()` 통과(네트워크 없이 assert 기반).

## 남은 것
- 월요일 07:50 첫 실제 아침 회차에서 카톡·텔레그램 두 통이 동시에 정상 나가는지 웰리 검수 필요(다음 항목 참조).
