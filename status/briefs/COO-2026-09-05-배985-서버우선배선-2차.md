# 배985·1010 — 서버 우선 읽기 배선 2차 (시우 2026-09-05)

시토 배990·980(커밋 c3a8dd7c3)으로 서버 쪽 4건이 배포된 뒤, 배1010이 지정한 나머지 5장 중
진행 가능한 3장을 배선하고 검수했다. 나머지 2장은 코드는 완성했으나 저장소 도메인 가드에
막혀 커밋하지 못했다 — 아래에 상세.

## 완료 3장

| 화면 | 읽는 API | 결과 | 커밋 |
|---|---|---|---|
| 전사_거래업체.html | `/api/board/ALLDEPT_VENDOR_CATEGORIES`·`/api/misc/ops/vendor_list` | 배985 09-04 선배선(5e39bb26e) 그대로 — 재확인만, 코드 변경 없음 | (기존) |
| 전사_일정.html | `/api/misc/schedule/load_schedule`·`/api/misc/ops/vendor_list` | 배985 09-04 선배선(5e39bb26e) 그대로 — 재확인만, 코드 변경 없음 | (기존) |
| 파트너팀 체계.html | `/api/todo?limit=1000`(기본 GM행 제외) | 신규 배선. GitHub Pages 라이브검수(agent-browser): 콘솔 오류 0 · network 요청이 `script.google.com`(GAS) 직행 확인(ERP_API_ON=false 정상 — /api/todo 시도 자체가 없음) · `#workStats` 전체29·진행중3·완료25·보류1 · `#workUpdated`="ERP 동기화(실시간)" 정상 | 9ef4e121a |

패턴은 GM업무.html "리더 현황 띠"(배985 09-04 기배선)를 그대로 복제 — ERP 도메인에서만
`/api/todo`를 먼저 읽고 실패 시 GAS `action=todo_list`로 폴백, GitHub Pages에서는 그대로 GAS.

## 미완료 2장 — CHRO 도메인 차단 (BLOCKED)

**업무 현황 SSOT.html·결재 현황 SSOT.html**은 코드를 다 준비했다(둘 다 `node --check` 문법 통과):

- 업무 현황 SSOT.html — `todo_list` 읽기 2곳(본문 로드·Qeval 라이브 점수판)을 `fetchTodoList()`
  헬퍼로 통합, ERP 도메인에서 `/api/todo?limit=1000`(기본 GM행 제외 — 이 화면은 원래도 GM행 안 씀)
  우선 조회 후 실패 시 GAS 폴백.
- 결재 현황 SSOT.html — 기존 `action=todo_list&include_gm=1&gmkey=1531` 호출을 ERP 도메인에서
  `/api/todo?include_gm=1&gmkey=1531&limit=1000`으로 먼저 보내고(같은 게이트 값을 서버가 그대로
  검증), 실패 시 GAS 폴백.

`scripts/safe_commit.py`의 CHRO(시로) 도메인 차단(2026-08-05 GM 확정, `CHRO_DOMAIN_PATHS`)에
이 두 파일이 등록돼 있어 **AI가 직접 수정 커밋을 못 한다** — 소유자는 나우열M. 강행 스위치
(`WP_DOMAIN_FORCE=CHRO`)는 안내문 자체가 "★강행은 사람·리드가 판단한다 — 막혔다고 자동으로
누르지 마라"고 명시해, 스스로 누르지 않고 로컬 워킹트리에 미커밋 상태로 남겨 뒀다(비파괴 —
git 이력에 안 남고, 아무 때나 되돌리거나 그대로 커밋할 수 있다).

**판단 필요**: 이 변경은 화면 로직(카테고리·필터·렌더)은 전혀 안 건드리고 읽기 엔드포인트
하나만 서버 우선으로 바꾸는 순수 인프라 작업이라, 나우열M의 콘텐츠 판단이 필요한 영역이
아니다. 시토가 이미 인접 파일(.deploy-todo/업무&결재 현황.js)에서 "정당한 인프라 변경은
WP_DOMAIN_FORCE=CHRO 로 강행" 전례를 남겨 뒀다(가드 주석 141행). 같은 성격의 작업이라
① 강행 승인하고 그대로 커밋하거나, ② 그래도 나우열M 경유가 필요하면
`queue_dispatch.py --to ceo`로 ★중간관리자 방 전달 중 택일이 필요하다.

## 큐

배985 note에 이번 진행분·차단 상세 추가, 상태 IN_PROGRESS 유지(27장 중 남은 2장 = CHRO
차단분, 그 외 나머지는 이전 회차에서 완료 확인됨). 배1010 note에 회신 추가, DONE 보류 —
5장 중 2장이 차단 상태라 PENDING 유지. 커밋 be7ffe8be.

DONE(부분): 9ef4e121a
