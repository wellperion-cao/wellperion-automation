# [시토] 업무관리 방 에이전트 — 나우열M 쪽 CHRO 와 AI 라이브 연동 (배1068, GM 지시 2026-09-05)

GM 물음: "업무관리 방은 AI끼리 자동으로·라이브로 업무 가능해?" → 코드로 가능하게 만들었다(내일 아침 봇 재기동 후 라이브).

## 1. 무엇을 만들었나

새 파일 1개 `telegram_bot/work_room_agent.py` + `telegram_bot/bot.py` 3줄 추가(모듈 임포트 · 그룹 수신 지점 호출 1줄 · `wrk:` 콜백 등록 1줄). 큐 쓰기는 전부 `queue_lock.mutate_queue` 경유(직접 쓰기 없음).

## 2. 흐름도

| 방향 | 신호 | 분류 | 조치 |
|---|---|---|---|
| 수신 | 「반영했습니다」「완료」「등록했습니다」「처리했습니다」류(TODO 번호 없음) | done | `send_ops_digest._reply_match` 재사용으로 배 찾기 → `mutate_queue` 로 DONE + note 기록 + GM 개인 봇방 1줄(`✅ 나우열M 반영 — <제목>`) |
| 수신 | 물음표·「확인 부탁」류 | question | `ssot/canon_values.json` values[] 라벨 정확매치 시 GM 계정(`send_as_gm`)으로 답, 없으면 GM 개인 봇방에 카드(`❓ ... → 답 필요`) |
| 수신 | 「등록했습니다 TODO-…」류(TODO 번호 포함 — 완료 낱말보다 우선) | register_confirm | `/api/todo`(todo_list) 대조 → 있으면 해당 배 note 에 「SSOT 등록 확인 — 업무명 (TODO-…)」, 없으면 GM 개인 봇방 1줄 |
| 발신 | staff_to=나우열M · note 에 '업무관리 발송' 마커 없는 열린 배 | — | GM 개인 봇방에 CHRO 규격 미리보기 + [✅ 승인]/[⛔ 보류] 카드(`wrk:` 콜백) → 승인 시 `send_chro_task` 로 GM 계정 발송 + 배 note 「업무관리 발송 HH:MM」 |

분류 우선순위(`classify()`): TODO 번호 있음 > 완료 낱말 > 물음표/확인부탁 > 그 외.

## 3. 실측(2026-09-05, 봇 재기동 없이 함수 단위)

임시 [테스트] 배 1척을 큐에 넣고(mutate_queue) 실측 뒤 즉시 제거하는 방식으로 진행했다(사유는 §4 참고 — 실제 열린 배를 썼다면 중복발송이 됐다).

1. **승인 카드 실발송** — GM 개인 봇방(8254867551)에 실제 인라인 카드 발송 확인(message_id=10115).
2. **콜백 함수 직접 호출** → `send_chro_task` 로 GM 계정(Telethon) 이 「업무관리」 방(-5492623600)에 CHRO 규격 지시 1통 실제 발송 확인(message_id=10857, 텔레톤 `get_messages` 로 대조) → 테스트 문구라 즉시 삭제.
3. 배 note 에 `[업무관리 발송 17:31]` 마커 기록 확인.
4. **회신 감지** — "테스트 실측용 반영했습니다" 를 `find_ship_for_reply` 에 넣어 방금 그 배로 정확히 매치되는 것 확인(코퍼스 함수만 이 1건으로 임시 대체 — 실제 로그 파일은 안 건드림 · 운영 중엔 `_log_group_message` 가 회신을 먼저 적재하므로 이 대체가 필요 없다).
5. `_handle_done` 직접 호출 → 배 status=DONE, note 에 `[업무관리 회신 17:31] 테스트 실측용 반영했습니다` 추가, GM 개인 봇방에 `✅ 나우열M 반영 — [테스트] ...` 실제 발송 확인.
6. 테스트 배를 큐에서 제거(mutate_queue, allow_shrink=True) — 종료 후 큐 143척 그대로(원상복구 확인).
7. `python telegram_bot/test_work_room_agent.py` — classify() 6케이스 통과.

## 4. 실측 중 발견한 위험 — 옛 릴레이와의 중복 발송 가능성 (조치 완료, 후속 필요)

배 `NEXT-20260902-074440`([웰리] 부서 월간보고 자리 링크 요청, staff_to=나우열M)로 원래 실측하려 했으나, `logs/telegram_sent-2026-09-05.log` 대조 결과 이 요청이 오늘 16:43·16:45 두 차례 이미 **다른 경로**(`scripts/send_ops_digest.py` 의 옛 릴레이 → `notify.telegram_send.send`, "[AI 웰리]" 서식)로 같은 방에 나가 있었다. 이 위에 새 CHRO 카드로 세 번째 발송을 했다면 나우열M 에게 같은 요청을 세 번 보내는 사고가 됐을 것이라 실제 배 대신 임시 테스트 배로 바꿔 실측했다(§3).

**남은 문제**: `scan_new_instructions()`(신규 발신 대상 스캔)는 배 note 의 '업무관리 발송' 마커만 보고, 옛 릴레이(`build_relay_message`→`send_nawool_telegram`)가 이미 보낸 적 있는지는 모른다. 두 시스템이 **같은 배의 같은 staff_message** 를 각자 다른 서식으로 계속 보낼 수 있다 — 실측 중 이 배의 카드가 실제로 GM 개인 봇방에 발송됐고(승인 안 된 채로 남음), 뒤늦게 발견해 대기열(`work_room_pending.json`)에서 빼고 GM 봇방에 정정 안내(message_id=10118)를 보냈다. **이 배(NEXT-20260902-074440)는 손대지 않았다** — status·staff_message 전부 원래대로다.

후속 결정 필요(웰리 또는 GM): 이 CHRO 카드 플로우가 자리잡으면 옛 릴레이(`send_ops_digest.py` 의 나우열M 몫 발송)를 끄거나, 최소한 두 시스템이 같은 배를 두고 서로의 발송 여부를 알게 해야 한다.

## 4-1. §8 추적·보고 루프 (GM 확정 17:4x · status/briefs/CEO-2026-09-05-업무관리방-운영규약.md §8 반영)

CHRO 규격 발송 뒤를 사람 회신 없이 SSOT 로만 추적한다 — `_scan_todo_tracking()`(그룹 메시지 수신 시점마다 best-effort 호출, `_scan_pending_cards` 와 같은 자리):

| §8 단계 | 구현 | 실측(실제 SSOT 데이터로 확인) |
|---|---|---|
| ③ 매칭 | 발송 마커 있고 TODO 매칭 마커 없는 배 → `/api/todo?action=todo_list&include_gm=1` 전체 행에서 업무명 **정확 일치** → note 에 `[TODO 매칭 …] 업무명 → TODO번호 · 담당 · 상태 · 종료일` 기록 | 임시 배(업무명="잡코리아 기업정보 소개 문구 교체 (공식 표기 통일)")로 재현 → 실제 TODO-20260905171415092 매칭 확인(담당=나우열M·상태=진행중·종료일=2026-09-11) |
| ④ 추적 | 매칭된 배는 사람에게 안 묻고 같은 API 응답의 상태 필드만 다시 읽음 | 위와 동일 호출 재사용(같은 pass 안에서 처리 — API 두 번 안 부름) |
| ⑤ 보고 | 상태='완료' → 즉시 `_close_ship`+GM 봇방 1줄. 진행중 → `build_tracking_section()` 이 절 렌더(07:50 통에 실릴 본문 — 그 통 자체 스케줄링은 이번 배 범위 밖, §5 한계 참고) | `--dry-run` 결과: `📋 나우열M 업무 진행 현황\n▪ 잡코리아 기업정보 소개 문구 교체 (공식 표기 통일) — 진행중(2026-09-11)` |
| ⑥ 미등록 | 발송 뒤 1영업일(토·일 제외) 지나도 매칭 안 되면 같은 절에 「등록 확인 부탁」 1줄 | 임시 배(3일 전 발송·미존재 업무명)로 재현 → `❓ 등록 확인 부탁\n▪ 절대매치안될업무명즉시테스트XYZ — 등록 확인 부탁` 확인 |

4건 전부 임시 [테스트] 배(mutate_queue 로 넣고 지움)로 확인 — 실제 배는 손 안 댐. 큐는 실측 전후 동일(다른 레인의 정상 동시편집만 반영).

## 4-2. 07:50 배선 — 그룹 발송 OFF · GM 개인 봇방 1통으로 일원화 (GM 확정, 배1068 후속)

`send_ops_digest.py` 의 `send_mgr_brief()`(07:50 회차) 나우열M 절 발송 지점을 바꿨다 — 그룹(-5492623600) 발송은 `send_nawool_telegram()` 안에서 껐고(호출부는 그대로 · 회귀 최소화), 같은 자리에서 `send_gm_room_digest()` 가 GM 개인 봇방(8254867551)으로 **1통**만 보낸다. 본문 = `work_room_agent.build_gm_room_digest()`: 「업무관리 진행 N건 · 승인 대기 M건」 + §4-1 24h 등록 KPI 한 줄 + 진행 현황·미등록(「등록 확인 부탁」) 상세. 그룹 발신은 이제 GM 계정(승인 카드 뒤)만 나간다 — 방향이 완전히 갈렸다: 그룹 = GM 계정만, GM 개인 봇방 = 봇 요약만.

옛 릴레이가 오늘 그룹에 실제로 보낸 「[AI 웰리]」 2통(16:43·16:45)은 GM 계정(Telethon)으로 삭제 완료 — message_id **10802·10804**, 봇 API `deleteMessage` 는 "Message to delete not found"(기본 group·비-supergroup 특성 추정)로 실패해 `client.delete_messages()` 로 처리, 재조회로 방에서 사라진 것 확인. 대상 배 `NEXT-20260902-074440` note 에 `[업무관리 발송 2026-09-05 16:43] (소급 …)` 마커 기록 — `scan_new_instructions()` 에서 제외 확인, `work_room_pending.json` 도 빈 상태 확인. 배 자체(status·staff_message)는 안 건드림. 배 `NEXT-20260905-173526`(옛 배1077) note 에 "결정: 그룹 발신 = GM 계정 단일 경로 · 옛 릴레이 그룹 발송 OFF" 기록 후 종결.

실측(`python scripts/send_ops_digest.py --mgr-preview`, 2026-09-05 17:55 KST):
```
===== 업무관리-나우열M 그룹 발송 미리보기 (2026-09-04) =====
그룹 발송 0건 — 그룹(나우열M) 발신 경로는 work_room_agent(GM 계정) 로 일원화(GM 결정 2026-09-05)

===== GM 개인 봇방 §8 요약 미리보기 =====
업무관리 진행 0건 · 승인 대기 0건
전달→등록 24h 이내 0/0(100%) · 미등록 0건
```
현재 추적 대상 0건(추적할 배가 아직 없음)이라 0/0 으로 뜬다 — 계산 로직 자체는 §4-1 표에서 실제 SSOT 데이터(TODO-20260905171415092)로 1/1(100%) 재현 확인함.

## 5. 재기동 전 한계

- 코드는 커밋됐지만 **봇 프로세스는 재기동하지 않았다**(GM 지시 준수) — 지금 도는 봇은 옛 코드로 계속 돈다. 내일 아침 자동 재기동 뒤에야 실제 그룹 메시지에 라이브로 반응한다.
- 승인 카드 발신 트리거는 그룹 메시지 수신 시점(`handle_group_message` 끝에서 `_scan_pending_cards` 호출)뿐이다 — 방이 조용하면 새 배가 있어도 카드가 늦게 뜬다. 5분 하트비트 등 별도 트리거는 아직 없다(최소 변경 원칙 · 필요하면 후속으로 하트비트에 한 줄 추가).
- `answer_from_canon` 은 `ssot/canon_values.json` 의 라벨이 질문에 **그대로** 들어있을 때만 답한다(낱말 하나만 겹치는 느슨한 매치는 오답 위험이 실측으로 확인돼 뺐다) — 정본에 없는 질문·표현이 다른 질문은 전부 GM 개인 봇방 카드로 넘어간다(안전 우선 · 재현율은 낮음).
- §8 ⑤ 「GM 봇방 07:50 한 줄 묶음」은 §4-2 로 실제 배선 완료 — `send_ops_digest.send_mgr_brief()` 07:50 회차가 `send_gm_room_digest()` 를 불러 GM 개인 봇방에 1통 낸다(그 예약작업이 실제로 도는 것은 봇 재기동과 무관 · `--force`/스케줄 자체는 기존 그대로). 완료 자동종결(④⑤)은 그룹 메시지가 올 때마다 이미 처리되므로 07:50 을 안 기다린다 — 07:50 통은 "진행중·미등록 목록 + 요약을 하루 한 번 보는 것"만 담당.

---
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01FhpdS81eJykyiHCDp4xe4Q
