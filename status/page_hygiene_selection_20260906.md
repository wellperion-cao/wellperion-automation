# 페이지 위생 후보 선별 — 20260906 (배1092 · 웰리 판단 보조)

후보 총수 329건 · A(지움 추천) 8건 · B(보류) 321건

판정 기준: 자동감사가 각 항목에 붙인 게이트 문구를 그대로 근거로 삼았다. 게이트가 "참조 0건(선언뿐·git grep 실측) — 삭제 안전"이라고 명시한 것만 A. 그 외(참조 N건 남음 확인 필요·symbol 없어 실측 불가·이름이 검색에 안 잡힘·grep 자체 실패·판정 거부·선언 외 소비자 발견)는 전부 B — 그레이존을 A로 당겨쓰지 않았다. 원본 파일의 실제 항목 후보는 332건인데, 이미 자동승인된 3건(자율현황 페이지 "자동 적용됨" 섹션 — 소비자 0건으로 감사 스스로 자동삭제 가능 판정)은 판단할 대상이 아니라서 뺐다(332-3=329). 참고로 강습회원관리.html은 파일 자체를 못 찾아 감사가 실패한 별개 케이스로, 애초에 후보 목록에 없다.

## A. 지움 추천 (8건)

| 경로 | 왜 지워도 되나 |
|---|---|
| `3. 웰페리온 가이드/coo/check/지원부 체계.html` :: 최종 등록 기준 2026-06-24 [stale-notice] | 참조 0(선언뿐·git grep 실측) — #tab-guide 직원 구성·역할 카드 하단 <p> 요소 — 직원 목록은 JS가 동적으로 채우는데 '최종 등록 기준 2026-06-24' 날짜 배지는 정적 하드코딩 — 이후 직원 정보가 갱신돼도 배지는 2.5개월째 자동 업데이트되지 않음 |
| `3. 웰페리온 가이드/coo/check/지원부 체계.html` :: wpLdySheetSetRemark [stale-notice] | 참조 0(선언뿐·git grep 실측) — wpLdySheetSet 함수 본문 직전 주석 — 삭제된 함수 wpLdySheetSetRemark의 묘비 주석만 남음 — 함수 본체는 없고 과거 이력만 기록 |
| `3. 웰페리온 가이드/coo/check/지원부 체계.html` :: 시드 자동동기 폐지 [stale-notice] | 참조 0(선언뿐·git grep 실측) — wpLdyLoadLocal(); wpLdyMarkTodo(); 호출 직전 두 주석 블록 — 첫 주석은 이미 폐지된 자동동기 기능(setTimeout·시그니처 가드)을 설명하고, 두 번째가 2026-06-27 폐지 선언 — 함께 삭제된 기능의 이력만 남긴다 |
| `3. 웰페리온 가이드/coo/check/주차관리부 체계.html` :: pg_clean [duplicate-text] | 참조 0(선언뿐·git grep 실측) — PARK_GUIDE_SEED.guide id:pg_clean vs PARK_MANUAL_SEED.manual id:pm_clean — 가이드 탭 pg_clean과 매뉴얼 탭 pm_clean이 '주차관리인 청결관리' 내용을 문장 단위로 거의 동일하게 반복(지상·지하 바닥 청소/통행로/쓰레기/배수구/화장실/청소 장비 7개 항목 일치) |
| `3. 웰페리온 가이드/coo/check/파트너팀 체계.html` :: reviveInstr [js-function] | 참조 0(선언뿐·git grep 실측) — PAGE IIFE — applyLiveInstr 직후 — IIFE 전체에서 reviveInstr() 호출부 0건. 정의만 존재 · 소유=coo, 실행 전 도메인 승인 필요 |
| `3. 웰페리온 가이드/coo/notice/notice_template.html` :: ntool-fmt-bar .sep [css-class] | 참조 0(선언뿐·git grep 실측) — <style> — .ntool-fmt-bar button:active 룰 직후 — v2.27 이후 fmt-bar 구분자가 .fmt-group 박스화로 대체됐고, .ntool-fmt-bar 안에 class="sep" 요소가 HTML에도 JS 동적 생성에도 존재하지 않음 · 소유=coo, 실행 전 도메인 승인 필요 |
| `3. 웰페리온 가이드/wellperion_guide(main).html` :: toggleInc [js-function] | 참조 0(선언뿐·git grep 실측) — 재발방지 현황 섹션 incidents IIFE 내 선언부 — 함수가 정의되어 있으나 실제 onclick 핸들러는 동일 로직의 인라인 익명 IIFE로 구현되어 있어 toggleInc 호출부 0 · 소유=coo, 실행 전 도메인 승인 필요 |
| `3. 웰페리온 가이드/cfo/finance/매출지출현황.html` :: SUP_CATS [js-function] | 참조 0(선언뿐·git grep 실측) — JS 전역 선언부 — SUPPLY_API·SUPPLY_KEY 근처 — 선언 후 코드 어디서도 참조되지 않음. renderSupply의 분류 셀은 inCell(text input) 직접 입력, 새 품목은 cat:'비품' 하드코딩, select 드롭다운에 바인딩되지 않음. · 소유=cfo, 실행 전 도메인 승인 필요 |

## B. 보류 (321건 · 부서·사유별로 묶음)

| 경로 | 보류 이유 |
|---|---|
| coo(운영)/… 54건 | git grep 재확인 결과 참조가 남아 있어(1건 이상) 지워도 되는지 아직 확인 안 됨 |
| coo(운영)/… 25건 | 이름이 서술형이라 리포 검색에 안 걸림 — 참조 여부 확인 불가 |
| coo(운영)/… 15건 | 심볼명이 없는 HTML 주석·서술형 블록이라 grep으로 자동 확인 불가 — 사람이 직접 봐야 함 |
| coo(운영)/… 4건 | grep 명령이 문법 오류로 실패(예: --blue 같은 이름을 옵션으로 오인) — 재실측 필요 |
| cpo(회원)/… 18건 | git grep 재확인 결과 참조가 남아 있어(1건 이상) 지워도 되는지 아직 확인 안 됨 |
| cpo(회원)/… 6건 | 이름이 서술형이라 리포 검색에 안 걸림 — 참조 여부 확인 불가 |
| cpo(회원)/… 5건 | 심볼명이 없는 HTML 주석·서술형 블록이라 grep으로 자동 확인 불가 — 사람이 직접 봐야 함 |
| cpo(회원)/… 3건 | grep 재확인 결과 선언 말고 실제 쓰는 곳이 나옴 — 지우면 화면 깨짐 |
| cmo(마케팅)/… 14건 | grep 재확인 결과 선언 말고 실제 쓰는 곳이 나옴 — 지우면 화면 깨짐 |
| cmo(마케팅)/… 8건 | git grep 재확인 결과 참조가 남아 있어(1건 이상) 지워도 되는지 아직 확인 안 됨 |
| cmo(마케팅)/… 6건 | 심볼명이 없는 HTML 주석·서술형 블록이라 grep으로 자동 확인 불가 — 사람이 직접 봐야 함 |
| cfo(재무)/… 6건 | 이름이 서술형이라 리포 검색에 안 걸림 — 참조 여부 확인 불가 |
| cfo(재무)/… 4건 | git grep 재확인 결과 참조가 남아 있어(1건 이상) 지워도 되는지 아직 확인 안 됨 |
| cfo(재무)/… 1건 | grep 명령이 문법 오류로 실패(예: --blue 같은 이름을 옵션으로 오인) — 재실측 필요 |
| chro(인사)/… 39건 | git grep 재확인 결과 참조가 남아 있어(1건 이상) 지워도 되는지 아직 확인 안 됨 |
| chro(인사)/… 33건 | 심볼명이 없는 HTML 주석·서술형 블록이라 grep으로 자동 확인 불가 — 사람이 직접 봐야 함 |
| chro(인사)/… 9건 | 이름이 서술형이라 리포 검색에 안 걸림 — 참조 여부 확인 불가 |
| chro(인사)/… 2건 | grep 명령이 문법 오류로 실패(예: --blue 같은 이름을 옵션으로 오인) — 재실측 필요 |
| cto(시설)/… 2건 | 심볼명이 없는 HTML 주석·서술형 블록이라 grep으로 자동 확인 불가 — 사람이 직접 봐야 함 |
| cto(시설)/… 2건 | git grep 재확인 결과 참조가 남아 있어(1건 이상) 지워도 되는지 아직 확인 안 됨 |
| 공통(메인가이드 등)/… 23건 | 이름이 서술형이라 리포 검색에 안 걸림 — 참조 여부 확인 불가 |
| 공통(메인가이드 등)/… 16건 | git grep 재확인 결과 참조가 남아 있어(1건 이상) 지워도 되는지 아직 확인 안 됨 |
| 공통(메인가이드 등)/… 13건 | grep 재확인 결과 선언 말고 실제 쓰는 곳이 나옴 — 지우면 화면 깨짐 |
| 공통(메인가이드 등)/… 12건 | 심볼명이 없는 HTML 주석·서술형 블록이라 grep으로 자동 확인 불가 — 사람이 직접 봐야 함 |
| 공통(메인가이드 등)/… 1건 | 감사가 스스로 판정을 보류함(추적 안 되는 파일일 수 있어 안전측 처리) |

## 의심되는 것 (A로 넣었지만 확신 80% 미만)

- `3. 웰페리온 가이드/coo/check/주차관리부 체계.html` :: pg_clean [duplicate-text] — '죽은 코드 삭제'가 아니라 '두 탭(가이드 탭 vs 매뉴얼 탭)에 겹치는 안내문 병합'이다. pg_clean이라는 식별자 자체는 참조 0건이 맞지만, 실제로는 사람이 읽는 화면 문구 두 벌 중 어느 쪽을 남길지 판단이 필요해 단순 삭제로 보기엔 확신도가 낮다.
