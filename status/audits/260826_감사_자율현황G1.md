# 자율현황·G1 감사 (2026-08-26)

측정 시각 19:19~19:20 KST. 원천 = `status/_queue.json` 로컬 스냅샷(19:19:56 복사, origin/master 56fc1a6c6 과 행·상태 완전 동일 확인). 숫자는 전부 파이썬으로 원천에서 직접 셈. 파일 수정·커밋 없음.

## 1) 숫자·목록 출처 지도

### 자율현황.html (`3. 웰페리온 가이드/자율현황.html`)
| 화면 요소 | 출처 | 코드 위치 |
|---|---|---|
| 🧭 항로 섹터 척수(🚢·⚓·🏁 + 🎯/🔴 must_finish) | `status/_queue.json` — raw.githubusercontent master, GAS read_file 폴백 | `renderHangro()` :624 |
| 🤖 꼬리표 | 같은 큐의 `audience==='ai'` | `isAiShip()` :594 |
| 🗄 보관함 | 같은 큐의 `trashed===true \|\| status==='폐기'` | `renderTrash()` :752 |
| 📊 ERP 화면 완성도 배지(개수·평균·85%미만·열람) | `status/page_score.json` + `status/page_ping.json` (raw) | 인라인 스크립트 :293 |
| 📚 학습(최근 12 + 전체 N건) | `status/learning_proposals.json` | `renderLearn()` :729 |
| 📨 알림 한 장(방 N·발신 N·🟢🟡🔴⚫) | `notify_registry.json` + `telegram_rooms.json` + `scripts/kakao_rooms.json` + `erp_status.json`(automation_health) + `kakao_last_send.json` + `module_registry.json` + `notify_drift.json` | `renderAlertBoard()` :1178 |
| 🟢 CLI 라이브 | `127.0.0.1:8787/live` (GM PC 전용) | `renderCliLive()` :1382 |

### G1 (`wellperion_guide(main).html` `#G1` — GM 개인 판)
| 화면 요소 | 출처 | 코드 위치 |
|---|---|---|
| 🌟 GM 개인 북극성 문구 | `status/gm_personal_routine.json` 의 `북극성` 칸 | `renderHero()` :6018 |
| 🌤️ 오늘 하루 한 장 — 담긴 토막 N/5 · 7일 스파크 · "최근 7일 중 N일" | 같은 파일 `days{}`·`toroks[]`·`axes[]` (GAS read_file 우선 → raw 폴백) | :5897~ |
| 습관 N일째(금연·절주) | `#gm-habit-table` 정적 시작일 → 클라이언트 날짜 계산 | `habitStreaks()` :6004 |
| 개인 캘린더·적어 둔 할 일 | localStorage `gm1_unified_tasks` (GM 브라우저 한정) | :8856~ |
| (숨은 fetch) 시트 todo_list + `_queue.json` + `_queue_archive.json` | GAS + raw — **렌더 소비자 없음(§5-①)** | `gm1FetchSsot()` :9128 |

## 2) 원천 대조 (표)

| 화면 숫자 | 화면이 보일 값 | 원천 직접 센 값 | 판정 |
|---|---|---|---|
| 항로 🚢 진행중 | 8척 | IN_PROGRESS 8 | 일치 |
| 항로 ⚓ 대기·정박 | 25척 | PENDING 25 (완료·진행 외 열린 배 전부) | 일치 |
| 항로 🏁 오늘 입항 | 15척 | DONE이며 processed_at→done_at→updated_at 이 2026-08-26 인 것 15 (오늘 외 DONE 0) | 일치 |
| 🎯/🔴 must_finish 섹션 | 미표시 | must_finish_on 있는 열린 배 0척 | 일치 |
| 🤖 꼬리표 | 열린 배 4척에 표시 | audience=ai 4 · office 24 · 미지정 5 (미지정은 꼬리표 없음=의도) | 일치 |
| "N일째 멈춤" 배지 | 0건 | updated_at/enqueued_at 기준 14일+ 정지 열린 배 0 | 일치 |
| 🗄 보관함 | "비어 있습니다" | trashed 0 | 일치 |
| ERP 완성도 배지 | 43개 · 평균 86% · 85%미만 13개 · 열람 흔적 23개 · 제외 6개 | page_score.json 43건 평균 86, <85 13건, excluded_count 6 · page_ping.json opened_count 23 (오늘 07:40 수집 확인) | 일치 |
| 📚 학습 | 최근 12건 + "전체 34건" | learning_proposals.json 34건 | 일치 |
| 📨 요약줄 | 방 14개 · 발신 총 50건 · 🟢29 · 🟡19 · 🔴0 · ⚫2 | notifySeverity 로직을 파이썬으로 재현해 로컬 원천 4종에서 산출(⚫2 = kakao-0930-worksot-ops[state:dead] + win-0930-kakao-sales[kakao_last_send ok:false]) | 로직 재현 일치 |
| 📨 드리프트 배너 | 🔴 "드리프트 1건" | notify_drift.json mismatches 1건 (2026-08-26 18:57 생성 — 대조기 가동 중) | 일치 |
| 📨 신선도 주석 | "notify_registry.json 14일 전(정지 의심)" | generated_at 2026-08-12 (등록부 자체 14일 미갱신 — 화면은 정직 표기) | 일치(참고) |
| G1 담긴 토막 | 0 / 5 | 오늘 days 항목 axes = 아침식사 X·아침운동 X 뿐 → 켜진 토막 0 | 일치 |
| G1 "최근 7일 중 N일" | 7일 | 08-20~08-26 전일 기록 존재 | 일치 |
| G1 습관 | 금연 425일째 · 절주 237일째 | 2025-06-27·2026-01-01 기점 날짜 계산 | 일치 |
| G1 북극성 | 문구 표시 | `북극성` 칸 채워져 있음("사랑하는 단순한 하루 루틴 하나 …") | 일치 |
| hangro_board.py 항로(같은 큐의 두 번째 구현) | today 32 + done 15 | 화면 열린 배 33(8+25) | **1척 어긋남 → §4-①** |

측정 유의: 화면은 raw.githubusercontent(=origin/master 푸시본)를 읽는다. 감사 도중 19:18 푸시가 지나가며 로컬과 순간 1척 차이가 있었으나, 19:19 스냅샷 기준 로컬=origin 완전 동일을 diff 로 확인하고 그 스냅샷으로 셌다.

## 3) 중복 — 같은 자료를 두 곳이 그리는 것

- **화면 간 중복 없음.** 오늘(08-26) 병합으로 자율현황 「항로↔전체 배」 이중 표는 해소됐고, G1 은 배·업무 표면이 전부 삭제(08-08)돼 큐를 그리지 않는다.
- 남은 것은 화면 중복이 아니라 **구현 중복**: 같은 큐를 `자율현황.html renderHangro()`(JS)와 `scripts/hangro_board.py _classify()`(아침 보고·'현황')가 서로 다른 코드로 두 번 계산한다. 오늘 실측으로 두 구현이 1척 다른 답을 냈다(§4-①). 섹터 규칙도 다르다: py 는 🔴급한입항(마감≤3일)·🔁routine·🪝trigger 를 별도 칸으로 빼고 완료 창이 3일이지만, HTML 은 전부 ⚓/🚢에 합치고 완료 창이 오늘 하루다(오늘은 두 조건 다 해당 배가 0이라 숫자 차이 없음 — 잠복 차이).

## 4) 누락 — 어느 화면에도 안 뜨는 자료

① **배 11017 이 아침 보고(hangro_board.py) 표면에서 떨어진다.** 웰리 배 11016·11017 (★중간관리자 방 호출 — 이경연 실장님: 주차기 전화기 / 손소독제) 두 척이 **같은 task_id** `CEO-2026-08-26-중간관리자-방-호출-웰페리온-운영부-이경연-실장님-` 를 공유한다(task_id 가 제목 앞부분 슬러그라 콜론 앞이 같으면 충돌). `_classify()` 의 seen_ids 중복 차단이 두 번째 배를 조용히 버려 py 항로가 32척(화면 33척)이다. 화면(자율현황)엔 둘 다 뜨므로 GM 이 보는 곳은 무사하나, 아침 텔레그램 보고·'현황' 텍스트에서 손소독제 건이 안 보인다. 뿌리 = queue_dispatch 의 task_id 슬러그 생성.

② **GM 개인 할 일·개인 캘린더(localStorage `gm1_unified_tasks`)는 현재 표시 표면이 0이다.** 08-08 업무 표면 삭제로 `#today-task-list` 가 HTML 에서 사라져 `gm1RenderToday()` 가 첫 줄에서 반환하고, 개인 캘린더(`#gm1-cal-wrap`, 기본 display:none)를 여는 유일한 토글 버튼이 바로 그 죽은 툴바 안에서만 생성된다(:9578) — 즉 캘린더를 열 방법이 화면에 없다. GM 이 적어 둔 항목이 실제로 있는지는 GM 브라우저 localStorage 라 **못 쟀음**. 항목이 하나라도 있다면 어디에도 안 보이는 상태다. (메모리의 "'개인' 분류 localStorage 한정 → 후속" 건과 같은 뿌리.)

③ (표시 누락 아님·참고) audience 미지정 열린 배 5척 — 병합 항로 덕에 화면엔 뜨지만 🤖도 office 도 아닌 무분류로 남아 있다.

## 5) 죽은 포인터 — 문서/주석이 가리키는 곳이 실제로 안 하고 있는 것

① **G1 `gm1FetchSsot()` — 소비자 없는 6.8MB fetch 루프.** 페이지 로드 시 + 90초마다 + 탭 포커스마다(:10885~10907) GAS todo_list + `_queue.json` + `_queue_archive.json`(**6.8MB**)을 받아오는데, 유일한 최종 소비자 `gm1RenderToday()` 가 `#today-task-list` 부재로 즉시 반환한다. `gm1InProgressCount`(8)·`gm1ParkedCount`(25)·`gm1QueueFetchFailed` 경고 배너 전부 계산만 되고 그릴 곳이 없다. 주석 :5618 은 "함수를 지우면 다른 호출부가 깨진다"며 함수만 남겼지만, **fetch 루프까지 산 채로 남았다.**

② **G1 캘린더 주석 :5628 "항로 툴바 토글로 표시/숨김"** — 그 툴바가 ①로 인해 안 그려져 토글이 존재하지 않는다(§4-② 와 같은 뿌리).

③ **자율현황 로드 시 fetch 만 하고 아무도 안 쓰는 원천 6종**: `gm_aide_scan_log.jsonl`(a0)·`gm_observation_ledger.jsonl`(a1)·`audit_registry.json`(a6)·CPO churn GAS(a11 — 주석은 "시포 라이브 스탯 스트립용"인데 그 섹션은 08-15 삭제됨)·`module_silence_snapshot.json`(a15)·`kungjjak_today.json`(a20). `d.scanLog/obsLedger/auditReg/archive` 변수는 만들어지고 소비 0.

④ **`voyage_map.json` → `_northstarByRole`** — 채우기만 하고(:1452) 소비자였던 「전체 배」 표 북극성 칸이 오늘 병합에서 renderAllShips 와 함께 삭제돼 죽었다. a2 fetch 도 이제 무의미. (:539 주석이 아직 "전체 배 표 칸으로 흡수"를 가리킨다.)

⑤ **자율현황 항로 제목·주석의 "AI 배" 잔재** — h2 :270 이 여전히 "🧭 항로 (AI 배 — …)"인데 병합 후 항로는 실무 배 포함 전 배를 그린다. `isAiShip` 주석 :593 "★G1 렌더와 같은 한 줄을 써야"도 죽은 포인터 — G1 은 배를 아예 안 그린다.

⑥ **G1 주석 :9229 "자율현황이 이미 큐 전량을 그린다(audience 무관)"** — 08-03(audience 필터 도입)~08-26(병합) 사이 3주간 거짓이었고(4척만 그림), 오늘 병합으로 다시 참이 됐다. 오늘 GM 이 짚은 그 사건의 G1 쪽 흔적.

⑦ **🗂 폴드 요약문 :373 "배 정리·보관함 — 백엔드에 실제로 있는 배 전부"** — 전체 배 표 제거 후 폴드 안엔 보관함(현재 0건)만 남아 문구가 실물보다 크다(정리 버튼은 항로 표로 이동 완료).

⑧ (의도된 죽은 계산·참고) `renderAutonomyStats()` — 통계를 다 계산하고 빈 문자열을 반환한다(08-08 GM 지시로 표시만 제거). 매 렌더마다 도는 무의미 집계 루프.

### 못 잰 것
1. **GM localStorage(`gm1_unified_tasks`) 내용** — GM 브라우저에만 있어 이 세션에서 읽을 수 없음(§4-② 판단은 코드로만).
2. **📨 알림 한 장의 브라우저 실표시값** — 화면은 raw(커밋본)를 읽는데 감사는 로컬 파일로 재현함. 로컬·origin 이 큐에선 동일함을 확인했으나 알림 원천 4종(erp_status 등)은 git status 상 수정 중이라 커밋본과 순간차가 있을 수 있음.
3. **🟢 CLI 라이브 섹션** — 127.0.0.1:8787 GM PC 로컬 서버 전용이라 이 감사 환경에서 확인 불가(서버 부재 시 섹션 자동 숨김은 코드로 확인).

DONE: <어긋남 8건 / 못잰 것 3건>
