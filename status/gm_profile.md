# GM 프로필 (웰리 학습 · GM 보좌 자율화 phase 1)

- 생성: 2026-07-14 06:30:58
- 생성 두뇌: ClaudeCLI(claude-opus-4-8)
- 근거: `status/gm_observation_ledger.jsonl` + `status/_queue*.json` + `status/northstar_log.jsonl`
- 성격: 사람이 읽는 서술 프로필(일일 갱신·멱등). 라이브 부작용 0.
- 배: CEO-2026-07-02-GM-AIDE-AUTONOMY (ship 237) · 스펙: `.omc/specs/deep-interview-gm-aide-autonomy.md`

---

## 핵심 수치 카운터 (결정적 집계)

| 지표 | 값 |
|---|---|
| 총 관찰 | 87건 |
| GM 결정 이력 | 30건 |
| 추천 카드 미응답 | 3건 |
| 추천 미응답 연속 | 0일 |
| 표류(원장/라이브) | 11 / 0건 |
| 활성 큐 | 29척 (PENDING 9·IN_PROGRESS 19·DONE 0) |
| 장기 대기(30일+) | 0척 |
| 완료 아카이브 | 1336건 |

관찰 유형별: `{'missed': 3, 'approval_pattern': 1, 'decision': 30, 'repeat': 11, 'routine': 1, 'hit_rate': 1, 'activation': 1, 'auto_exec': 39}`

---

## 선호
- **가역적 자율실행을 신뢰한다(추정).** 관찰 87건 중 39건이 `auto_exec`(원복 힌트가 달린 되돌릴 수 있는 자율 조치)인데, 라이브 표류 0건·장기 대기 0척과 함께 이 조치들이 계속 유지되고 있다는 점에서 GM이 "되돌릴 수 있으면 먼저 실행" 방식을 허용·선호한다고 추정됩니다(메모리 `feedback_no_deferral_handle_now`와도 일치).
- **결정은 하되 사후 추천에는 선택적으로 응답한다.** GM 결정 이력 30건(`decision`) 대비 추천 카드 미응답 3건 — 능동적 결정은 활발하나, 웰리가 밀어 올린 추천 카드 중 일부는 응답 없이 흘려보낸 이력이 있습니다.
- **SSOT 무변경·미러 재동기 방식을 선호한다.** 다이제스트에 `mirror_sync`(원본 무변경)·`stale_republish`(순수 재생성 산출물)가 반복 등장 — 원본은 손대지 않고 파생물만 재생성하는 안전한 갱신 방식이 정착되어 있습니다.

## 습관
- **여러 배를 동시에 진행 상태로 둔다.** 활성 큐 29척 중 IN_PROGRESS가 19척(65%)으로, 착수해 두고 병렬로 굴리는 성향이 뚜렷합니다. PENDING 9·STANDBY 1로 대기는 상대적으로 적습니다.
- **완료 후 '다음'을 남기지 않고 끝내는 경향(주의 신호).** 표류(완료 후 다음 미정) 관찰이 11건 — 입항은 하지만 항로점(다음 다리)을 안 놓고 마무리하는 습관이 반복 포착됩니다. 다만 라이브 표류는 0건이라 실제 방치로 이어지진 않았습니다.
- **같은 유형의 조치가 반복된다.** `repeat` 11건 + 다이제스트상 동일한 `nudge`/`depends_resolved`/`resumable_tag` 3종 세트가 여러 배(CTO 관련)에서 반복 — 특정 유형(선행 의존 해소·담당 재촉)이 되풀이되는 작업 리듬이 있습니다.
- **큐를 방치하지 않는다.** 장기 대기(30일+) 0척 — 오래 묵히는 배가 없어, 활성 항목을 꾸준히 소화·회전시키는 습관으로 해석됩니다.

## 자주 놓치는 것
- **완료건의 '다음 수' 지정.** 표류 11건이 핵심 누락 지점 — §3-1 규칙상 표류는 "핵심조언 + 👉 다음 정하세요" 촉구 대상이므로, 웰리가 완료 직후 다음 항로를 선제 제시해야 하는 자리입니다.
- **웰리 추천 카드에 대한 회신.** 미응답 3건 — 최근 연속 미응답은 0일이라 현재 밀린 것은 없으나, 과거 추천이 결정으로 이어지지 않고 사라진 사례가 존재합니다.
- **놓침 자체는 소량.** `missed` 3건으로 절대 수는 적음 — 구조적 누락보다는 위의 '다음 수 미지정'이 실질적 빈틈입니다.
- **(관측 한계) 놓침의 구체 내용은 이 지표로 특정 불가.** `missed`가 어떤 업무였는지는 유형 카운트만 있어 서술할 수 없습니다 — 원장 원문 확인이 필요합니다.

## 루틴 준수
- **정기 유지보수 루틴이 무인으로 돈다.** `context_budget`의 `sunday_context_maintenance`(읽기전용 측정 갱신), `erp_status` 재발행이 다이제스트에 반복 — 주간·상시 루틴이 사람 개입 없이 준수되고 있습니다.
- **자율실행 게이트가 정상 작동한다.** 모든 `auto_exec`에 원복 힌트가 붙어 있고 라이브 표류 0 — "가역=자율, 라이브=GM go 대기" 게이트 원칙(메모리 `project_gm_aide_autonomy`)이 지켜지는 정황입니다.
- **`routine`·`activation`·`hit_rate` 각 1건으로 명시적 루틴 관찰은 희소.** 루틴 준수의 상당 부분이 코드/훅으로 박제되어 관찰 원장에는 드물게 남는 것으로 추정됩니다(행동=코드, 지식만 메모리 원칙과 일치).

## 핵심 수치지표 해석
- **관찰 원장의 45%(39/87)가 시스템 자율실행 로그다.** 즉 이 프로필의 '측정 가능한 GM 직접 행동'은 실제로는 `decision` 30 + `missed` 3 + `repeat`·표류 정도로 한정됩니다 — 원장이 GM 행동보다 웰리 보좌 활동을 더 많이 기록하고 있음을 정직히 밝힙니다.
- **결정 30 : 미응답 3 = 능동성 우위.** 미응답 비율이 낮고 최근 연속 미응답 0일이라, 추천-결정 루프는 현재 건강한 상태로 해석됩니다.
- **표류 11이 유일한 뚜렷한 개선 레버.** 다른 부정 지표(missed 3·라이브 표류 0·장기대기 0)가 모두 낮은 가운데 표류만 두 자릿수 — 웰리의 선제 포착 우선순위는 '완료 직후 다음 수 제시'에 두는 것이 근거상 타당합니다.
- **큐 건강도 양호.** IN_PROGRESS 19·장기대기 0은 착수율은 높고 정체는 없다는 신호 — 다만 진행중 19척이 실제 동시 처리 한계를 넘는지는 이 지표만으로 판단 불가(관측 한계)이며, 완료율·회전 속도를 별도로 봐야 합니다.
