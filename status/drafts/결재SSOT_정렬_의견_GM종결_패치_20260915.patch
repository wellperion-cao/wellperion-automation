diff --git a/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html b/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html
index a74d133c4a..386bb91f03 100644
--- a/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html	
+++ b/3. 웰페리온 가이드/coo/todo/결재 현황 SSOT.html	
@@ -754,8 +754,12 @@ function render() {
   } else if (activeSortBy === '시작일순') {
     list.sort((a, b) => String(b.startDate || '').localeCompare(String(a.startDate || '')));
   } else {
-    // 최신순(기본): 생성일(createdAt) 최신 상단 — GM 2026-06-12 (생성일 결측 시 수정일→시작일→완료시각 폴백)
-    const sortKey = t => String(t.createdAt || t['생성일'] || t.updatedAt || t['수정일'] || t.startDate || t['시작일'] || t.completeTime || '');
+    // 최신순(기본): 결재 날짜 최신 상단 — GM 2026-09-15 「완료건을 결재 날짜 기준으로 상단에 · 찾아다니기 불편」.
+    //   결재완료시각 → GM싸인 → 부서장싸인 → 생성일 순으로 첫 값을 쓴다(서명이 없는 대기 건은 종전대로 생성일).
+    //   싸인 칸 값 「YYYY-MM-DD HH:MM:SS (페이지)」 는 앞 19자만 비교한다.
+    const _sd = v => String(v || '').slice(0, 19);
+    const sortKey = t => _sd(t.completeTime || t['결재완료시각']) || _sd(t.gmSign || t['GM싸인']) || _sd(t.deptSign || t['부서장싸인'])
+      || String(t.createdAt || t['생성일'] || t.updatedAt || t['수정일'] || t.startDate || t['시작일'] || '');
     list.sort((a, b) => sortKey(b).localeCompare(sortKey(a)));
   }
 
@@ -1010,17 +1014,34 @@ function repStageOf(t) {
   var s = String((t && (t.repSign || t['대표싸인'])) || '').trim();
   if (/^https?:\/\//.test(s)) return 'done';
   if (s.toUpperCase() === 'PENDING') return 'pending';
+  if (s === 'GM종결') return 'gmfinal';     // GM 선에서 마무리한 건(GM 지시 2026-09-15) — 대표 보고 없음
   return 'none';
 }
 
+// GM 선 종결 — 결재완료 뒤 「대표 보고 올리기」와 나란히 두는 두 번째 갈래(GM 지시 2026-09-15).
+//   대표싸인 칸에 'GM종결' 표식만 적는다(todo_update 부분 갱신 · 이 화면의 본문 수정과 같은 길 · 새 GAS 액션 없음).
+//   GM업무 화면의 「GM 완료건 / 대표님 결재완료」 두 칸이 이 표식으로 가른다. 취소는 ↩(repCancel · 대표싸인 비움).
+async function gmFinalClose(id) {
+  if (!confirm('이 건을 GM 선에서 마무리합니다(대표 보고 없음).\nGM업무 「GM 완료건」 칸으로 들어갑니다.\n\n계속하시겠습니까?')) return;
+  try {
+    var res = await window.erpTodoCall(TODO_API_URL, { action: 'todo_update', id: id, '대표싸인': 'GM종결' });
+    if (res.ok) { loadTasks(); }
+    else { alert('GM 선 종결 실패: ' + (res.error || '알 수 없는 오류')); }
+  } catch (e) { alert('연결 실패: ' + e.message); }
+}
+
 // GM 결재완료(isDone) 카드의 대표 단계 UI — 3상태별 버튼/배지.
 function renderRepApprovalRow(t) {
   var stage = repStageOf(t);
   var id = t.id;
   var h = '<div class="rep-sign-row" style="margin-top:8px;display:flex;flex-wrap:wrap;align-items:center;gap:8px;"><span style="flex-basis:100%;font-size:10px;color:var(--dim);line-height:1.6;">※ 시스템 결재는 GM 종착. 대표 보고는 선택적 오프라인 종이 절차로, 결재완료와 무관.</span>';
   if (stage === 'none') {
-    // 대표 미올림 → [대표 결재 올리기]
+    // 결재완료 직후 — 두 갈래를 나란히(GM 지시 2026-09-15): [대표 보고 올리기] / [GM 결재완료·종료]
     h += '<button onclick="event.stopPropagation();repEscalate(\'' + escAttr(id) + '\')" style="display:inline-flex;align-items:center;gap:4px;font-size:11px;font-weight:700;padding:5px 12px;border-radius:7px;background:var(--purple-bg);color:var(--purple);border:1px solid rgba(167,139,218,0.4);cursor:pointer;font-family:inherit;">🏛 대표 보고 올리기(선택·종이)</button>';
+    h += '<button onclick="event.stopPropagation();gmFinalClose(\'' + escAttr(id) + '\')" style="display:inline-flex;align-items:center;gap:4px;font-size:11px;font-weight:700;padding:5px 12px;border-radius:7px;background:var(--green);color:#fff;border:1px solid var(--green);cursor:pointer;font-family:inherit;">✅ GM 결재완료·종료(대표 보고 없음)</button>';
+  } else if (stage === 'gmfinal') {
+    h += '<span class="chip" style="font-size:11px;font-weight:700;color:var(--green);background:var(--green-bg);border:1px solid rgba(11,128,67,0.3);padding:4px 10px;border-radius:7px;">✅ GM 선 종결 (대표 보고 없음)</span>';
+    h += '<button onclick="event.stopPropagation();repCancel(\'' + escAttr(id) + '\')" style="display:inline-flex;align-items:center;gap:4px;font-size:11px;font-weight:600;padding:4px 10px;border-radius:6px;background:transparent;color:var(--dim);border:1px solid var(--border);cursor:pointer;font-family:inherit;" title="GM 선 종결 취소 — 대표싸인 비움">↩ 취소</button>';
   } else if (stage === 'pending') {
     // 대표 올림(서명본 대기) → 상태 + 🖨 인쇄(대표 서명란 포함) + 📄 업로드 + 올림 취소
     h += '<span class="chip" style="font-size:11px;font-weight:700;color:var(--yellow);background:var(--yellow-bg);border:1px solid rgba(214,166,79,0.3);padding:4px 10px;border-radius:7px;">🏛 대표 보고 대기(오프라인 종이)</span>';
@@ -1709,10 +1730,9 @@ function renderOpinions(t, route, midName) {
       h += '<div class="opinion-row"><button onclick="event.stopPropagation();deleteOpinion(\'' + escAttr(t.id) + '\',' + idx + ')" class="opinion-del" title="이 의견 삭제 (GM 비밀번호)">✕</button><span class="opinion-who">' + nm + '</span><span class="opinion-time">' + tm + '</span><div class="opinion-text">' + esc(o.text || '') + '</div></div>';
     });
   }
-  var btns = '';
-  if (route.includes('부서장')) btns += '<button onclick="event.stopPropagation();addOpinion(\'' + escAttr(t.id) + '\',\'부서장\',\'' + escAttr(midName || '부서장') + '\')" class="opinion-btn">💬 ' + esc(midName || '부서장') + ' 의견 남기기</button>';
-  if (route.includes('GM')) btns += '<button onclick="event.stopPropagation();addOpinion(\'' + escAttr(t.id) + '\',\'GM\',\'김남욱GM\')" class="opinion-btn">💬 GM 의견 남기기</button>';
-  if (btns) h += '<div class="opinion-btn-row">' + btns + '</div>';
+  // 「의견 남기기」 버튼은 뺐다 — 의견은 반려할 때만 남긴다(GM 지시 2026-09-15 「의견을 남기고 최종 결재 요청을
+  //   하면 내가 어떻게 결재를 해?」). 반려 대화상자의 사유가 곧 의견이다. 승인하면서 조건을 달 일이면 반려한다.
+  //   addOpinion·deleteOpinion 함수와 기존 의견 표시는 그대로 둔다(과거 기록 보존).
   h += '</div>';
   return h;
 }
