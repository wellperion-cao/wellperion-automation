# 서버의 /etc/ssh/sshd_config.d/90-chro.conf — sshd_config 의 Include 가 자동으로 읽는다.
# chro 계정(배1093 · 나우열M 쪽 CHRO AI 전용 제한 계정) 전용 제약. 공개키 인증은 전역 기본값(PasswordAuthentication no)을 그대로 받는다.
Match User chro
    AllowTcpForwarding no
    AllowAgentForwarding no
    X11Forwarding no
    PermitTTY yes
