# 상담봇 테스트 원장 — 테스트 데이터는 자산이다 (GM 2026-09-05)

> GM: "테스트 데이터가 정말 중요할 것 같다 — 정본·정형화된 데이터가 쌓일수록 어떤 답변이든 답답한 상담을 안 한다. 세일즈 스킬도 넣자."

| 파일 | 무엇 |
|---|---|
| `{tenant}_questions.txt` | 시험 세트(한 줄 = 손님 질문). 웰페리온 30문 · 다캠 22문. 미답·👎·대표님 답에서 자란다 |
| `1_wellperion_raw_topics.txt` | 문의 원장 2,555건에서 뽑은 손님 문의 주제 120건(전화·이름 제거) — 새 질문 캐낼 광맥 |
| `log.jsonl` | **모든 테스트 문답 한 줄씩**(시각·tenant·질문·답·모델·초·검사 판정·평가·메모) — `counselbot_test.py` 가 자동 기록 |

**학습 회로(테스트 → 정본)**: ①`counselbot_test.py --set` 로 세트 실행(Max 구독 · 0원) → ②`log.jsonl` 의 답을 사람이 읽고 `--rate <id> good|bad --note "…"` → ③bad·핸드오프 = 정본에 빈칸이 있다는 뜻 → 그 사실을 대표/실무진께 받아 `tenants/{t}.json`·FAQ 에 채움 → ④세트에 그 질문 추가 → 다음 주 재실행. **답이 나빠지는 건 모델 탓이 아니라 정본에 없어서다** — 고치는 자리는 항상 정본.
